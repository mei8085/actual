# 自动银行同步交易合并去重概念报告

本文档专注于 **自动银行同步链路**（GoCardless、SimpleFin、Pluggy.ai、Enable Banking），不包含文件导入（Import）流程。

---

## 一、整体架构

```
银行服务器
    ↓ 调用同步API
sync-server（实际的同步中间服务器）
    ↓ 返回标准化交易数据
前端/loot-core 本地进程
    ├─→ syncAccount() 同步逻辑
    ├─→ normalizeBankSyncTransactions() 规范化
    ├─→ reconcileTransactions() 匹配去重
    └─→ 写入本地数据库
    ↓
前端状态更新
    ├─→ 通知 (Notification)
    ├─→ 交易列表高亮
    └─→ 账户同步状态标记
```

---

## 二、服务端匹配去重流程

### 2.1 入口函数

自动银行同步的核心入口在 `packages/loot-core/src/server/accounts/sync.ts`：

```typescript
syncAccount(
  userId,           // 用户ID
  userKey,          // 用户密钥
  acctId,           // 本地账户ID
  externalAccountId,// 银行侧账户ID
  bankId,           // 银行ID
  startingDate,     // 起始日期（首次同步可选）
  startingBalance,  // 起始余额（首次同步可选）
)
```

在应用层，通过以下 API 触发：
- `accounts-bank-sync`：普通银行同步（GoCardless、Pluggy.ai、Enable Banking）
- `simplefin-batch-sync`：SimpleFin 批量同步

### 2.2 交易规范化

银行返回的原始数据首先经过 `normalizeBankSyncTransactions` 函数（`sync.ts:427-512`）处理：

#### 规范化关键字段

| 输出字段 | 来源 | 说明 |
|---------|------|------|
| `imported_id` | `transactionId` 或 `${account}-${internalTransactionId}` | 用于精确匹配的唯一标识 |
| `imported_payee` | 银行原始收款人名称（trim 后） | 保留银行原始名称，供规则和展示使用 |
| `cleared` | `booked` 字段 | 是否已在银行清算 |
| `raw_synced_data` | `JSON.stringify(原始交易)` | 完整原始数据，用于自定义映射和排查 |
| `amount` | 金额字段 × 100 后四舍五入 | 内部统一整数表示 |

#### 字段自定义映射

用户可在"银行同步设置"中自定义以下字段的映射来源：

- **Date**：可映射自 `date`、`bookingDate`、`valueDate`、`postedDate`、`transactedDate` 等
- **Payee**：可映射自 `payeeName`、`creditorName`、`debtorName`、`remittanceInformationUnstructured`、`merchant.name` 等
- **Notes**：可映射自 `notes`、`remittanceInformationUnstructured`、`additionalInformation`、`merchant.name` 等

映射配置存储在账户级偏好中，key 格式为 `custom-sync-mappings-${accountId}`。

### 2.3 匹配去重核心逻辑

匹配发生在 `reconcileTransactions` 函数中（`sync.ts:538-696`），其内部调用 `matchTransactions`（`sync.ts:698-897`）。

#### 匹配维度

| 维度 | 类型 | 约束 |
|------|------|------|
| `account` | 账户ID | 必须相同（仅在同一账户内匹配） |
| `imported_id` | 字符串 | 精确匹配（最高优先级） |
| `amount` | 整数（分） | 必须完全相等 |
| `date` | YYYY-MM-DD | ±7天范围内 |
| `payee` | 收款人ID | 增强匹配（第二优先级） |

#### 匹配优先级（三步渐进式）

**Pass 1：`imported_id` 精确匹配（最高）**

```sql
SELECT * FROM v_transactions
WHERE imported_id = ? AND account = ?
```

- 这是最可靠的匹配方式
- 银行对每笔交易生成唯一标识符（`transactionId`）
- 优先保证银行交易的唯一标识不重复

**Pass 2：模糊匹配 + Payee 匹配（中等）**

当 `imported_id` 未命中时：

1. **构建候选集**：
   - 日期范围：目标日期 ± 7 天
   - 金额：完全相等
   - 账户：同一账户
   - 候选集按"与目标日期的时间差"从近到远排序

2. **严格 ID 检查（strictIdChecking）**：
   - 如果导入交易有 `imported_id`，且本地交易也有 `imported_id`，则跳过（避免不同银行 ID 被误合并）
   - **银行同步源账户自动关闭严格 ID 检查**（因为银行可能对同一笔交易生成不同 ID）

3. **Payee 匹配**：在候选集中查找 payee ID 相同且尚未被匹配的交易

**Pass 3：模糊匹配 + 首个可用（最低）**

- 如果 payee 匹配也未命中，则直接选取候选集中第一个尚未被匹配的交易
- 通过 `hasMatched` Set 保证一笔本地交易最多被匹配一次

#### 匹配流程图

```
银行交易 T
    ↓
有 imported_id?
    ├─ Yes → Pass 1: 精确匹配 existing.imported_id
    │         ├─ 命中 → 匹配成功
    │         └─ 未命中 → 继续 Pass 2
    └─ No → 直接 Pass 2
              ↓
Pass 2: 构建候选集
        条件: account相同, amount相等, date在±7天内
        严格ID检查: 双方都有imported_id则跳过
        按日期距离排序 (近→远)
              ↓
候选集非空?
    ├─ Yes → 尝试 payee 匹配
    │         ├─ 命中 → 匹配成功
    │         └─ 未命中 → Pass 3: 取首个未匹配的
    └─ No → 匹配失败 → 作为新增交易
```

### 2.4 冲突回退规则

当匹配成功，需要更新本地交易时，采用 **"现有值优先"** 策略（`sync.ts:585-594`）：

| 字段 | 保留规则 | 说明 |
|------|---------|------|
| `imported_id` | 新值 | 用于下次同步匹配 |
| `imported_payee` | 新值 | 保留银行原始名称 |
| `payee` | 现有 > 新 > null | 保护用户设置的收款人 |
| `category` | 现有 > 新 > null | 保护用户设置的分类 |
| `notes` | 现有 > 新 > null | 保护用户备注 |
| `cleared` | 现有 OR 新 | 任一为已清算则保持已清算 |
| `raw_synced_data` | 现有优先 | 保留首次同步的原始数据 |
| `date` | 视配置而定 | 默认保留现有，若 `sync-update-dates-${acctId}=true` 则用新值 |

**已对账交易保护**：
- 如果本地交易 `reconciled = true`（已对账），则 **跳过更新**
- 原因：对账是用户的重要确认动作，不应被自动同步覆盖

### 2.5 新增 vs 更新的判定

```typescript
// reconcileTransactions 返回结果
{
  added: string[],    // 新增的交易 ID 列表
  updated: string[],  // 更新（匹配成功）的交易 ID 列表
}
```

**新增交易**：
- 匹配失败（`match` 为空）
- 或 `forceAddTransaction = true`（文件导入时用户可选择不合并）

**更新交易**：
- 匹配成功且字段有变化

**忽略交易**：
- 匹配成功但已对账
- 或字段无变化（不产生任何操作）

---

## 三、前端状态变化与用户可见效果

### 3.1 同步触发与执行流程

用户通过以下方式触发自动银行同步：

1. **手动同步**：在账户详情页点击"同步"按钮
2. **启动时自动同步**：应用启动时可配置自动同步所有已链接账户

#### 关键流程（`useSyncAndDownloadMutation`，`mutations.ts:758-802`）

```
用户点击同步
    ↓
步骤 1: 先同步云端（多设备同步）
    目的：获取其他客户端已创建的交易，避免重复导入
    ↓
步骤 2: 银行同步（download）
    ├─ SimpleFin: 调用 simplefin-batch-sync（批量）
    └─ 其他: 调用 accounts-bank-sync（逐个账户）
    ↓
步骤 3: 如有新增，再次同步云端
    目的：让其他设备知晓新导入的交易
```

### 3.2 服务端返回结果结构

```typescript
// SyncResponseWithErrors（app.ts:1217-1219）
{
  errors: SyncError[],           // 同步错误列表
  newTransactions: string[],     // 新增的交易 ID
  matchedTransactions: string[], // 已匹配（更新）的交易 ID
  updatedAccounts: string[],     // 有新增交易的账户 ID
}
```

### 3.3 前端状态更新

#### 账户同步状态

同步过程中，前端维护 `accountsSyncing` 状态，显示哪些账户正在同步：

```typescript
// 开始同步
dispatch(setAccountsSyncing({ ids: [acct1, acct2, acct3] }));

// 同步完成一个后
dispatch(setAccountsSyncing({ ids: [acct2, acct3] })); // 进度式移除

// 全部完成
dispatch(setAccountsSyncing({ ids: [] }));
```

#### 交易状态（Redux Store）

同步结果通过 `setNewTransactions` action 存入 Redux（`transactionsSlice.ts:38-49`）：

```typescript
// Redux State
transactions: {
  newTransactions: [id1, id2, ...],     // 新增交易 ID 列表
  matchedTransactions: [id3, id4, ...], // 已匹配交易 ID 列表
  lastTransaction: Transaction | null,
}
```

**状态累积**：每次同步会将新结果追加到现有列表中（不是替换）。

**状态清除**：当用户编辑某笔交易时，通过 `updateNewTransactions` 将其从两个列表中移除（高亮消失）。

#### 账户成功/失败状态

```typescript
// 同步成功
dispatch(markAccountSuccess({ id: accountId }));

// 同步失败（仅针对真实的 SyncError）
if (error.type === 'SyncError') {
  dispatch(markAccountFailed({
    id: accountId,
    errorType: error.category, // e.g., 'RATE_LIMIT_EXCEEDED'
    errorCode: error.code,
  }));
}
```

### 3.4 用户界面可见效果

#### 通知（Notification）

**成功场景**：
- 无专门的成功通知
- 交易列表自动刷新，新增/匹配的交易有高亮

**失败场景**：
- 显示错误通知，包含具体错误信息
- 特殊情况：`RATE_LIMIT_EXCEEDED` 会显示友好提示：
  > "Failed syncing account {账户名}. Rate limit exceeded. Please try again later."

#### 交易列表高亮

交易列表通过 `isNew` 和 `isMatched` 函数判断每笔交易的状态：

```typescript
// Account.tsx:938-944
isNew = (id) => this.props.newTransactions.includes(id);
isMatched = (id) => this.props.matchedTransactions.includes(id);
```

然后传递给 `TransactionsTable`，最终传递给每一行：

```typescript
// TransactionsTable.tsx:2442-2444
<Transaction
  added={isNew?.(trans.id)}      // 是否新增
  matched={isMatched?.(trans.id)} // 是否已匹配
  ...
/>
```

**视觉效果**：
- **新增交易**（`added=true`）：高亮显示，提示用户这是刚从银行同步来的新交易
- **已匹配交易**（`matched=true`）：另一种高亮样式，提示用户这笔本地交易已与银行同步数据匹配
- 用户点击编辑任意一笔交易后，高亮消失（状态从 Redux 中移除）

#### 账户同步状态标记

- **同步中**：账户列表显示加载/同步指示器
- **同步失败**：账户显示失败状态，可能包含错误类型标记
- **最后同步时间**：账户的 `last_sync` 字段被更新为同步完成的时间戳

---

## 四、关键账户级偏好配置

以下偏好影响自动银行同步的行为（定义于 `prefs.ts`，通过 `useSyncedPref` 管理）：

| 偏好 Key | 类型 | 默认值 | 作用 |
|---------|------|--------|------|
| `sync-import-pending-${acctId}` | boolean | true | 是否导入待清算（未 booked）的交易 |
| `sync-import-notes-${acctId}` | boolean | true | 是否导入交易备注 |
| `sync-reimport-deleted-${acctId}` | boolean | true | 是否重新导入已删除的交易 |
| `sync-update-dates-${acctId}` | boolean | false | 匹配成功时是否用银行日期更新本地日期 |
| `sync-import-transactions-${acctId}` | boolean | true | 是否实际导入交易（可设为 false 仅同步余额） |
| `custom-sync-mappings-${acctId}` | string | 默认映射 | 自定义字段映射（JSON 序列化） |

这些偏好可在"银行同步设置"模态框中修改（`EditSyncAccount.tsx`）。

---

## 五、首次同步（Initial Sync）的特殊处理

对于新账户的首次同步（`initialSync = true`）：

### 5.1 起始余额处理

```
银行当前余额: B
银行交易总金额: T（所有交易 amount 之和）

起始余额交易金额 = B - T
```

1. 创建一笔起始余额交易（`starting_balance_flag = true`）
2. 金额为倒推得出的期初余额
3. 然后按正常流程 reconcile 导入的交易
4. 最终账户余额应与银行余额一致

### 5.2 起始日期（startingDate）

用户可在链接账户时指定起始日期：
- 仅同步该日期之后的交易
- 起始余额相应调整

---

## 六、关键代码速查

| 功能 | 文件 | 函数/类型 | 行号 |
|------|------|-----------|------|
| 同步主入口 | `loot-core/src/server/accounts/app.ts` | `accountsBankSync` | 1221-1288 |
| SimpleFin 批量同步 | `loot-core/src/server/accounts/app.ts` | `simpleFinBatchSync` | 1290-1412 |
| 同步响应处理 | `loot-core/src/server/accounts/app.ts` | `handleSyncResponse` | 1117-1144 |
| 同步核心逻辑 | `loot-core/src/server/accounts/sync.ts` | `syncAccount` | 主函数 |
| 银行交易规范化 | `loot-core/src/server/accounts/sync.ts` | `normalizeBankSyncTransactions` | 427-512 |
| 交易匹配去重 | `loot-core/src/server/accounts/sync.ts` | `matchTransactions` | 698-897 |
|  reconcile 主逻辑 | `loot-core/src/server/accounts/sync.ts` | `reconcileTransactions` | 538-696 |
| 字段更新规则 | `loot-core/src/server/accounts/sync.ts` | 构建 updates 对象 | 585-594 |
| 前端同步 Mutation | `desktop-client/src/accounts/mutations.ts` | `useSyncAccountsMutation` | 549-690 |
| 同步+下载 Mutation | `desktop-client/src/accounts/mutations.ts` | `useSyncAndDownloadMutation` | 758-802 |
| 同步响应前端处理 | `desktop-client/src/accounts/mutations.ts` | `handleSyncResponse` | 692-752 |
| Redux 交易状态 | `desktop-client/src/transactions/transactionsSlice.ts` | `setNewTransactions` | 38-49 |
| 银行同步设置 UI | `desktop-client/src/components/banksync/EditSyncAccount.tsx` | `EditSyncAccount` | 139-276 |
| 字段映射配置 | `desktop-client/src/components/banksync/EditSyncAccount.tsx` | `mappableFields` | 36-98 |

---

## 七、总结

### 匹配去重核心原则

1. **精确优先，模糊兜底**：`imported_id` 精确匹配 → 日期+金额+payee 模糊 → 日期+金额兜底
2. **用户数据保护**：现有 payee、category、notes 优先，银行同步数据不覆盖用户手动设置
3. **已对账锁定**：`reconciled=true` 的交易不被自动更新
4. **银行源宽松**：银行同步源自动关闭严格 ID 检查，适应银行可能的 ID 变化

### 前端用户体验

1. **状态透明**：同步中/成功/失败状态清晰可见
2. **视觉反馈**：新增和已匹配交易有不同的高亮效果
3. **灵活配置**：可自定义字段映射和同步偏好（是否导入待清算、是否更新日期等）
4. **渐进清除**：用户编辑交易后高亮自动消失，不干扰日常使用
