# 银行同步交易合并去重概念报告

## 概述

Actual Budget 支持多种银行同步方式（GoCardless、SimpleFin、Pluggy.ai、Enable Banking），从外部账户下载交易数据后，需要与本地账本中的现有交易进行匹配和去重，确保数据一致性，避免重复记账。

本文档梳理了以下核心内容：
1. 参与匹配的维度及匹配优先级
2. 多候选或字段冲突时的回退策略
3. 同步数据到本地账本前后的关键状态变化
4. 用户前端看到的合并效果

---

## 一、交易匹配与去重机制

### 1.1 参与匹配的核心维度

匹配交易时涉及以下关键字段（`TransactionEntity` 类型，定义于 `packages/loot-core/src/types/models/transaction.ts`）：

| 字段 | 类型 | 说明 | 匹配角色 |
|------|------|------|----------|
| `imported_id` | string | 银行提供的交易唯一标识符 | **主键匹配** |
| `account` | string | 账户 ID | **必须** 仅在同一账户内匹配 |
| `amount` | number (整数，无小数) | 交易金额 | **必须** 金额必须相等 |
| `date` | string (YYYY-MM-DD) | 交易日期 | **日期窗口** ±7 天范围 |
| `payee` | string | 收款人/付款人 ID | **增强匹配** 第二优先级 |
| `imported_payee` | string | 原始收款人名称（导入时保留） | 来源标识 |
| `cleared` | boolean | 是否已清算 | 状态字段 |
| `reconciled` | boolean | 是否已对账 | 锁定标志 |

### 1.2 匹配优先级（从高到低）

匹配流程在 `matchTransactions` 函数中实现（`packages/loot-core/src/server/accounts/sync.ts:698-897`），采用 **三步渐进式匹配策略**：

#### **Pass 1: 精确 ID 匹配（最高优先级）**
- 使用 `imported_id` 作为精确匹配键
- 查询条件：`imported_id = ? AND account = ?`
- 这是最可靠的匹配方式，优先保证银行交易的唯一标识不重复

> **代码位置**：`sync.ts:746-758`

```typescript
// 伪代码示意
if (trans.imported_id) {
  match = await db.query(
    `SELECT * FROM v_transactions 
     WHERE imported_id = ? AND account = ?`,
    [trans.imported_id, acctId]
  );
}
```

#### **Pass 2: 模糊匹配 + Payee 匹配（中等优先级）**
当 `imported_id` 未命中时，构建模糊匹配候选集：

**候选集筛选条件**：
- 日期范围：目标日期 ±7 天
- 金额：完全相等
- 账户：同一账户
- 额外约束（strictIdChecking 模式）：若双方都有 `imported_id` 且之前未匹配，则跳过该笔（避免不同银行 ID 被误合并）

**候选集排序**：按与目标日期的时间差从近到远排序

**匹配逻辑**：在候选集中查找 payee ID 相同且尚未被匹配的交易

> **代码位置**：`sync.ts:861-874`

```typescript
// 伪代码示意
fuzzyDataset = db.query(
  `SELECT ... FROM v_transactions
   WHERE date BETWEEN ? AND ? 
     AND amount = ? 
     AND account = ?`,
  [date-7d, date+7d, amount, acctId]
);

// 按日期距离排序
fuzzyDataset.sortByDateDistance();

// 第二遍：payee 匹配
match = fuzzyDataset.find(row => row.payee === target.payee && !matched);
```

#### **Pass 3: 模糊匹配 + 首个可用（最低优先级）**
如果 payee 匹配也未命中，则直接选取候选集中第一个尚未被匹配的交易

> **代码位置**：`sync.ts:880-889`

```typescript
// 伪代码示意
match = fuzzyDataset.find(row => !matched);
```

### 1.3 多候选或字段冲突时的回退策略

#### **情况 A：同一账户内有多笔金额相同、日期相近的交易**

**处理方式**：
1. 候选集按日期距离排序（最近优先）
2. 通过 `hasMatched` Set 保证一笔本地交易最多被匹配一次
3. 先尝试 payee 匹配，再回退到"首个可用"

**场景示例**：
- 本地有两笔 $100 交易：日期分别为 4月5日、4月8日
- 银行同步来一笔 $100 交易：日期 4月6日
- 匹配结果：优先匹配 4月5日（更近）

#### **情况 B：严格 ID 检查模式下的冲突**

**配置项**：`strictIdChecking = true`（非银行同步源账户默认值）

**约束**：
- 如果导入交易有 `imported_id`，则不会匹配已有 `imported_id` 的本地交易
- 原因：银行可能对同一笔交易生成不同 ID，需要宽松模式处理

**特殊情况**：银行同步源账户（`account_sync_source` 已设置）自动关闭严格 ID 检查
> **代码位置**：`sync.ts:976-979`

```typescript
const useStrictIdChecking = !acctRow.account_sync_source;
```

#### **情况 C：字段值冲突（合并时的保留策略）**

当匹配成功，需要更新本地交易时，采用 **"已有值优先"** 策略（`sync.ts:585-594`）：

| 字段 | 保留规则 |
|------|----------|
| `payee` | 现有值 > 新值 > null |
| `category` | 现有值 > 新值 > null |
| `notes` | 现有值 > 新值 > null |
| `cleared` | 现有已清算 或 新值已清算（OR 逻辑） |
| `imported_id` | 新值（用于下次同步匹配） |
| `imported_payee` | 新值（保留银行原始名称） |
| `raw_synced_data` | 现有值优先（保留首次同步的原始数据） |

> **代码位置**：`sync.ts:585-594`

```typescript
const updates = {
  imported_id: trans.imported_id || null,
  payee: existing.payee || trans.payee || null,
  category: existing.category || trans.category || null,
  imported_payee: trans.imported_payee || null,
  notes: existing.notes || trans.notes || null,
  cleared: existing.cleared || trans.cleared || false,
  raw_synced_data: existing.raw_synced_data ?? trans.raw_synced_data ?? null,
};
```

#### **情况 D：已对账交易保护**

- 如果本地交易 `reconciled = true`（已对账），则 **跳过更新**
- 原因：对账是用户的重要确认动作，不应被自动同步覆盖
- 结果：该笔同步交易在预览中标记为 `ignored = true`

> **代码位置**：`sync.ts:571-575`

---

## 二、同步前后的关键状态变化

### 2.1 交易状态生命周期

```
银行原始数据
    ↓ 下载
未同步（临时对象，含 transactionId、booked 标志等）
    ↓ 规范化 normalizeBankSyncTransactions()
规范化交易（含 imported_id、cleared、imported_payee、raw_synced_data）
    ↓ 规则引擎 runRules()
应用自动规则后的交易（payee/category 可能已被规则修改）
    ↓ 匹配 reconcileTransactions()
    ├─→ 匹配成功 → 更新现有交易（added 列表为空，updated 列表有值）
    └─→ 匹配失败 → 新增交易（added 列表有值，updated 列表为空）
```

### 2.2 银行同步交易的特有字段

银行同步的交易在规范化阶段（`normalizeBankSyncTransactions`，`sync.ts:427-512`）会设置以下字段：

| 字段 | 值来源 | 用途 |
|------|--------|------|
| `imported_id` | 优先 `transactionId`，否则 `${account}-${internalTransactionId}` | 精确匹配主键 |
| `imported_payee` | 银行返回的原始收款人名称（trim 后） | 保留原始信息，供规则引擎使用 |
| `cleared` | `Boolean(trans.booked)` | 是否已在银行清算 |
| `raw_synced_data` | `JSON.stringify(trans)` | 完整原始数据，供排查问题 |
| `amount` | 整数化后的金额（乘 100） | 内部统一表示 |

### 2.3 新增 vs 更新的判定

在 `reconcileTransactions` 函数中（`sync.ts:538-696`）：

**判定结果**：
- **Added（新增）**：`match` 为空 或 `forceAddTransaction = true`
- **Updated（更新）**：`match` 存在且字段有变化
- **Ignored（忽略）**：`match` 存在但已对账 或 无字段变化

**新增交易时的默认值**：
- `id`：新生成 UUID
- `cleared`：`trans.cleared ?? defaultCleared`（银行同步时默认为 true）
- `sort_order`：按导入时间生成（确保显示顺序）

### 2.4 相关账户级配置项

同步行为受以下账户级偏好控制（定义于 `packages/loot-core/src/types/prefs.ts`）：

| 偏好 Key | 类型 | 默认值 | 作用 |
|----------|------|--------|------|
| `sync-import-pending-${acctId}` | boolean | true | 是否导入待清算交易（未 booked） |
| `sync-import-notes-${acctId}` | boolean | true | 是否导入交易备注 |
| `sync-reimport-deleted-${acctId}` | boolean | true | 是否重新导入已删除的交易 |
| `sync-update-dates-${acctId}` | boolean | false | 匹配成功时是否用银行日期更新本地日期 |
| `sync-import-transactions-${acctId}` | boolean | true | 是否实际导入交易（可仅同步余额） |

### 2.5 起始余额处理（首次同步）

对于新账户的首次同步（`initialSync = true`）：

1. 插入一笔 **起始余额交易**（`starting_balance_flag = true`）
2. 金额 = 银行当前余额 - 所有交易金额累加（倒推期初余额）
3. 然后按正常流程 reconcile 导入的交易
4. 最终账户余额应与银行余额一致

> **代码位置**：`sync.ts:999-1073`

---

## 三、前端用户看到的合并效果

### 3.1 文件导入模态框（ImportTransactionsModal）

文件导入时（`packages/desktop-client/src/components/modals/ImportTransactionsModal/ImportTransactionsModal.tsx`），用户可以在预览阶段看到匹配结果。

#### 预览时的交易状态标记

| 状态标记 | 显示方式 | 含义 |
|----------|----------|------|
| `existing = true` | 特殊样式高亮 | 该导入交易将与现有交易合并 |
| `ignored = true` | 灰显/跳过 | 因已对账或无变化而忽略 |
| `tombstone = true` | 删除标记 | 某些场景下的逻辑删除（较少见） |
| `isMatchedTransaction` | 成对显示 | 预览列表中会同时显示"将被更新的现有交易"，方便对比 |

#### 用户交互：三态选择

对于匹配到现有交易的导入项，复选框有三种状态循环（`ImportTransactionsModal.tsx:533-573`）：

```
状态1: selected=true, selected_merge=true
  → 默认状态：导入并合并（更新现有交易）
  
状态2: selected=true, selected_merge=false
  → 强制新增（不合并，作为两笔独立交易）
  
状态3: selected=false, selected_merge=false
  → 完全跳过（不导入这笔）
```

**触发条件**：点击复选框在三个状态间切换

#### 预览与实际导入的差异

**预览阶段**（`onImportPreview`，`ImportTransactionsModal.tsx:740-822`）：
- 调用 `useImportPreviewTransactionsMutation`
- 生成 `updatedPreview` 列表，包含 `transaction`（导入）、`existing`（匹配到的现有）、`ignored`、`tombstone`
- 不修改数据库

**实际导入阶段**（`onImport`，`ImportTransactionsModal.tsx:577-736`）：
- 根据用户选择构建 `finalTransactions`
- 若用户选择"不合并"，设置 `forceAddTransaction = true`
- 调用 `useImportTransactionsMutation` 执行实际写入

### 3.2 交易列表中的视觉效果

合并完成后，在交易列表中：

**成功合并的效果**：
- 原有交易的 `imported_id`、`imported_payee` 被更新
- 分类、收款人等信息按"现有优先"规则保留
- 用户手动设置的分类不会被银行同步覆盖（除非原分类为空）

**新增交易的效果**：
- 显示为新交易记录
- 带有 `imported_payee`（银行原始名称）
- `cleared` 状态由银行数据决定

**拆分交易的特殊处理**：

在手动合并（`mergeTransactions`，`packages/loot-core/src/server/transactions/merge.ts`）时：
- 如果被保留的交易没有拆分、被删除的交易有拆分，则将拆分转移到保留交易
- 保留交易变为 `is_parent = true`，拆分指向新的 parent_id
- 分类字段：父交易设为 null（拆分才有分类）

> **代码位置**：`merge.ts:64-85`

---

## 四、手动合并交易（非同步场景）

除了银行同步时的自动合并，用户还可以手动选择两笔金额相同的交易进行合并（快捷键 G 或菜单操作）。

### 4.1 手动合并的保留/删除判定

`determineKeepDrop` 函数（`merge.ts:116-141`）按以下优先级决定哪笔保留：

1. **有 `imported_id`（银行同步）** 的优先保留
2. 否则，**有 `imported_payee`（文件导入）** 的优先保留  
3. 否则，**日期较早** 的保留

### 4.2 合并时的字段填充

保留交易的空字段会从被删除交易填充：
- `payee`、`category`、`notes`
- `cleared`、`reconciled`（OR 逻辑：任一为 true 则为 true）
- `schedule`（计划链接）

### 4.3 手动合并的限制

- 仅支持两笔交易合并
- 金额必须完全相等
- 不会自动与银行同步机制联动（手动合并是独立操作）

---

## 五、关键代码文件速查

| 功能 | 文件路径 | 核心函数/类型 |
|------|----------|---------------|
| 账户同步主流程 | `packages/loot-core/src/server/accounts/sync.ts` | `syncAccount()`, `reconcileTransactions()`, `matchTransactions()` |
| 交易匹配逻辑 | `packages/loot-core/src/server/accounts/sync.ts` | `matchTransactions()` (L698-L897) |
| 银行交易规范化 | `packages/loot-core/src/server/accounts/sync.ts` | `normalizeBankSyncTransactions()` (L427-L512) |
| 手动合并 | `packages/loot-core/src/server/transactions/merge.ts` | `mergeTransactions()`, `determineKeepDrop()` |
| 交易模型定义 | `packages/loot-core/src/types/models/transaction.ts` | `TransactionEntity` |
| 导入交易模型 | `packages/loot-core/src/types/models/import-transaction.ts` | `ImportTransactionEntity` |
| 同步偏好定义 | `packages/loot-core/src/types/prefs.ts` | `SyncedPrefs` |
| 前端导入模态框 | `packages/desktop-client/src/components/modals/ImportTransactionsModal/ImportTransactionsModal.tsx` | `ImportTransactionsModal` |
| 匹配测试用例 | `packages/loot-core/src/server/accounts/sync.test.ts` | 多场景覆盖 |
| 合并测试用例 | `packages/loot-core/src/server/transactions/merge.test.ts` | 保留优先级验证 |

---

## 六、总结

Actual Budget 的交易合并去重策略采用 **"精确为主、模糊为辅、用户可控"** 的设计理念：

1. **匹配策略**：`imported_id` 精确匹配 → 日期+金额+payee 模糊匹配 → 日期+金额兜底匹配
2. **冲突处理**：已有值优先，保护用户手动录入的分类和收款人信息
3. **状态保护**：已对账交易不被自动更新
4. **用户控制**：预览阶段可选择"合并/强制新增/跳过"三种处理方式
5. **可配置性**：通过账户级偏好调整同步行为（是否导入待清算、是否更新日期等）

这种设计平衡了自动化和用户控制权，既能减少重复记账的工作量，又避免了自动匹配可能造成的数据误改。
