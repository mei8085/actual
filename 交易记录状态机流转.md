# 交易记录状态机流转文档

## 一、状态字段定义

### 1.1 数据库 Schema

交易记录表 `transactions` 中有两个独立的布尔字段控制状态：

| 字段名       | 类型              | 默认值    | 迁移脚本                                                                                                                                              | 说明                       |
| ------------ | ----------------- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- |
| `cleared`    | INTEGER (boolean) | 1 (true)  | [1582384163573_cleared.sql](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/loot-core/migrations/1582384163573_cleared.sql)               | 是否已结算（银行已清算）   |
| `reconciled` | INTEGER (boolean) | 0 (false) | [1697046240000_add_reconciled.sql](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/loot-core/migrations/1697046240000_add_reconciled.sql) | 是否已对账（人工核对锁定） |

### 1.2 TypeScript 类型定义

在 [transaction.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/loot-core/src/types/models/transaction.ts#L24-L25) 中：

```typescript
export type TransactionEntity = {
  // ... 其他字段
  cleared?: boolean; // 已结算
  reconciled?: boolean; // 已对账
  // ... 其他字段
};
```

---

## 二、状态优先级与视觉呈现

### 2.1 状态显示优先级

UI 渲染时按以下优先级判断显示状态（优先级从高到低）：

```
reconciled = true  →  "reconciled" 状态（锁图标）
    ↓
cleared = true     →  "cleared" 状态（实心对勾图标）
    ↓
两者都为 false     →  "uncleared" 状态（空心圆圈图标，UI 表现为 null）
```

核心代码位于 [TransactionsTable.tsx#L1878-L1886](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L1878-L1886)：

```tsx
status={
  isPreview
    ? (previewStatus as StatusTypes)
    : reconciled
      ? 'reconciled'
      : cleared
        ? 'cleared'
        : null
}
```

### 2.2 状态视觉属性表

在 [StatusBadge.tsx#L33-L92](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/schedules/StatusBadge.tsx#L33-L92) 中定义：

| 状态值                  | 图标                              | 颜色                            | 含义                           |
| ----------------------- | --------------------------------- | ------------------------------- | ------------------------------ |
| `null` (未结算)         | `SvgCheckCircleHollow` (空心圆圈) | 灰色（禁用色）                  | 待银行清算的交易               |
| `'cleared'` (已结算)    | `SvgCheckCircle1` (实心对勾)      | `theme.noticeTextLight`（蓝色） | 银行已清算的交易               |
| `'reconciled'` (已对账) | `SvgLockClosed` (锁图标)          | `theme.noticeTextLight`（蓝色） | 已人工核对并锁定，不可随意修改 |

---

## 三、完整状态流转图

```
                    ┌──────────────────┐
                    │   Uncleared      │
                    │  未结算           │
                    │ cleared=false    │
                    │ reconciled=false │
                    └────────┬─────────┘
                             │
                  点击状态列 / 批量操作
                  快捷键 L / 银行导入
                             ▼
                    ┌──────────────────┐
        ┌──────────▶│     Cleared      │◀──────────┐
        │           │    已结算         │           │
        │           │ cleared=true     │           │
        │           │ reconciled=false │           │
        │           └────────┬─────────┘           │
        │                    │                     │
        │          对账模式完成 "Lock"             │
        │          批量查询: cleared=true,         │
        │          reconciled=false 的全部交易     │
        │                    │                     │
  点击状态列解锁:                              点击状态列 / 批量操作
  弹窗确认 → reconciled=false
        │                    ▼                     │
        │           ┌──────────────────┐          │
        └───────────│   Reconciled     │──────────┘
                    │   已对账          │
                    │ cleared=true     │
                    │ reconciled=true  │
                    └──────────────────┘

  ── 对账模式差额不为 0 时 ─────────────────────────────────────
  │ 点击 "Create adjustment"                              │
  │    ↓                                                   │
  │ 新建调节交易: cleared=true, reconciled=false           │
  │ notes = "Reconciliation balance adjustment"            │
  │ amount = 银行余额 - 已结算余额                          │
  │    ↓                                                   │
  │ → 作为 Cleared 状态参与对账（下次 Lock 时被一并锁定）   │
  ─────────────────────────────────────────────────────────

  特殊约束: Reconciled 状态下点击 cleared 不会只切换 cleared，
  而是触发解锁流程 → 先将 reconciled 设为 false

  拦截体系（所有影响对账余额的操作）:
  ┌─────────────────────────────────────────────────────┐
  │ 单行/批量: 编辑 amount/payee/account/date             │
  │ 单行/批量: 删除                                       │
  │ 批量: 组合转账关系                                    │
  │    ↓ 两级检测                                         │
  │  Step 1: 选中交易自身 reconciled=true?                │
  │    ├─ 是 → 弹 confirm-transaction-edit                │
  │    └─ 否 → Step 2: 关联转账对方 reconciled=true?      │
  │           ├─ 是 → 弹 *Transfer 后缀确认               │
  │           └─ 否 → 直接放行                             │
  └─────────────────────────────────────────────────────┘
```

---

## 四、状态切换触发入口详解

### 4.1 路径 A：未结算 ↔ 已结算（切换 `cleared`）

#### 入口 1：单行状态列点击

- **位置**：[TransactionsTable.tsx#L384-L388](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L384-L388)
- **触发方式**：交易行左侧「状态列」图标点击
- **逻辑**：
  ```typescript
  function onSelect() {
    if (isClearedField) {
      onUpdate('cleared', !(status === 'cleared'));
    }
  }
  ```
- **约束**：若当前是 `reconciled` 状态，不会进入此分支，而是走「解锁」流程（见 4.3）

#### 入口 2：批量操作菜单

- **位置**：[SelectedTransactionsButton.tsx#L409](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/transactions/SelectedTransactionsButton.tsx#L409)
- **触发方式**：
  - 选中多条交易 → 顶部批量菜单 → Edit field → Cleared
  - 快捷键：`L`
- **核心处理逻辑**：[useTransactionBatchActions.ts#L106-L118](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/hooks/useTransactionBatchActions.ts#L106-L118)
  ```typescript
  // Cleared 是特殊处理：只要有任意一条未结算，就全部设为已结算；否则全部未结算
  if (name === 'cleared') {
    value = !!transactionsToChange.find(t => !t.cleared);
  }
  // ...
  // 重要：跳过已对账（reconciled）的交易，不改变它们的状态
  if (name === 'cleared' && trans.reconciled) {
    return; // 不参与批量切换
  }
  ```

#### 入口 3：银行同步/文件导入

- **位置**：
  - 新交易默认值：[sync.ts#L654-L661](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/loot-core/src/server/accounts/sync.ts#L654-L661)
  - 匹配到已存在交易时：[sync.ts#L585-L594](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/loot-core/src/server/accounts/sync.ts#L585-L594)
- **触发方式**：银行同步 / 导入银行文件（OFX/QIF/CSV）
- **逻辑**：

  ```typescript
  // 新导入交易：默认 cleared = defaultCleared 参数（通常为 true）
  const finalTransaction = {
    ...newTrans,
    cleared: trans.cleared ?? defaultCleared,
  };

  // 匹配到已有交易：取 OR 关系，保持已结算状态
  cleared: existing.cleared || trans.cleared || false,
  ```

---

### 4.2 路径 B：已结算 → 已对账（`reconciled=true`，锁定）

#### 入口：对账模式完成「Lock transactions」

- **位置**：[Account.tsx#L954-L986](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/accounts/Account.tsx#L954-L986)
- **前置步骤**：
  1. 账户菜单 → Reconcile → 输入银行对账单余额（[Reconcile.tsx#L130-L232](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/accounts/Reconcile.tsx#L130-L232)）
  2. 进入对账模式（`reconcileAmount != null`），此时 `showCleared` 强制为 true
  3. 手动勾选/取消勾选交易的 cleared 状态，使「已结算余额」与「银行余额」差值为 0
  4. 点击 "Lock transactions" 按钮
- **核心逻辑**：

  ```typescript
  lockTransactions = async () => {
    // 1. 查询当前账户所有 已结算但未对账 的交易
    const { data } = await aqlQuery(
      q('transactions')
        .filter({ cleared: true, reconciled: false, account: accountId })
        .select('*')
        .options({ splits: 'grouped' }),
    );

    // 2. 全部设置 reconciled = true
    transactions.forEach(trans => {
      const { diff } = updateTransaction(transactions, {
        ...trans,
        reconciled: true, // 锁定！
      });
      // ... 收集 diff
    });

    // 3. 批量更新到数据库
    await send('transactions-batch-update', changes);
  };
  ```

- **附加操作**：完成后更新账户 `last_reconciled` 时间戳（[Account.tsx#L1029-L1030](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/accounts/Account.tsx#L1029-L1030)）

---

### 4.3 路径 C：已对账 → 已结算（`reconciled=false`，解锁）

#### 入口：点击已对账交易的状态列

- **位置**：[TransactionsTable.tsx#L1071-L1086](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L1071-L1086)
- **触发方式**：在 `reconciled` 状态的交易行上点击状态列（锁图标）
- **流程**：
  1. 不直接切换 `cleared`，而是先弹出确认对话框（`confirmReason: 'unlockReconciled'`）
  2. 用户确认后，执行 `onUpdateAfterConfirm('reconciled', false)`
  3. 结果：`reconciled` 变为 false，`cleared` 仍保持 true → 回到「已结算」状态

```typescript
// Allow un-reconciling (unlocking) transactions
if (name === 'cleared' && transaction.reconciled) {
  dispatch(
    pushModal({
      modal: {
        name: 'confirm-transaction-edit',
        options: {
          onConfirm: () => {
            onUpdateAfterConfirm('reconciled', false); // 只解锁，不动 cleared
          },
          confirmReason: 'unlockReconciled',
        },
      },
    }),
  );
}
```

---

### 4.4 路径 D：对账差额 → 创建调节交易（附带状态设置）

#### 入口：对账模式差额不为零时「Create adjustment」

- **位置**：
  - 触发按钮：[Reconcile.tsx#L104-L116](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/accounts/Reconcile.tsx#L104-L116)
  - 创建逻辑：[Account.tsx#L1038-L1070](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/accounts/Account.tsx#L1038-L1070)
- **触发场景**：对账模式下，「已结算余额」与「输入的银行余额」差值不为 0 时，用户选择「Create adjustment」创建调节交易以消除差额
- **核心逻辑**：

  ```typescript
  onCreateReconciliationTransaction = async (diff: number) => {
    const reconciliationTransactions = realizeTempTransactions([
      {
        id: 'temp',
        account: this.props.accountId!,
        cleared: true, // ⚠️ 调节交易默认已结算
        reconciled: false, // ⚠️ 调节交易默认未对账
        amount: diff, // 差额为正记收入，负记支出
        date: currentDay(),
        notes: t('Reconciliation balance adjustment'),
      },
    ]);

    // 1. 先进行规则匹配（可能被分类到某个账户）
    const ruledTransactions = await Promise.all(
      reconciliationTransactions.map(transaction =>
        send('rules-run', { transaction }),
      ),
    );

    // 2. 落库（added）
    await send('transactions-batch-update', {
      added: ruledTransactions.filter(trans => !trans.tombstone),
      deleted: ruledTransactions.filter(trans => trans.tombstone),
    });
  };
  ```

- **状态语义**：
  - `cleared=true`：调节交易默认视为已被银行清算（因为它是用来平账的）
  - `reconciled=false`：调节交易需要和其他交易一起，在下一次对账 Lock 时一并锁定；或用户可单独点击状态列手动锁定
  - `notes='Reconciliation balance adjustment'`：标记为对账调节交易，便于后续审计

- **影响范围**：
  - 创建一条新交易，金额等于差额（diff），方向由差额正负决定
  - 若规则引擎命中（如自动分类到某个费用/收入账户），交易的 category / transfer_id 等字段会被修改
  - 创建后立即在列表中显示，并参与当前对账会话的已结算余额计算

---

## 五、已对账（reconciled）的二次确认拦截体系

所有对 `reconciled=true` 交易的破坏性操作（编辑关键字段、删除）、以及对其关联转账对方的破坏性操作，都会经过统一的二次确认拦截。

### 5.1 确认弹窗：ConfirmTransactionEditModal

弹窗本体：[ConfirmTransactionEditModal.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/modals/ConfirmTransactionEditModal.tsx)

弹窗根据 `confirmReason` 参数展示不同的提示文案：

| confirmReason                       | 触发场景                                              | 提示内容                                                             |
| ----------------------------------- | ----------------------------------------------------- | -------------------------------------------------------------------- |
| `editReconciled`                    | 单行编辑已对账交易的关键字段（金额/账户/收款人/日期） | 「保存对已对账交易的更改可能导致对账余额失衡」                       |
| `unlockReconciled`                  | 点击已对账交易的状态列（解锁）                        | 「解锁后，对金额、账户、收款人等的修改将不再被警告」                 |
| `batchEditWithReconciled`           | 批量编辑包含已对账交易                                | 「编辑已对账交易可能导致对账余额失衡」                               |
| `batchEditWithReconciledTransfer`   | 批量编辑的交易中存在**关联转账对方是已对账状态**      | 「该转账在另一个账户有关联的已对账交易，编辑可能导致该账户对账失衡」 |
| `batchDeleteWithReconciled`         | 批量删除包含已对账交易                                | 「删除已对账交易可能导致对账余额失衡」                               |
| `batchDeleteWithReconciledTransfer` | 批量删除的交易中存在**关联转账对方是已对账状态**      | 「该转账在另一个账户有关联的已对账交易，删除可能导致该账户对账失衡」 |
| `deleteReconciled`                  | 单行删除已对账交易                                    | 「删除已对账交易可能导致对账余额失衡」                               |

### 5.2 拦截检测函数：checkForReconciledTransactions

统一入口：[useTransactionBatchActions.ts#L451-L518](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/hooks/useTransactionBatchActions.ts#L451-L518)

该函数采用 **两级检测**：

```
Step 1: 检查"选中交易本身"是否有 reconciled=true
  ├─ 是 → 弹 confirm-transaction-edit（reason: batchEditWithReconciled / batchDeleteWithReconciled）
  └─ 否 → 进入 Step 2

Step 2: 检查"选中交易的关联转账对方"（transfer_id）是否有 reconciled=true
  ├─ 是 → 弹 confirm-transaction-edit（reason: *Transfer 后缀变体）
  └─ 否 → 直接放行，执行后续操作（无弹窗）
```

两级检测的核心代码：

```typescript
const checkForReconciledTransactions = async (
  ids,
  confirmReason,
  onConfirm,
) => {
  // Step 1: 检查选中的交易自身是否已对账
  const { data } = await aqlQuery(
    q('transactions')
      .filter({ id: { $oneof: ids }, reconciled: true })
      .select('*')
      .options({ splits: 'grouped' }),
  );
  const transactions = ungroupTransactions(data as TransactionEntity[]);

  if (transactions.length > 0) {
    // 自身已对账 → 弹窗
    dispatch(
      pushModal({
        modal: {
          name: 'confirm-transaction-edit',
          options: { confirmReason, onConfirm },
        },
      }),
    );
    return;
  }

  // Step 2: 检查关联转账对方是否已对账
  const { data: selectedData } = await aqlQuery(
    q('transactions')
      .filter({ id: { $oneof: ids } })
      .select(['transfer_id']),
  );

  const transferIds = (selectedData as TransactionEntity[])
    .map(t => t.transfer_id)
    .filter((id): id is string => id != null);

  if (transferIds.length > 0) {
    const { data: reconciledTransfers } = await aqlQuery(
      q('transactions')
        .filter({ id: { $oneof: transferIds }, reconciled: true })
        .select('*'),
    );

    if ((reconciledTransfers as TransactionEntity[]).length > 0) {
      // 对方已对账 → 弹窗（使用 *Transfer 后缀的 reason）
      dispatch(
        pushModal({
          modal: {
            name: 'confirm-transaction-edit',
            options: {
              confirmReason: transferReasonMap[confirmReason], // e.g. batchDeleteWithReconciledTransfer
              onConfirm,
            },
          },
        }),
      );
      return;
    }
  }

  // 两级都未命中 → 直接执行
  onConfirm(ids);
};
```

### 5.3 拦截的调用入口

#### 5.3.1 批量删除 onBatchDelete

位置：[useTransactionBatchActions.ts#L320-L398](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/hooks/useTransactionBatchActions.ts#L320-L398)

流程：

```
选中多条交易 → 批量菜单 Delete / 键盘 Delete
  ↓
checkForReconciledTransactions(ids, 'batchDeleteWithReconciled', onConfirmDelete)
  ├─ 命中已对账（自身或转账对方） → 弹窗二次确认
  │                              ↓ 用户确认
  │                                onConfirmDelete(ids) → confirm-delete 弹窗 → 执行删除
  └─ 未命中 → onConfirmDelete(ids) → confirm-delete 弹窗 → 执行删除
```

**注意**：删除本身还有一个 confirm-delete 弹窗（普通删除确认），已对账拦截是在这个弹窗**之前**额外增加的一道防线。

#### 5.3.2 批量编辑 onBatchEdit（关键字段）

位置：[useTransactionBatchActions.ts#L82-L284](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/hooks/useTransactionBatchActions.ts#L82-L284)

仅对以下 **5 个关键字段** 触发拦截：`amount`、`payee`、`account`、`date`（`cleared`、`category`、`notes` 不拦截，但有各自的特殊规则）。

```typescript
if (
  name === 'amount' ||
  name === 'payee' ||
  name === 'account' ||
  name === 'date'
) {
  await checkForReconciledTransactions(
    ids,
    'batchEditWithReconciled',
    openFieldEditor,
  );
} else {
  openFieldEditor(); // 直接打开字段编辑器，无对账拦截
}
```

**特殊规则补充**：

- `name === 'cleared'`：虽然不经过 checkForReconciledTransactions，但在 onChange 内部自行跳过已对账交易（见 4.1 入口 2）
- `name === 'account'`：即使通过了确认弹窗，最终写入时还会强制 `reconciled=false`（见 6.2）

#### 5.3.3 单行编辑（关键字段）

位置：[TransactionsTable.tsx#L1006-L1061](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L1006-L1061)

逻辑与批量一致，但不调用 checkForReconciledTransactions，而是内联实现了同样的两级检测：

1. 第一步：`if (transaction.reconciled === true && isReconciledField)` → 弹窗 `confirmReason: 'editReconciled'`
2. 第二步：若自身未对账，但 `transfer_id` 对方 `reconciled=true` → 弹窗 `confirmReason: 'batchEditWithReconciledTransfer'`

#### 5.3.4 单行删除 deleteReconciled

位置：[TransactionsTable.tsx 内联逻辑](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx)

类似 5.3.3，内联使用 `confirmReason: 'deleteReconciled'`。

#### 5.3.5 批量设置转账关系 onSetTransfer

位置：[useTransactionBatchActions.ts#L520-L568](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/hooks/useTransactionBatchActions.ts#L520-L568)

将两条交易组合成一对转账（互相设置 transfer_id 和支付方为对方账户）时，也会经过 `checkForReconciledTransactions`，使用 `batchEditWithReconciled` 作为 reason。

#### 5.3.6 移动端编辑表单 TransactionEdit

位置：[TransactionEdit.tsx#L795-L833](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/mobile/transactions/TransactionEdit.tsx#L795-L833)

移动端更严格：只要存在任意 `reconciled=true` 的交易就弹出 `editReconciled` 确认，**不区分编辑了哪个字段**（Web 端只拦截关键字段）。之后同样检测关联转账对方。

---

## 六、影响范围与特殊约束（续）

### 6.1 编辑已对账交易的保护机制

当交易 `reconciled=true` 时，修改以下字段会弹出确认对话框：

- `credit` / `debit`（即金额）
- `payee`（收款人/付款人）
- `account`（所属账户）
- `date`（日期）

代码位置：[TransactionsTable.tsx#L1006-L1033](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L1006-L1033)

此外，若交易的关联转账（`transfer_id`）对方是已对账状态，也会触发确认。详见第五章拦截体系。

### 6.2 更改账户时自动取消对账

代码位置：

- 单行编辑：[TransactionsTable.tsx#L1119-L1121](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L1119-L1121)
- 批量编辑：[useTransactionBatchActions.ts#L155-L157](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/hooks/useTransactionBatchActions.ts#L155-L157)

```typescript
if (name === 'account' && transaction.account !== value) {
  newTransaction.reconciled = false; // 强制取消对账
}
```

### 6.3 复制交易时重置状态

代码位置：[useTransactionBatchActions.ts#L298-L306](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/hooks/useTransactionBatchActions.ts#L298-L306)

```typescript
realizeTempTransactions(ungroupTransaction(trans)).map(t => ({
  ...t,
  cleared: false, // 重置为未结算
  reconciled: false, // 重置为未对账
}));
```

### 6.4 银行对账匹配时跳过已对账交易

代码位置：[sync.ts#L571-L575](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/loot-core/src/server/accounts/sync.ts#L571-L575)

```typescript
// Skip updating already reconciled (locked) transactions
if (match.reconciled) {
  updatedPreview.push({ transaction: trans, ignored: true });
  continue; // 跳过，不更新该笔交易
}
```

### 6.5 隐藏已对账交易的筛选

代码位置：

- 查询过滤：[Account.tsx#L472-L476](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/accounts/Account.tsx#L472-L476)
- 前端过滤（双重保险）：[TransactionsTable.tsx#L2407-L2411](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L2407-L2411)

当账户设置 `showReconciled=false` 时，不显示 `reconciled=true` 的交易。

### 6.6 分割/合并交易的约束

- **不允许分割已对账交易**：在 `canMakeAsSplitTransaction` 中检查 `areNoReconciledTransactions`
- **不允许反分割已对账交易**：在 `canUnsplitTransactions` 中检查 `areNoReconciledTransactions`

---

## 七、数据流转完整链路

### 7.1 调用链总览

```
UI 操作
  ↓
onUpdate / onBatchEdit  (前端)
  ↓
onUpdateAfterConfirm  (确认弹窗处理 + 特殊规则应用)
  ↓
onSave → deserializeTransaction  (Serialized→Entity 转换)
  ↓
updateTransaction (共享函数, 处理父/子交易联动)
  ↓
send('transactions-batch-update', changes)  (IPC/网络调用)
  ↓
handleBatchUpdateTransactions (loot-core server)
  ↓
batchUpdateTransactions  (核心批量处理)
  ↓
db.updateTransaction / db.insertTransaction / db.deleteTransaction
  ↓
同步消息（CRDT）→ 多设备同步
```

### 7.2 父/子（拆分）交易联动

在 [transactions.ts#L47-L48](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/loot-core/src/shared/transactions.ts#L47-L48) 中，新建子交易时继承父交易状态：

```typescript
cleared: parent.cleared != null ? parent.cleared : null,
reconciled: parent.reconciled != null ? parent.reconciled : null,
```

银行对账匹配时，若父交易的 `cleared` 或 `date` 变化，会同步到所有子交易（[sync.ts#L626-L644](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/loot-core/src/server/accounts/sync.ts#L626-L644)）。

---

## 八、文件索引

| 文件                                                                                                                                                                    | 作用                                                                                        |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| [transaction.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/loot-core/src/types/models/transaction.ts)                                                 | TransactionEntity 类型定义                                                                  |
| [transactions.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/loot-core/src/shared/transactions.ts)                                                     | 交易共享工具（updateTransaction, makeChild, split 等）                                      |
| [index.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/loot-core/src/server/transactions/index.ts)                                                      | 后端 batchUpdateTransactions 核心实现                                                       |
| [sync.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/loot-core/src/server/accounts/sync.ts)                                                            | 银行导入时的 reconcileTransactions 匹配逻辑                                                 |
| [TransactionsTable.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx)                   | 交易表格渲染 + 单行 onUpdate 状态切换 + 编辑/删除拦截                                       |
| [StatusBadge.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/schedules/StatusBadge.tsx)                                  | 状态图标和颜色定义                                                                          |
| [Account.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/accounts/Account.tsx)                                           | 账户页：lockTransactions、onReconcile、onDoneReconciling、onCreateReconciliationTransaction |
| [Reconcile.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/accounts/Reconcile.tsx)                                       | ReconcileMenu 输入对账余额 + ReconcilingMessage 差额/调节按钮                               |
| [SelectedTransactionsButton.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/transactions/SelectedTransactionsButton.tsx) | 批量操作菜单入口（Edit field → Cleared，Delete 等）                                         |
| [useTransactionBatchActions.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/hooks/useTransactionBatchActions.ts)                     | 批量编辑/删除/复制 实现、cleared 特殊处理、checkForReconciledTransactions 两级拦截          |
| [ConfirmTransactionEditModal.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/modals/ConfirmTransactionEditModal.tsx)     | 已对账二次确认弹窗（7 种 confirmReason 文案）                                               |
| [TransactionEdit.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/mobile/transactions/TransactionEdit.tsx)                | 移动端编辑表单：已对账拦截（不区分字段）                                                    |
| [Modals.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/2-actual/packages/desktop-client/src/components/Modals.tsx)                                                      | 模态框注册表：confirm-transaction-edit 等                                                   |
