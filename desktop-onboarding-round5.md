# 桌面客户端首次启动引导流程深度分析（第五轮）

> 聚焦首次引导末端的精确细节修正

---

## 1. 预算目录 ID 规范化规则：连字符匹配真相

### 1.1 正则表达式精确解析

**源码**: `budget-name.ts:42`

```typescript
let id = name.replace(/( |[^A-Za-z0-9])/g, '-') + '-' + uuidv4().slice(0, 7);
```

**正则拆解**: `/( |[^A-Za-z0-9])/g`

| 部分 | 含义 |
|------|------|
| `/g` | 全局匹配（替换所有匹配项） |
| `|` | 逻辑或 |
| 第一个分支 ` ` | 匹配空格字符（ASCII 32） |
| 第二个分支 `[^A-Za-z0-9]` | 匹配**任何不是** A-Z、a-z、0-9 的字符 |

### 1.2 连字符匹配问题

**关键问题**: 连字符 `-` 是否会被匹配？

**答案**: ✅ **会被匹配**

**原因**:
- 连字符 `-` 的 ASCII 码是 45
- `0-9` 范围是 ASCII 48-57
- `A-Z` 范围是 ASCII 65-90
- `a-z` 范围是 ASCII 97-122
- 45 不在上述任何范围内，因此被 `[^A-Za-z0-9]` 匹配

**替换结果**: 连字符 `-` 会被替换为 `-`（字符没变，但确实经历了匹配-替换过程）

### 1.3 字符匹配分类表（修正版）

| 字符 | ASCII | 是否匹配 `[^A-Za-z0-9]` | 替换结果 |
|------|-------|------------------------|----------|
| `A-Z` | 65-90 | ❌ 否 | 保留原字符 |
| `a-z` | 97-122 | ❌ 否 | 保留原字符 |
| `0-9` | 48-57 | ❌ 否 | 保留原字符 |
| `-`（连字符） | 45 | ✅ 是 | 替换为 `-`（不变） |
| `_`（下划线） | 95 | ✅ 是 | 替换为 `-` |
| 空格 | 32 | ✅ 是（匹配第一个分支） | 替换为 `-` |
| 中文/日文/韩文等 | >127 | ✅ 是 | 替换为 `-` |
| 标点 `!@#$%^&*()` | 各种 | ✅ 是 | 替换为 `-` |
| emoji | >127 | ✅ 是 | 替换为 `-` |

### 1.4 与 ID 校验规则的对比

**ID 校验** (`platform/server/fs/shared.ts:26-30`):
```typescript
if (id.match(/[^A-Za-z0-9\-_]/)) {
  throw new Error(`Invalid budget id "${id}"...`);
}
```

**生成规则 vs 校验规则**:
- 生成规则: 只允许 `A-Za-z0-9-`（因为其他都被替换为 `-`）
- 校验规则: 允许 `A-Za-z0-9-_`（多了下划线 `_`）

> 💡 **细微差异**: 生成的 ID 永远不会包含下划线（因为下划线会被替换为 `-`），但校验规则允许下划线。这是为了兼容老版本或手动创建的目录。

---

## 2. 家庭预算 2024 示例：逐字符精确重算

### 2.1 输入字符串分析

**输入**: `"家庭预算 2024"`

**逐字符分解**（统一计数口径）:

| 索引 | 字符 | Unicode | 字符类型 | 是否匹配正则 | 替换为 |
|------|------|---------|----------|-------------|--------|
| 0 | 家 | U+5BB6 | 中文 | ✅ 是 | `-` |
| 1 | 庭 | U+5EAD | 中文 | ✅ 是 | `-` |
| 2 | 预 | U+9884 | 中文 | ✅ 是 | `-` |
| 3 | 算 | U+7B97 | 中文 | ✅ 是 | `-` |
| 4 | （空格） | U+0020 | 空格 | ✅ 是 | `-` |
| 5 | 2 | U+0032 | 数字 | ❌ 否 | `2` |
| 6 | 0 | U+0030 | 数字 | ❌ 否 | `0` |
| 7 | 2 | U+0032 | 数字 | ❌ 否 | `2` |
| 8 | 4 | U+0034 | 数字 | ❌ 否 | `4` |

**统计**:
- 总字符数: 9 个
- 匹配数: 5 个（4个中文 + 1个空格）
- 保留数: 4 个（数字 2、0、2、4）

### 2.2 替换过程

```
原始:    家  庭  预  算    2  0  2  4
索引:    0   1   2   3   4  5  6  7  8
匹配:    ✅  ✅  ✅  ✅  ✅  ❌  ❌  ❌  ❌
替换:    -   -   -   -   -  2   0   2   4
         └───────────────────┘
             5个连字符
```

**规范化结果**: `"-----2024"`（5个连字符 + 4个数字 = 9个字符）

### 2.3 最终 ID 生成

```
规范化结果: "-----2024"
加上 UUID 后缀: "-----2024" + "-" + "a1b2c3d"
最终 ID: "-----2024-a1b2c3d"
```

**最终长度**: 9 + 1 + 7 = 17 个字符

### 2.4 修正声明

> ⚠️ **之前版本的错误**: 之前说"家庭预算"是6个汉字，实际是4个汉字 + 1个空格，所以是5个连字符而非7个。之前的计数口径错误。

---

## 3. 目录重命名 vs 移出根目录：可发现性与可加载性边界

### 3.1 概念再定义（精确版）

| 概念 | 精确定义 | 判定函数 |
|------|---------|---------|
| **可发现性** | 调用 `getBudgets()` 时，该预算是否出现在返回列表中 | `budgetfiles/app.ts:105-140` |
| **可加载性** | 调用 `loadBudget(id)` 时，能否成功打开并加载预算 | `budgetfiles/app.ts:508-640` |

### 3.2 可发现性的精确边界

**函数逻辑**:

```typescript
async function getBudgets() {
  // 边界1: 只扫描 DocumentDir 的直接子级
  const paths = await fs.listDir(fs.getDocumentDir());
  
  return Promise.all(paths.map(async name => {
    // 边界2: 目录内必须有 metadata.json
    const prefsPath = fs.join(fs.getDocumentDir(), name, 'metadata.json');
    if (await fs.exists(prefsPath)) {
      // 边界3: 目录名不能是 DEMO_BUDGET_ID ('_demo-budget')
      if (name !== DEMO_BUDGET_ID) {
        return { id: name, ... };
      }
    }
    return null;
  }));
}
```

**可发现性的三个必要条件（AND 关系）**:
1. ✅ 目录必须是 `getDocumentDir()` 的**直接子目录**（不能嵌套）
2. ✅ 目录内必须存在 `metadata.json` 文件
3. ✅ 目录名不能是 `_demo-budget`（特殊排除）

### 3.3 可加载性的精确边界

**函数逻辑**:

```typescript
async function _loadBudget(id: Budget['id']) {
  // 边界1: ID 格式校验
  let dir = fs.getBudgetDir(id);  // 不允许路径遍历字符
  
  // 边界2: 目录必须存在
  if (!(await fs.exists(dir))) {
    return { error: 'budget-not-found' };
  }
  
  // 边界3: metadata.json 必须存在且可解析
  await prefs.loadPrefs(id);
  
  // 边界4: db.sqlite 必须存在且可打开
  await db.openDatabase(id);
  
  // ... 后续检查
}
```

**可加载性的四个必要条件（AND 关系）**:
1. ✅ ID 只包含 `A-Za-z0-9-_`（路径安全校验）
2. ✅ 目录 `join(DocumentDir, id)` 必须存在
3. ✅ `metadata.json` 必须存在且是有效 JSON
4. ✅ `db.sqlite` 必须存在且是有效的 SQLite 数据库

### 3.4 操作场景对比（精确版）

#### 场景1: 在扫描根目录内重命名

**操作**: `Actual/My-Finances-a1b2c3d` → `Actual/My-Old-Budget`

| 检查项 | 结果 | 说明 |
|--------|------|------|
| **可发现性** | ✅ 是 | 仍是直接子目录，有 metadata.json |
| **可加载性** | ✅ 是 | 新目录名通过格式校验，目录存在 |
| **ID 变化** | ✅ 变化 | 新 ID = `My-Old-Budget` |
| **metadata.id** | 被覆盖 | `loadPrefs(id)` 强制 `prefs.id = id` |
| **lastBudget 记录** | ⚠️ 失效 | 存的是旧 ID，需重新打开 |
| **同步关联** | ✅ 保留 | cloudFileId/groupId 仍在 metadata.json 中 |
| **对用户的可见影响** | 低 | 列表中显示新名称，需手动打开一次 |

#### 场景2: 移出扫描根目录

**操作**: `Actual/My-Finances-a1b2c3d` → `Desktop/My-Finances-a1b2c3d`

| 检查项 | 结果 | 说明 |
|--------|------|------|
| **可发现性** | ❌ 否 | 不在 DocumentDir 下，listDir 扫描不到 |
| **可加载性** | ❌ 否 | `fs.exists(dir)` 返回 false |
| **ID** | - | 无关（路径不存在） |
| **数据完整性** | ✅ 完整 | 文件只是移动，没有损坏 |
| **同步关联** | ✅ 保留 | metadata.json 仍在目录内 |
| **移回后可恢复** | ✅ 是 | 移回 DocumentDir 后完全恢复 |
| **对用户的可见影响** | 高 | 预算从列表中"消失"了 |

#### 场景3: 根目录内嵌套

**操作**: 创建 `Actual/Archives/MyBudget`

| 检查项 | 结果 | 说明 |
|--------|------|------|
| **可发现性** | ❌ 否 | `listDir` 只返回直接子级 `Archives`，不递归 |
| **可加载性** | ❌ 否 | 尝试 `loadBudget('Archives/MyBudget')` 会触发路径校验失败 |
| **数据完整性** | ✅ 完整 | 文件都在 |
| **恢复方式** | 移到直接子级 | 如 `Actual/MyBudget` |

> 💡 **安全设计**: 不递归扫描是故意的，防止路径遍历攻击。预算必须是 DocumentDir 的直接子目录。

#### 场景4: 删除 metadata.json

**操作**: 删除 `Actual/MyBudget/metadata.json`

| 检查项 | 结果 | 说明 |
|--------|------|------|
| **可发现性** | ❌ 否 | `fs.exists(prefsPath)` 返回 false |
| **可加载性** | ❌ 否 | `loadPrefs(id)` 会抛异常 |
| **数据完整性** | ⚠️ 部分 | db.sqlite 还在，数据完整 |
| **恢复难度** | 中 | 需要重建 metadata.json（需要知道原来的 budgetName、cloudFileId 等） |

#### 场景5: 删除 db.sqlite

**操作**: 删除 `Actual/MyBudget/db.sqlite`

| 检查项 | 结果 | 说明 |
|--------|------|------|
| **可发现性** | ✅ 是 | metadata.json 还在 |
| **可加载性** | ❌ 否 | `openDatabase(id)` 会抛异常 |
| **数据完整性** | ❌ 丢失 | 主要数据文件没了 |
| **恢复方式** | ❌ 无法恢复 | 除非有备份 |

### 3.5 排障指引（修正版）

**之前的误导表述**: "移出根目录后数据丢失"

**修正后**: "移出根目录后数据**不可见但不丢失**，移回即可恢复。只有删除 db.sqlite 才会真正丢失数据。"

**常见问题排查表**:

| 现象 | 可能原因 | 排查步骤 | 修复方法 |
|------|---------|---------|---------|
| 预算列表中看不到 | 1. 目录被移出 Actual 文件夹<br>2. metadata.json 被删除<br>3. 目录嵌套在子文件夹中 | 1. 检查文件系统中目录位置<br>2. 检查目录内是否有 metadata.json<br>3. 检查是否是直接子目录 | 1. 移回 Actual 文件夹<br>2. 从备份恢复 metadata.json<br>3. 移到直接子级 |
| 列表中能看到但打不开 | 1. db.sqlite 被删除<br>2. 数据库文件损坏<br>3. 权限问题 | 1. 检查目录内是否有 db.sqlite<br>2. 尝试用 SQLite 工具打开<br>3. 检查文件权限 | 1. 从备份恢复<br>2. 尝试修复数据库<br>3. 修正权限 |
| 打开后 ID 变了 | 目录被重命名 | 检查目录名是否与预期一致 | 这是正常行为，目录名即 ID |
| 移回后 ID 变化 | 移出期间目录被重命名 | - | 正常，同步关联仍在 metadata.json 中 |

---

## 4. 本论修正总结

| 之前的表述 | 修正后的准确表述 | 修正原因 |
|-----------|-----------------|---------|
| "家庭预算 2024 → -------2024" | "家庭预算 2024 → -----2024" | 之前多算了2个连字符，实际是4个汉字+1个空格=5个连字符 |
| "连字符不被正则匹配" | "连字符会被匹配，但替换为相同字符" | 连字符 ASCII 45 不在 A-Za-z0-9 范围内 |
| "移出根目录数据丢失" | "移出根目录数据不可见但不丢失，移回即可恢复" | 容易误导用户以为数据被删除，实际只是不在扫描范围内 |
| "getBudgets 扫描所有子目录" | "getBudgets 只扫描直接子级，不递归" | 嵌套目录不会被发现，这是安全设计 |

---

## 5. 关键代码位置速查

| 功能 | 文件 | 行号 |
|------|------|------|
| ID 生成正则 | `budget-name.ts` | L42 |
| ID 安全性校验 | `platform/server/fs/shared.ts` | L26-30 |
| getBudgets 扫描逻辑 | `budgetfiles/app.ts` | L105-140 |
| _loadBudget 加载检查 | `budgetfiles/app.ts` | L508-541 |
| loadPrefs 强制覆盖 ID | `prefs.ts` | L41-44 |
| DEMO_BUDGET_ID 定义 | `budgetfiles/app.ts` | L47 |
