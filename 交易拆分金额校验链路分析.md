# 交易拆分金额校验链路分析

## 一、整体架构概览

Actual Budget 对「交易拆分（Split Transaction）」的金额一致性校验采用 **纯前端校验 + 错误状态标记 + UI 交互拦截** 的模式。后端数据库写入层不做一致性强制校验，完全信任前端传入的数据。

校验链路按数据流从底层到 UI 分为 5 层：

| 层级 | 核心职责 | 关键文件 |
|------|----------|----------|
| 数据模型层 | 定义 `error` 字段承载错误状态 | [transaction.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/types/models/transaction.ts) |
| 核心算法层 | `recalculateSplit` 统一计算并注入错误 | [transactions.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/shared/transactions.ts) |
| 规则引擎层 | 规则执行后同样走 `recalculateSplit` | [rule.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/server/rules/rule.ts) |
| 桌面端 UI 层 | 条件渲染错误条 / 隐藏 Add 按钮 | [TransactionsTable.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx) |
| 移动端 UI 层 | Footer 按钮替换 + 滚动提示 | [TransactionEdit.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/mobile/transactions/TransactionEdit.tsx) |

---

## 二、数据模型层：错误状态的承载

### 2.1 `TransactionEntity.error` 字段

定义在 [transaction.ts#L32-L36](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/types/models/transaction.ts#L32-L36)：

```typescript
error?: {
  type: 'SplitTransactionError';
  version: 1;
  difference: number;  // 父金额 - 子项之和（可为正=不足，负=超额）
} | null;
```

- **`difference > 0`**：子项加总**不足**（还有金额未分配）
- **`difference < 0`**：子项加总**超额**（分配金额超过父项）
- **`error === null`**：金额完全一致（校验通过）

这个字段直接挂在**父交易（`is_parent`）**对象上，随交易数据一起在前端状态、API 请求中传递。

---

## 三、核心算法层：`recalculateSplit` — 校验的唯一入口

### 3.1 辅助函数 `num`

定义在 [transactions.ts#L17-L19](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/shared/transactions.ts#L17-L19)：

```typescript
function num(n: number | null | undefined) {
  return typeof n === 'number' ? n : 0;
}
```

将可能为 `null` / `undefined` 的金额统一为 `0`，确保 `reduce` 求和时不报错。

### 3.2 错误构造器 `SplitTransactionError`

定义在 [transactions.ts#L21-L29](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/shared/transactions.ts#L21-L29)：

```typescript
function SplitTransactionError(total: number, parent: TransactionEntity) {
  const difference = num(parent.amount) - total;
  return {
    type: 'SplitTransactionError' as const,
    version: 1 as const,
    difference,
  };
}
```

`difference = 父金额 - 子项和`，所以符号语义与直觉一致：正数代表不足，负数代表超额。

### 3.3 核心计算函数 `recalculateSplit`

定义在 [transactions.ts#L88-L102](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/shared/transactions.ts#L88-L102)：

```typescript
export function recalculateSplit(trans: TransactionEntity) {
  const total = (trans.subtransactions || []).reduce(
    (acc, t) => acc + num(t.amount),
    0,
  );

  const { error: _error, ...rest } = trans;
  return {
    ...rest,
    error:
      total === num(trans.amount) ? null : SplitTransactionError(total, trans),
  } satisfies TransactionEntity;
}
```

**设计要点**：

1. 只做**纯计算**，不修改输入对象（immutable），返回新对象
2. 每次调用时先通过解构 `_error` 清除旧错误，再根据最新计算结果决定是否注入新错误
3. 比较用严格相等 `===`，不做浮点近似——Amount 均为 `IntegerAmount` 整数（以「分」为单位），所以 `===` 完全可靠
4. 比较对象是 `total`（子项之和）与 `num(trans.amount)`（父项金额），两方都经过 `num()` 保护

### 3.4 所有触发 `recalculateSplit` 的操作

`recalculateSplit` 被精确地插在每一个**可能破坏金额平衡**的操作之后：

| 操作 | 调用位置 | 说明 |
|------|----------|------|
| **更新任意交易字段** | [updateTransaction()#L289-L292](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/shared/transactions.ts#L289-L292) | 修改父/子金额、分类等任何字段后重算 |
| **删除子项** | [deleteTransaction()#L316-L319](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/shared/transactions.ts#L316-L319) | 子项减少，剩余子项之和可能不再等于父项 |
| **初次拆分** | [splitTransaction()#L348](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/shared/transactions.ts#L348) | 父金额非零时立刻标记 error |
| **规则引擎执行后** | [rule.ts#L91](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/server/rules/rule.ts#L91) | 规则可能修改子项金额 |
| **账户同步导入后** | [sync.ts#L46](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/server/accounts/sync.ts#L46) | 银行同步可能改变交易数据 |

### 3.5 初次拆分时的错误预置

在 [splitTransaction()#L340-L355](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/shared/transactions.ts#L340-L355)：

```typescript
export function splitTransaction(
  transactions: readonly TransactionEntity[],
  id: string,
  createSubtransactions?: (parent: TransactionEntity) => TransactionEntity[],
) {
  return replaceTransactions(transactions, id, trans => {
    if (trans.is_parent || trans.is_child) {
      return trans;
    }
    const subtransactions = createSubtransactions?.(trans) || [
      makeChild(trans),
    ];
    const { error: _error, ...rest } = trans;
    return {
      ...rest,
      is_parent: true,
      error: num(trans.amount) === 0 ? null : SplitTransactionError(0, trans),
      subtransactions: subtransactions.map(t => ({
        ...t,
        sort_order: t.sort_order || -1,
      })),
    } satisfies TransactionEntity;
  });
}
```

如果父项已有金额，拆分后子项总和初始为 0，**立刻挂 error**，UI 一进入拆分模式就显示差额。

### 3.6 删除子项时的重算

在 [deleteTransaction()#L299-L325](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/shared/transactions.ts#L299-L325)：

```typescript
export function deleteTransaction(
  transactions: TransactionEntity[],
  id: string,
) {
  return replaceTransactions(transactions, id, trans => {
    if (trans.is_parent) {
      if (trans.id === id) {
        return null;                              // 删父 → 整组删除
      } else if (trans.subtransactions?.length === 1) {
        const { subtransactions: _subtransactions, ...rest } = trans;
        return { ...rest, is_parent: false, error: null };  // 只剩 1 子 → 降为普通交易
      } else {
        const sub = trans.subtransactions?.filter(t => t.id !== id);
        return recalculateSplit({ ...trans, ...(sub && { subtransactions: sub }) });
      }
    } else {
      return null;
    }
  });
}
```

三种子情况：
- 删父 → 整组删除
- 只剩 1 子 → 降为普通交易，**清空 error**
- 删中间子项 → `recalculateSplit` 重算

### 3.7 更新交易字段时的重算

在 [updateTransaction()#L259-L297](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/shared/transactions.ts#L259-L297)：

```typescript
export function updateTransaction(
  transactions: readonly TransactionEntity[],
  transaction: TransactionEntity,
) {
  return replaceTransactions(transactions, transaction.id, trans => {
    if (trans.is_parent) {
      const parent =
        trans.id === transaction.id ? { ...trans, ...transaction } : trans;
      const originalSubtransactions =
        parent.subtransactions ?? trans.subtransactions;
      const sub = originalSubtransactions?.map(t => {
        let child = t;
        if (trans.id === transaction.id) {
          // 父交易被更新 → 同步 payee 到与父相同的子项
          const { payee: childPayee, ...rest } = t;
          const newPayee =
            childPayee === trans.payee ? transaction.payee : childPayee;
          child = { ...rest, ...(newPayee != null ? { payee: newPayee } : {}) };
        } else if (t.id === transaction.id) {
          child = transaction;
        }
        return makeChild(parent, child);
      });

      return recalculateSplit({
        ...parent,
        ...(sub && { subtransactions: sub }),
      });
    } else {
      return transaction;
    }
  });
}
```

关键：无论修改的是父交易还是子交易，最终都走 `recalculateSplit` 重算。同时用 `makeChild(parent, child)` 确保子项继承父项的 `parent_id`、`account`、`date` 等字段。

---

## 四、三端保存拦截与引导机制对比分析

这是最容易混淆的部分。Actual Budget 对三种编辑场景采用了**完全不同**的保存模型：

| 场景 | 保存触发方式 | 差额时的拦截机制 | error 字段是否被保存到后端 |
|------|-------------|-----------------|:--------------------------:|
| **桌面端：新建交易** | 点击 Add 按钮 / Ctrl+Enter / 最后一项按 Enter | ✅ 按钮被替换 + Enter 拦截 | ❌ 无法进入保存流程 |
| **桌面端：编辑已有交易** | 每个字段 blur（失焦）/ Tab / Enter 即时保存 | ⚠️ blur 不拦截、Enter 在最后子项才拦截 | ✅ **error 字段会随数据一起写入数据库** |
| **移动端：编辑/新建** | 底部 Footer 按钮整体提交 | ✅ 按钮互斥渲染（根本不渲染 Save） | ❌ 无法进入保存流程 |

下面逐个场景详细拆解。

### 4.1 桌面端：新建交易（NewTransaction）

#### 4.1.1 保存触发路径

```
用户在最后一个子项按 Enter / 点击 Add 按钮 / Ctrl+Enter
  ↓
onAddTemporary()  [TransactionsTable L2900-L2963]
  ↓
setNewTransactions(realizeTempTransactions(...)) → 本地 state
  ↓
props.onAdd(transactions) → 交给上层 TransactionList.onAdd()
  ↓
saveDiff() → send('transactions-batch-update') → 后端批量写入
```

#### 4.1.2 差额时的拦截机制

**（1）按钮被彻底替换**

在 [NewTransaction#L2240-L2259](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L2240-L2259)：

```tsx
{error ? (
  <TransactionError
    error={error}
    isDeposit={isDeposit}
    onAddSplit={() => onAddSplit(transactions[0].id)}
    onDistributeRemainder={() => onDistributeRemainder(transactions[0].id)}
  />
) : (
  <Button variant="primary" onPress={handleAddClick}>
    <Trans>Add</Trans>
  </Button>
)}
```

`error` 存在时，`Add` 按钮从 DOM 中彻底消失。

**（2）Enter 键拦截**

在 [onCheckNewEnter()#L3043-L3087](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L3043-L3087)：

```typescript
afterSave(() => {
  const lastTransaction = getLastTransaction(latestState);
  if (latestState.current.newTransactions[0].error
      && newNavigator.editingId === lastTransaction.id) {
    onAddSplit(lastTransaction.id);    // 有 error → 加子项，不提交
  } else if (newNavigator.editingId === lastTransaction.id
      && (!isSplit || !lastTransaction.error)) {
    onAddTemporary();                   // 无 error → 正常添加
  }
});
```

两个分支是**互斥**的：有 error 时走 `onAddSplit`，不会执行 `onAddTemporary`。

#### 4.1.3 错误显示位置

在新建输入区的最下方（最后一个子项下面），`TransactionError` 直接渲染。

---

### 4.2 桌面端：编辑已有交易（TransactionRow inline editing）

这是最容易产生误解的场景。

#### 4.2.1 保存触发路径：每次 blur 即保存

桌面端表格是 **inline editing** 模型——**没有统一的"保存"按钮**。每个单元格独立编辑，失去焦点（blur）或按 Tab/Enter 时就立刻保存当前字段。

关键代码在 [CustomCell#L452-L500](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/table.tsx#L452-L500)：

```typescript
function onBlur_(e: FocusEvent) {
  // Only save on blur if the app is focused.
  if (document.hasFocus()) {
    onUpdate?.(value);         // ← blur 立刻调用 onUpdate
    fireBlur(onBlur, e);
  }
}

function onKeyDown(e: KeyboardEvent) {
  if (shouldSaveFromKey(e)) {  // Tab / Enter
    onUpdate?.(value);         // ← 按键也立刻保存
  }
}
```

这里的 `onUpdate` 最终会调用 Transaction 组件内部的 [onUpdateAfterConfirm#L1089-L1145](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L1089-L1145)：

```typescript
const onUpdateAfterConfirm: TransactionUpdateFunction = (name, value) => {
  const newTransaction = { ...transaction, [name]: value };
  // ...
  const deserialized = deserializeTransaction(newTransaction, originalTransaction);
  setTransaction(serializeTransaction(deserialized, showZeroInDeposit));

  const deserializedName = ['credit', 'debit'].includes(name) ? 'amount' : name;
  onSave(deserialized, subtransactions, deserializedName);  // ← 即时保存！
};
```

**完整链路**：

```
用户在单元格中输入后，点击别处 / 按 Tab / 按 Enter
  ↓
CustomCell.onBlur_ / onKeyDown
  ↓
onUpdate(name, value)  [Transaction 组件]
  ↓
onUpdateAfterConfirm → deserializeTransaction → onSave(deserialized, subtransactions, fieldName)
  ↓
TransactionTable.onSave [L3143-L3176]
  ├─ groupTransaction([parent, ...children])
  └─ onSaveProp(grouped)  →  交给上层 TransactionList.onSave()
  ↓
TransactionList.onSave [L427-L481]
  ├─ updateTransaction(transactionsLatest, transaction)
  │   └─ 内部 replaceTransactions → recalculateSplit 重算 error [!]
  ├─ transactionsLatest = changes.data   (error 字段也在里面)
  └─ void saveDiffAndApply(...) → send('transactions-batch-update') → 后端写入
```

**重点**：`updateTransaction` 内部调用了 `recalculateSplit`，**error 字段会被作为交易对象的一部分，和其他字段一起保存到后端数据库**。这就是为什么模糊（blur）操作不会被拦截——因为这里保存的是"当前单元格的数据"，不是"提交整组交易"。差额状态是合法存在的中间状态。

#### 4.2.2 差额时唯一的拦截：Enter 在最后子项

只有一种情况键盘行为会被修改——在**最后一个子项**上按 Enter 且父项有 error 时：

在 [onCheckEnter()#L3089-L3111](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L3089-L3111)：

```typescript
afterSave(() => {
  if (isLastChild(transactions, idx)
      && parent && parent.error
      && focusedField !== 'select') {
    e.stopPropagation();  // 阻止默认的"跳到下一行"
    onAddSplit(id);       // 改为新增一子项
  }
});
```

注意 `afterSave` 的语义——这里保存**已经完成**了（`savePending.current=true` 等数据回流后才执行回调），拦截的只是**后续的焦点跳转行为**，不是保存本身。

`afterSave` 机制的实现：
- 在 [TransactionsTable#L2909-L2978](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L2909-L2978) 通过 `savePending` ref + `useEffect` 监听 `props.transactions` 变化来实现"保存完成→执行回调"
- 这不是原子操作，而是 React 层面的异步调度

#### 4.2.3 错误显示位置

在 [TransactionRow#L2448-L2456](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L2448-L2456)：

```typescript
const error = expanded
  ? (parent && parent.error) || trans.error
  : trans.error;

const hasSplitError =
  (trans.is_parent || trans.is_child) &&
  (!expanded || isLastChild(transactions, index)) &&
  error && error.type === 'SplitTransactionError';
```

- 折叠时显示在父行下方
- 展开时只在**最后一个子项**下方显示（避免重复）
- 兼容从子项自身取 `error`（旧数据格式）

---

### 4.3 移动端：编辑/新建（TransactionEdit）

#### 4.3.1 保存触发路径

移动端是**显式按钮提交**模型：

```
用户点击底部 Footer 按钮
  ↓
（有 error 时永远走不到这里）
  ↓
onSaveInner [L726-L863]
  ├─ 检查：是否为未来日期？→ 弹出"转计划"确认
  ├─ 检查：是否包含已对账交易？→ 弹出对账确认
  ├─ 检查：是否包含已对账转账？→ 弹出转账对账确认
  └─ onConfirmSave()
      ├─ isAdding ? realizeTempTransactions(unserializedTransactions)
      ├─ onSave(transactionsToSave)  → 发送到上层
      └─ navigate(-1)  →  返回上一页
```

#### 4.3.2 差额时的拦截机制：按钮互斥渲染

在 [Footer#L286-L377](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/mobile/transactions/TransactionEdit.tsx#L286-L377)：

```tsx
{transaction.error?.type === 'SplitTransactionError' ? (
  // 第 1 层：有拆分校验错误 → 显示差额引导按钮
  <Button variant="primary" onPress={onClickRemainingSplit}>
    {!emptySplitTransaction
      ? <Trans>Add new split - {{ amount: ... }} left</Trans>
      : <Trans>Amount left: {{ amount: ... }}</Trans>
    }
  </Button>
) : !transaction.account ? (
  // 第 2 层：账户未选 → 显示选账户按钮
  <Button><SvgPiggyBank /><Trans>Select account</Trans></Button>
) : isAdding ? (
  // 第 3 层：新增模式 → 显示 Add transaction
  <Button><SvgAdd /><Trans>Add transaction</Trans></Button>
) : (
  // 第 4 层：编辑模式 → 显示 Save changes（只有 error===null 才会到这里）
  <Button><SvgPencilWriteAlternate /><Trans>Save changes</Trans></Button>
)}
```

四层互斥，有 error 时**永远不会渲染 Save 按钮**。同时所有按钮带 `isDisabled={!!editingField}`，输入中时全部不可点。

#### 4.3.3 差额按钮的引导行为

在 [Footer#L261-L271](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/mobile/transactions/TransactionEdit.tsx#L261-L271)：

```typescript
const onClickRemainingSplit = () => {
  if (childTransactions.length === 0) {
    onSplit(transaction.id);                          // 还没拆 → 执行拆分
  } else {
    if (!emptySplitTransaction) {
      onAddSplit(transaction.id);                     // 没有空行 → 新增一子项
    } else {
      onEmptySplitFound?.(emptySplitTransaction.id);  // 有空行 → 滚动定位过去
    }
  }
};
```

三种状态对应三种引导：
1. 父交易已标记为拆分但还没有子项 → 立刻创建第一个空子项
2. 所有子项都填了金额 → 再增加一个空子项
3. 已有空子项（金额为 0）→ 页面滚动到该空子项，光标聚焦提示用户填写

滚动定位由 [scrollChildTransactionIntoView](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/mobile/transactions/TransactionEdit.tsx#L1110-L1115) 配合 `useRef` 实现。

#### 4.3.4 onSaveInner 本身不校验 split error

虽然 Footer 保证了有 error 时点不到 Save，但 `onSaveInner` 函数内部（[L726-L863](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/mobile/transactions/TransactionEdit.tsx#L726-L863)）**并没有对 `SplitTransactionError` 做二次检查**。它只检查三件事：
- 未来日期是否转计划
- 是否包含已对账交易
- 是否包含已对账转账

如果开发者绕过 Footer，直接调用 `onSaveInner`，差额交易会被原样保存（error 字段也会被写入数据库，和桌面端编辑已有交易一样）。

---

### 4.4 三端对比总结

| 维度 | 桌面端新建 | 桌面端编辑已有 | 移动端 |
|------|:---------:|:-------------:|:------:|
| **保存模型** | 批量提交（按钮/Enter） | 逐字段即时保存（blur/Tab/Enter） | 整体提交（Footer 按钮） |
| **error 能否被保存到后端** | ❌ 不能 | ✅ 会被一同保存 | ❌ 不能 |
| **差额时能否触发保存** | ❌ 按钮被替换 + Enter 拦截 | ✅ blur / Tab 都会直接保存 | ❌ Save 按钮不渲染 |
| **差额时 Enter 键行为** | 拦截为 onAddSplit | 仅最后子项拦截为 onAddSplit，其余照常 | N/A（无 inline 编辑） |
| **error 显示位置** | 新建区下方 | 最后一个子项下方（展开）/ 父行下方（折叠） | Footer 按钮文案中 |
| **引导操作** | Distribute + Add Split 按钮 | Distribute + Add Split 按钮 | onSplit / onAddSplit / 滚动定位 三选一 |
| **校验二次防线** | 无（按钮已被替换） | 无（blur 直接保存） | 无（onSaveInner 不检查 error） |

### 4.5 设计意图解读

为什么桌面端编辑已有交易允许「带 error 保存」？

1. **编辑是持续的中间态**：用户可能改完子项 A 金额，需要先离开一下（比如去查另一个账户的数字），再回来改子项 B。如果 blur 就拦住，会严重打断工作流。
2. **error 是合法的数据状态**：`error` 字段被持久化到数据库，意味着系统承认"差额拆分"是一种可存储的状态。它不会影响余额计算（余额是按子项计算的），只是提示用户还没填完。
3. **与新建模式的区别**：新建交易没有"保存中间态"的需求——新建区是临时草稿，必须完整才能入库。

### 4.6 桌面端新建交易：Ctrl+Enter 快捷键绕过路径分析

表面上桌面端新建交易在差额状态下无法保存（按钮被替换、普通 Enter 被拦截），但 **Ctrl+Enter（Cmd+Enter）是一个例外**。

#### 4.6.1 快捷键触发路径

普通 Enter 在 [onCheckNewEnter#L3067-L3084](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L3067-L3084) 走完整的 error 分支判断（有 error 时走 onAddSplit，无 error 时走 onAddTemporary），而 Ctrl+Enter 走的是 [onCheckNewEnter#L3043-L3051](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L3043-L3051) 的独立分支：

```typescript
if (e.key === 'Enter') {
  if (e.metaKey || e.ctrlKey) {
    e.preventDefault();
    e.stopPropagation();
    afterSave(() => {
      shouldAddAndClose.current = true;   // ← 直接标记为"添加并关闭"
      forceRerender({});
    });
  } else if (!e.shiftKey) {
    // 普通 Enter：这里才做 error 检查
    afterSave(() => {
      if (latestState.current.newTransactions[0].error && ...) {
        onAddSplit(lastTransaction.id);
      } else if (...) {
        onAddTemporary();
      }
    });
  }
}
```

Ctrl+Enter 分支**完全没有检查 `error` 字段**——直接设置 `shouldAddAndClose.current = true`。

#### 4.6.2 添加/添加并关闭的统一检查点

接下来在 [L2934-L2963](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L2934-L2963) 的渲染循环中，只检查了一件事：

```typescript
if (shouldAdd.current || shouldAddAndClose.current) {
  if (newTransactions?.[0] && newTransactions[0].account == null) {
    dispatch(addNotification({ type: 'error', message: 'Account is a required field' }));
    newNavigator.onEdit('temp', 'account');
  } else {
    // 没有对 error 做任何检查！
    if (shouldAddAndClose.current) {
      props.onAdd(transactions);           // ← 直接入库
      props.onCloseAddTransaction();
    } else {
      props.onAdd(transactions);
    }
  }
  shouldAdd.current = false;
  shouldAddAndClose.current = false;
}
```

唯一的校验只有 `account == null`（账户必填）。**`SplitTransactionError` 完全不在检查范围内**。

#### 4.6.3 结论

| 提交方式 | 是否受 error 拦截 |
|---------|:---------------:|
| 点击 Add 按钮 | ✅ 被拦截（按钮不渲染） |
| 普通 Enter（最后子项） | ✅ 被拦截（走 onAddSplit 分支） |
| **Ctrl+Enter / Cmd+Enter** | ❌ **绕过，直接提交** |

这是一个潜在的绕过路径。只要账户已选且处于差额状态，按 Ctrl+Enter 就能把带 `SplitTransactionError` 的交易直接写入数据库（和桌面端编辑已有交易一样，差额状态会被持久化）。

---

## 五、差额为何能成为有效中间态——各层约束全链路分析

差额（`error: { type: 'SplitTransactionError', difference }`）能被保存到数据库、在多端同步后仍是有效状态，是因为整个技术栈从类型到存储**没有任何一层施加强制约束**。下面逐层拆解。

### 5.1 TypeScript 类型层

定义在 [transaction.ts#L32-L36](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/types/models/transaction.ts#L32-L36)：

```typescript
error?: {
  type: 'SplitTransactionError';
  version: 1;
  difference: number;
} | null;
```

特点：
- **可选字段**（`?` 标记）——可以是 `undefined`
- **联合类型**包含 `null`——可以显式为 `null`
- 没有 `@deprecated`、`@internal` 或 branded type 等修饰
- `difference` 仅标注 `number` 不做范围限制（可以正数/负数/0 都合法）

从 TS 类型上，不存在任何强制要求"有 error 就不能入库的静态检查。

### 5.2 AQL Schema 层

定义在 [aql/schema/index.ts#L35-L59](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/server/aql/schema/index.ts#L35-L59)：

```typescript
export const schema = {
  transactions: {
    id: f('id'),
    // ...
    amount: f('integer', { default: 0, required: true }),  // 有 required
    account: f('id', { ref: 'accounts', required: true }),  // 有 required
    error: f('json'),  // ← 无 required，无 validator，类型为 json
    // ...
  }
};
```

`error` 字段：
- 类型声明为 **`'json'`——写入时自动 `JSON.stringify`，读出时自动 `JSON.parse`
- **无 `required: true`**——允许缺省
- **无 `default`——也不自动注入
- **无自定义 validator**（AQL schema 机制支持 `validator` 函数但此处未用）

相较于 `amount` 和 `account` 都有 `required: true`，`error` 完全"裸奔"。

### 5.3 SQLite 存储层

定义在 [init.sql#L38-L56](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/server/sql/init.sql#L38-L56)：

```sql
CREATE TABLE transactions
  (id TEXT PRIMARY KEY,
   isParent INTEGER DEFAULT 0,
   isChild INTEGER DEFAULT 0,
   acct TEXT,
   amount INTEGER,        -- 业务字段
   error TEXT,         -- ← 无 NOT NULL / CHECK / FOREIGN KEY 等任何约束
   ...
   tombstone INTEGER DEFAULT 0);
```

数据库层 `error` 列定义为**普通 `TEXT`**：
- 无 `NOT NULL` → 允许 NULL
- 无 `CHECK (isParent = 1 AND error IS NOT NULL` 之类的条件约束
- 无 `CHECK` 或 `TRIGGER` 校验 `isParent=1 时子项 sum(amount) = parent.amount
- 无外键关联到其他表
- `error` 值为 `'{"type":"SplitTransactionError","version":1,"difference":1234}'` 的 JSON 字符串

### 5.4 数据库 CRUD 封装层

在 [db/index.ts#L808-L816](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/server/db/index.ts#L808-L816)：

```typescript
export function insertTransaction(transaction): Promise<TransactionEntity['id']> {
  return insertWithSchema('transactions', transaction);
}

export function updateTransaction(transaction) {
  return updateWithSchema('transactions', transaction);
}
```

这两个函数直接走 `convertForInsert` / `convertForUpdate`，仅做 **字段名映射（驼峰↔下划线）和 **类型转换（boolean↔INTEGER、json↔TEXT），不附加任何校验。

`insertWithSchema` 内部会为缺失字段走 schema 中 `required: true` 的检查，但 `error` 无 `required`。

### 5.5 业务 API 层

在 [batchUpdateTransactions#L40-L196](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/server/transactions/index.ts#L40-L196) 中对交易做的全部"规范化"操作：

```typescript
// added 阶段
added.map(async t => {
  if (t.is_parent || account?.offbudget === 1) {
    t.category = null;       // 仅清除 category
  }
  return db.insertTransaction(t);
});

// updated 阶段
updated.map(async t => {
  if (t.is_parent || account?.offbudget === 1) {
    t.category = null;       // 仅清除 category
  }
  await db.updateTransaction(t);
});
```

只做一件事：`is_parent` 或 `offbudget` → 清除 `category`。**没有任何地方校验"有 error 就拒绝写入。

### 5.6 Transfer 转账关联层

在 [transfer.ts#L47-L93](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/server/transactions/transfer.ts#L47-L93)：

```typescript
export async function addTransfer(transaction, transferredAccount) {
  if (transaction.is_parent) {
    // 父交易直接跳过，不处理（子交易处理转账
    return null;
  }
  // ... 只有非父交易才创建转账
}
```

`onUpdate` 里也只检查 `transaction.is_parent`，**对 `error` 完全不关心。

### 5.7 各层约束汇总表

| 层级 | 是否校验差额一致性 | 施加的约束 |
|------|:---------------:|-----------|
| **TypeScript 类型 | ❌ | `error` 为可选联合类型，无 branded / 无范围限制 |
| **AQL Schema | ❌ | `error: f('json')`，无 required、无 validator |
| **SQLite DDL | ❌ | `error TEXT`，无 NOT NULL、无 CHECK、无 TRIGGER |
| **DB CRUD 封装** | ❌ | 仅字段映射和类型转换 |
| **API 业务层** | ❌ | 仅清 category，不查子项和 |
| **Transfer 层** | ❌ | 仅区分父/子，不管 error |
| **前端 UI 渲染层** | ✅（部分） | 按钮互斥 / Enter 拦截，但 Ctrl+Enter 可绕过 |

**结论**：差额中间态是有效的根本原因在于——整个系统选择把校验责任完全交给前端 UI 渲染层，后端链路刻意不做数据库或 API 级别的约束。这种设计允许"未完成的草稿"可被持久化并跨设备同步，但同时也引入了绕过 UI 就能写入脏数据的风险。

---

## 六、差额自动修复：`onDistributeRemainder`

在 [TransactionsTable.tsx#L3315-L3443](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/desktop-client/src/components/transactions/TransactionsTable.tsx#L3315-L3443) 实现了两种分配策略：

### 5.1 获取父交易和剩余金额

```typescript
const parentTransaction = transaction?.is_parent
  ? transaction
  : targetTransactions.find(t => t.id === transaction?.parent_id);

const siblingTransactions = targetTransactions.filter(
  t => t.parent_id && t.parent_id === (transaction?.is_parent ? transaction?.id : transaction?.parent_id),
);

const remainingAmount =
  parentTransaction.amount - siblingTransactions.reduce((acc, t) => acc + t.amount, 0);
```

### 5.2 模式 A：存在「金额为 0 的空子项」 → 平均分配

```typescript
const emptyTransactions = siblingTransactions.filter(t => t.amount === 0);

if (emptyTransactions.length > 0) {
  const amountPerTransaction = Math.floor(remainingAmount / emptyTransactions.length);
  let remainingCents = remainingAmount - amountPerTransaction * emptyTransactions.length;

  amounts = new Array(emptyTransactions.length).fill(amountPerTransaction);

  // 把余下的分（rounding error）逐个加到前面的子项
  for (const [amountIndex] of amounts.entries()) {
    if (remainingCents === 0) break;
    amounts[amountIndex] += 1;
    remainingCents--;
  }

  // 逐个保存
  for (const [transactionIndex, transaction] of emptyTransactions.entries()) {
    await onSave({ ...transaction, amount: amounts[transactionIndex] });
  }
}
```

### 5.3 模式 B：所有子项都有值 → 按当前比例追加

```typescript
else if (emptyTransactions.length === 0 && siblingTransactions.length > 0) {
  const siblingTotal = siblingTransactions.reduce((acc, t) => acc + t.amount, 0);
  const siblingProportions = siblingTransactions.map(t => t.amount / siblingTotal);

  for (const [transactionIndex, transaction] of siblingTransactions.entries()) {
    amounts[transactionIndex] =
      Math.floor(siblingProportions[transactionIndex] * remainingAmount) + transaction.amount;
  }

  // 尾部分摊 rounding error
  let remainingCents = parentTransaction.amount - amounts.reduce((acc, a) => acc + a, 0);
  let amountIndex = 0;
  while (remainingCents !== 0) {
    amountIndex = amountIndex % amounts.length;
    if (remainingCents > 0) {
      amounts[amountIndex] += 1;
      remainingCents--;
    } else {
      amounts[amountIndex] -= 1;
      remainingCents++;
    }
    amountIndex++;
  }

  // 逐个保存
  for (const [transactionIndex, transaction] of siblingTransactions.entries()) {
    await onSave({ ...transaction, amount: amounts[transactionIndex] });
  }
}
```

所有分配最终都调用 `onSave()` 写回，而 `onSave` 内部走 `updateTransaction → recalculateSplit`，分配完成后 `error` 应变为 `null`。

---

## 六、规则引擎中的特殊处理

在 [rule.ts#L15-L92](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/server/rules/rule.ts#L15-L92)，规则系统利用 `error.difference` 实现了 `set-split-amount` 动作的三种分配模式：

### 6.1 `getSplitRemainder` 辅助函数

```typescript
function getSplitRemainder(transactions) {
  const { error } = recalculateSplit(groupTransaction(transactions));
  return error ? error.difference : 0;
}
```

规则引擎通过这个函数复用了 `recalculateSplit` 的计算结果。

### 6.2 三阶段分配

1. **fixed-amount**：直接设置固定金额
2. **fixed-percent**：基于 `remainingAfterFixedAmounts`（扣除固定金额后的差额）按百分比分配
3. **remainder**：基于 `remainingAfterFixedPercents`（再扣除百分比金额后的差额）平均分配，最后一个 remainder 子项吸收 rounding error

```typescript
// Distribute to remainder splits.
const remainderActions = splitAmountActions.filter(
  action => action.options.method === 'remainder',
);
const remainingAfterFixedPercents = getSplitRemainder(newTransactions);
if (remainderActions.length !== 0) {
  const amountPerRemainderSplit = Math.round(
    remainingAfterFixedPercents / remainderActions.length,
  );
  // ... 逐个分配
  // The last remainder split will be adjusted for any leftovers from rounding.
  newTransactions[lastNonFixedTransactionIndex].amount +=
    getSplitRemainder(newTransactions);
}
```

最终返回前再走一次 `recalculateSplit`：

```typescript
return recalculateSplit(groupTransaction(newTransactions));
```

---

## 七、保存时的完整调用链路

以桌面端「编辑已有交易 + 拆分后保存」为例：

```
① 用户在子项金额输入框 blur / 按 Enter
  ↓
② Transaction 子组件内部调用 onSave(deserialized, subtransactions, fieldName)
  ↓
③ TransactionsTable 的 onSave 回调 [L3143-L3176]
   ├─ groupTransaction([parent, ...children]) 将父+子合并为分组对象
   ├─ if 临时ID → setNewTransactions(updateTransaction(...).data)  → 本地 state 更新
   │   └─ updateTransaction 内部调用 recalculateSplit 重算 error
   └─ else       → props.onSaveProp(grouped) 交给上层
  ↓
④ TransactionList 的 onSave [L427-L481]
   ├─ updateTransaction(transactionsLatest, transaction)
   │   └─ 内部 replaceTransactions → recalculateSplit 重算 error
   ├─ transactionsLatest = changes.data (乐观更新)
   └─ void saveDiffAndApply(...) → send('transactions-batch-update') 发给后端
  ↓
⑤ 后端 batchUpdateTransactions [index.ts#L40-L196]
   ├─ db.insertTransaction / db.updateTransaction (无金额一致性校验)
   ├─ transfer.onInsert / onUpdate (转账关联处理)
   └─ 返回客户端
```

**关键观察**：

- 错误状态在第 ③ 步 `updateTransaction` 内部就被 `recalculateSplit` 重算了，所以即使进入保存流程，如果用户改过金额后 error 又非空，**下一次渲染**就会立刻把保存按钮藏起来
- 后端完全不校验，这是一个「前端信任模型」

---

## 八、子项创建与数据分组/解组的流转

### 8.1 `makeChild`：创建子交易

在 [transactions.ts#L33-L59](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/shared/transactions.ts#L33-L59)：

```typescript
export function makeChild<T extends GenericTransactionEntity>(
  parent: T,
  data: object = {},
) {
  const prefix = parent.id === 'temp' ? 'temp' : '';
  return {
    amount: 0,
    ...data,
    category: 'category' in data ? data.category : parent.category,
    payee: 'payee' in data ? data.payee : parent.payee,
    id: 'id' in data ? data.id : prefix + uuidv4(),
    account: parent.account,
    date: parent.date,
    cleared: parent.cleared != null ? parent.cleared : null,
    reconciled: parent.reconciled != null ? parent.reconciled : null,
    starting_balance_flag: parent.starting_balance_flag != null ? parent.starting_balance_flag : null,
    sort_order: 'sort_order' in data ? data.sort_order : (parent.sort_order ?? null),
    is_child: true,
    parent_id: parent.id,
    error: null,
  } as unknown as T;
}
```

子项默认金额为 0，继承父项的 `category`、`payee`、`account`、`date` 等字段。

### 8.2 `groupTransaction` / `ungroupTransactions`：分组与解组

```typescript
export function groupTransaction(split: TransactionEntity[]): TransactionEntity {
  return { ...split[0], subtransactions: split.slice(1) };
}

export function ungroupTransactions(transactions: TransactionEntity[]) {
  return transactions.reduce<TransactionEntity[]>((list, parent) => {
    const { subtransactions, ...trans } = parent;
    list.push(trans);
    for (let i = 0; i < (subtransactions || []).length; i++) {
      list.push(subtransactions[i]);
    }
    return list;
  }, []);
}
```

数据库和 API 层面，父子交易是**平铺存储**的（用 `is_parent`、`is_child`、`parent_id` 关联）；在前端编辑时，用 `subtransactions` 嵌套结构方便整体操作。两个方向的转换就是 `groupTransaction` / `ungroupTransactions`。

### 8.3 `addSplitTransaction`：在已有拆分中添加子项

在 [transactions.ts#L240-L257](file:///d:/fz/0601-1/solo-dogfeeding/code/61-actual/packages/loot-core/src/shared/transactions.ts#L240-L257)：

```typescript
export function addSplitTransaction(
  transactions: readonly TransactionEntity[],
  id: string,
) {
  return replaceTransactions(transactions, id, trans => {
    if (!trans.is_parent) {
      return trans;
    }
    const prevSub = last(trans.subtransactions || []);
    trans.subtransactions?.push(
      makeChild(trans, {
        amount: 0,
        sort_order: num(prevSub && prevSub.sort_order) - 1,
      }),
    );
    return trans;   // ← 注意：这里没有调 recalculateSplit！
  });
}
```

**重要细节**：`addSplitTransaction` 新增的子项金额为 0，不影响子项之和，所以**不需要调 `recalculateSplit`**。这保证了不会产生无意义的重算闪烁。

---

## 九、设计总结与风险点

### ✅ 设计亮点

1. **单一校验入口**：所有结构变更必经 `recalculateSplit`，避免校验逻辑分散
2. **错误状态即 UI 状态**：`error` 字段既是数据结构，又直接驱动 UI 渲染，不需要额外派生状态
3. **引导式拦截**：不是粗暴地禁用按钮，而是把按钮替换为「修复入口」（Add Split / Distribute / 滚动定位）
4. **整数金额体系**：`IntegerAmount` 避免浮点比较问题，`===` 严格相等完全可靠
5. **Immutable 更新**：每次操作返回新对象，不修改输入数据

### ⚠️ 潜在风险

1. **后端无防护**：若有人通过 API / 插件直接写入数据库，可能出现 `is_parent=true` 但子项和不等于父项的脏数据
2. **error 仅挂在父对象上**：如果客户端把 children 单独抽出来不看 parent，就感知不到错误（代码用 `parent.error || trans.error` 做了兼容）
3. **移动端保存路径多一步**：`onSaveInner` 只检查 reconciled、schedule，不检查 split error（因为 Footer 已经用渲染互斥保证了 error 时点不到 Save）——如果有人绕过 UI 直接调用 `onSave`，会把带 error 的交易写进库
4. **`addSplitTransaction` 不触发重算**：设计正确（0 金额不影响总和），但依赖于子项初始金额始终为 0 的隐式约束

---

## 十一、触发重算操作的完整清单

| 操作 | 触发 `recalculateSplit` | 说明 |
|------|:-----------------------:|------|
| `splitTransaction()` 初次拆分 | ❌（直接设置 error） | 一次性预置 error |
| `updateTransaction()` 更新字段 | ✅ | 任何字段修改都走重算 |
| `deleteTransaction()` 删除子项 | ✅ | 子项减少后重算 |
| `addSplitTransaction()` 添加子项 | ❌ | 新子项金额为 0，不影响总和 |
| 规则引擎 `execSplitActions()` | ✅ | 规则可能改金额 |
| 账户同步 `normaliseSyncAmounts()` | ✅ | 导入后重算 |
| `onDistributeRemainder()` 分配差额 | ✅（间接） | 通过 onSave → updateTransaction 触发 |
