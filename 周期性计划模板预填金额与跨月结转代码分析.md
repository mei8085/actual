# 周期性计划模板预填金额计算与跨月结转实现分析

## 一、概述

本文档梳理 Actual Budget 中**周期性计划模板（Schedule Template）**的预填金额计算过程，以及**跨月结转（Carryover）**的实现机制。

核心代码分布在以下文件中：

- 计划模板计算：[schedule-template.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/schedule-template.ts)
- 计划公共工具：[schedules.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/shared/schedules.ts)
- 模板上下文管理：[category-template-context.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/category-template-context.ts)
- Envelope 预算结转：[envelope.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/envelope.ts)
- Tracking 预算结转：[tracking.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/tracking.ts)
- 预算动作：[actions.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/actions.ts)
- 模板类型定义：[templates.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/types/models/templates.ts)

---

## 二、周期性计划模板（Schedule Template）预填金额计算

### 2.1 数据结构

Schedule Template 的类型定义：

```typescript
export type ScheduleTemplate = {
  type: 'schedule';
  name: string;           // 关联的计划名称
  full?: boolean;         // 是否在到期月一次性全额预填
  adjustment?: number;    // 调整值
  adjustmentType?: 'percent' | 'fixed';  // 调整类型
} & BaseTemplateWithPriority;
```

### 2.2 整体计算流程

入口函数为 `runSchedule()`，位于 [schedule-template.ts#L305-L395](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/schedule-template.ts#L305-L395)。

整体流程分为以下几个阶段：

```
runSchedule()
  └─ createScheduleList()          // 构建计划列表并计算每个计划的目标金额
       ├─ 从 DB 读取 schedule 信息
       ├─ 提取金额条件（extractScheduleConds）
       ├─ 计算基础金额 + 应用调整
       ├─ 执行规则动作（rule.execActions）处理 BALANCE_OF 等公式
       ├─ 确定目标金额 target（考虑收支符号）
       └─ 重复性计划的月度化累加
  ├─ 分类为 PayMonthOf / Sinking
  ├─ 计算各项汇总值
  └─ 根据资金充足情况选择计算分支
```

### 2.3 阶段一：构建计划列表（createScheduleList）

核心函数：[createScheduleList()](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/schedule-template.ts#L32-L209)

#### 2.3.1 提取金额条件

通过 `extractScheduleConds()` 从 schedule 的 rule conditions 中提取日期和金额条件：[schedules.ts#L328-L354](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/shared/schedules.ts#L328-L354)

支持的金额条件操作符：
- `is`：精确金额
- `isapprox`：近似金额
- `isbetween`：金额区间，取平均值 `(num1 + num2) / 2`

#### 2.3.2 基础金额计算 + 调整（adjustment）

代码位置：[schedule-template.ts#L54-L79](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/schedule-template.ts#L54-L79)

```typescript
// 1. 基础金额
let scheduleAmount = amountCondition.op === 'isbetween'
    ? Math.round(amountCondition.value.num1 + amountCondition.value.num2) / 2
    : amountCondition.value;

// 2. 应用调整
if (template.adjustment !== undefined && template.adjustmentType) {
    switch (template.adjustmentType) {
        case 'percent':  // 百分比调整
            scheduleAmount = scheduleAmount * (1 + template.adjustment / 100);
            break;
        case 'fixed':    // 固定金额调整（保持原符号方向）
            const sign = scheduleAmount < 0 ? -1 : 1;
            scheduleAmount += sign * amountToInteger(template.adjustment, currency.decimalPlaces);
            break;
    }
}
scheduleAmount = Math.round(scheduleAmount);
```

**示例**：
- 计划金额 $100，adjustment=10，adjustmentType=percent → $110
- 计划金额 $100，adjustment=5，adjustmentType=fixed → $105

#### 2.3.3 执行规则动作（处理 BALANCE_OF 公式）

代码位置：[schedule-template.ts#L88-L113](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/schedule-template.ts#L88-L113)

Schedule 模板会调用 `rule.execActions()` 执行关联规则上的动作，其中可能包含 `BALANCE_OF` 等公式。因此需要：

1. 收集动作中的公式字符串 `collectFormulasFromActions()`
2. 预取账户余额 `prefetchBalanceOfForTransaction()`
3. 执行规则得到处理后的金额 `postRuleAmount` 和子交易 `subtransactions`

#### 2.3.4 确定最终目标金额（target）

代码位置：[schedule-template.ts#L118-L124](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/schedule-template.ts#L118-L124)

```typescript
const sign = category.is_income ? 1 : -1;  // 收入类为正，支出类为负
const target = sign * (
    categorySubtransactions?.length
        ? categorySubtransactions.reduce((acc, t) => acc + t.amount, 0)  // 子交易金额
        : (postRuleAmount ?? scheduleAmount)  // 规则执行后金额，或原始金额
);
```

**关键点**：
- 支出类别最终取正值（因为预算金额是正数，而交易金额是负数）
- 如果规则产生了分配给当前类别的子交易，则累加子交易金额

#### 2.3.5 重复性计划的月度化处理

代码位置：[schedule-template.ts#L153-L200](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/schedule-template.ts#L153-L200)

对于**重复性计划（isRepeating）且未完成（!completed）**，需要计算当前月份内所有发生次数的总金额：

```typescript
// 计算从当前月到下月之间的所有计划发生日期，累加金额
while (nextDate < nextMonth) {
    monthlyTarget += -target;  // 每次发生的金额
    // 推进到下一次发生日期...
}
t[t.length - 1].target = -monthlyTarget;
```

**示例**：每日计划 $1 × 31 天 = $31

### 2.4 阶段二：分类 PayMonthOf / Sinking

代码位置：[schedule-template.ts#L326-L341](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/schedule-template.ts#L326-L341)

根据条件将计划分为两类：

#### PayMonthOf（当月支付类）—— 本月全额预填

满足以下任一条件即为 PayMonthOf：
- `template.full === true`（用户标记为全额）
- 每月一次且本月到期（`frequency=monthly`, `interval=1`, `num_months=0`）
- 每周计划且间隔 ≤ 4 周
- 每日计划且间隔 ≤ 31 天
- Tracking 预算模式下**所有计划**都走此分支

#### Sinking（沉淀/储蓄类）—— 按月分摊

不满足 PayMonthOf 条件的计划按到期日期排序（最近的优先），例如：
- 年度保险费（每年一次，距当前还有 11 个月）
- 双月账单（每 2 个月一次）
- 六周计划（间隔 > 4 周）

### 2.5 阶段三：计算汇总值

| 汇总值 | 计算函数 | 含义 |
|--------|----------|------|
| `totalPayMonthOf` | `getPayMonthOfTotal()` | 本月到期的 PayMonthOf 计划金额总和 |
| `totalSinking` | `getSinkingTotal()` | 所有 Sinking 计划的目标金额总和 |
| `totalSinkingBaseContribution` | `getSinkingBaseContributionTotal()` | 所有 Sinking 计划的月度基础分摊之和 |

### 2.6 阶段四：月度基础分摊计算（getMonthlyBaseContribution）

代码位置：[schedule-template.ts#L254-L288](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/schedule-template.ts#L254-L288)

不同频率的分摊公式：

| 频率 | 计算公式 | 示例 |
|------|----------|------|
| yearly（年度） | `target / interval / 12` | 年度 $1200 → 每月 $100 |
| monthly（月度） | `target / interval` | 双月 $200 → 每月 $100 |
| weekly（周度） | `target / intervalMonths` | 六周 $60，跨度 2 月 → 每月 $30 |
| daily（日度） | `target / intervalMonths` | 60 天 $60，跨度 2 月 → 每月 $30 |

### 2.7 阶段五：最终金额计算（两条分支）

代码位置：[schedule-template.ts#L367-L393](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/schedule-template.ts#L367-L393)

#### 分支 A：资金充足时（使用基础分摊）

触发条件（满足其一）：
1. 已有余额 ≥ 所有计划总目标（`balance >= totalSinking + totalPayMonthOf`）
2. 上月目标 < 本月总目标，且上月目标 ≠ 0，且余额 ≥ 上月目标，且存在周/日计划

```typescript
to_budget += Math.round(totalPayMonthOf + totalSinkingBaseContribution);
```

即：本月到期的全额 + 远期计划的月度分摊

#### 分支 B：资金不足时（使用精确分摊）

通过 `getSinkingContributionBreakdown()` 计算：[schedule-template.ts#L221-L252](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/schedule-template.ts#L221-L252)

逻辑：按到期日期从近到远，依次用已有余额覆盖，**剩余未覆盖部分平均分摊到剩余月份**。

```typescript
// 对于每个 schedule（按日期排序）
remainder = schedule.target - (index === 0 ? last_month_balance : remainder);
tg = remainder >= 0 ? remainder : 0;          // 本月需要覆盖的金额
remainder = remainder >= 0 ? 0 : Math.abs(remainder);  // 多余的余额留给下一个计划
contribution = tg / (schedule.num_months + 1);  // 分摊到每个月
```

**示例**：
- 5月账单 $1200（距当前 5 个月），11月账单 $600（距当前 11 个月），上月余额 $200
- 处理 5月：$1200 - $200 = $1000，分摊 5+1=6 月 → 每月 $166.67
- 处理 11月：$600 - $0 = $600，分摊 11+1=12 月 → 每月 $50.00
- 合计：$216.67

### 2.8 计算结果字段

`runSchedule()` 返回：

```typescript
{
    to_budget: number;              // 本月应预算总金额
    errors: string[];               // 错误信息（如计划已过期）
    remainder: number;              // 分摊后的剩余金额
    perScheduleMonthly: Map<string, number>;  // 每个计划的月度贡献（用于UI展示）
}
```

---

## 三、跨月结转（Carryover）实现

### 3.1 核心概念

跨月结转决定了**上个月的剩余/超支金额是否带入本月**。涉及两种预算模式：

| 模式 | 文件 | 核心差异 |
|------|------|----------|
| Envelope（信封预算） | [envelope.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/envelope.ts) | 超支不带入，结余可选择是否带入 |
| Tracking（跟踪预算） | [tracking.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/tracking.ts) | 超支和结余都可选择是否带入 |

### 3.2 电子表格单元格定义

每个类别每月在电子表格中创建以下单元格：

| 单元格名 | 类型 | 含义 |
|----------|------|------|
| `budget-{catId}` | Static | 本月预算金额 |
| `carryover-{catId}` | Static | 是否开启结转（布尔值） |
| `leftover-{catId}` | Dynamic | 本月剩余金额（考虑结转） |
| `leftover-pos-{catId}` | Dynamic | 本月正向剩余（仅 Envelope） |
| `spent-with-carryover-{catId}` | Dynamic | 含结转的支出（仅 Tracking） |

### 3.3 Envelope 模式结转实现

代码位置：[envelope.ts#L48-L72](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/envelope.ts#L48-L72)

#### leftover 计算公式

```typescript
leftover = budgeted                    // 本月预算
    + spent                             // 本月支出（负数）
    + (prevCarryover                   // 上月是否结转？
        ? prevLeftover                  // 是：带上月全部剩余（含超支负数）
        : prevLeftoverPos);             // 否：仅带上月正向结余
```

依赖项：
- `budget-{catId}`：本月预算
- `sum-amount-{catId}`：本月支出
- `${prevSheetName}!carryover-{catId}`：上月结转标志
- `${prevSheetName}!leftover-{catId}`：上月总剩余
- `${prevSheetName}!leftover-pos-{catId}`：上月正向剩余

#### leftover-pos（正向剩余）

```typescript
leftover-pos = leftover < 0 ? 0 : leftover;
```

仅保留非负值，用于下月未结转时的基础值。

#### 上月超支汇总（last-month-overspent）

代码位置：[envelope.ts#L129-L148](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/envelope.ts#L129-L148)

汇总所有**上月未开启结转**类别的超支金额：

```typescript
last-month-overspent = Σ (carryover ? 0 : min(0, leftover))
```

这部分超支会计入本月的 `to-budget`（可预算金额）中，意味着需要用本月收入来覆盖上月超支。

### 3.4 Tracking 模式结转实现

代码位置：[tracking.ts#L23-L46](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/tracking.ts#L23-L46)

#### leftover 计算公式

```typescript
if (is_income) {
    // 收入类别
    leftover = budgeted - spent + (prevCarryover ? prevLeftover : 0);
} else {
    // 支出类别
    leftover = budgeted + spent + (prevCarryover ? prevLeftover : 0);
}
```

**与 Envelope 的区别**：
- 收入类别单独处理（budgeted - spent）
- 未结转时，上月结余和超支都不带入（直接取 0）

#### spent-with-carryover（含结转支出）

代码位置：[tracking.ts#L47-L61](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/tracking.ts#L47-L61)

```typescript
spent-with-carryover = carryover
    ? max(0, budgeted + spent)   // 结转时：只显示正向部分
    : sumAmount;                   // 不结转：显示实际支出
```

### 3.5 类别模板上下文中的结转处理

代码位置：[category-template-context.ts#L55-L73](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/category-template-context.ts#L55-L73)

在 `CategoryTemplateContext.init()` 中确定 `fromLastMonth`（上月带入金额）：

```typescript
let fromLastMonth = await getSheetValue(lastMonthSheet, `leftover-${category.id}`);
const carryover = await getSheetBoolean(lastMonthSheet, `carryover-${category.id}`);

if (
    (fromLastMonth < 0 && !carryover) ||  // 上月超支 且 未结转 → 清零
    category.is_income ||                  // 收入类别 → 清零
    (isTrackingBudget() && !carryover)     // Tracking模式 且 未结转 → 清零
) {
    fromLastMonth = 0;
}
```

**清零条件总结**：
1. 上月超支（fromLastMonth < 0）且未开启结转
2. 收入类别（始终清零）
3. Tracking 预算模式且未开启结转

### 3.6 结转开关设置

代码位置：[actions.ts#L619-L636](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/actions.ts#L619-L636)

用户通过 `setCategoryCarryover()` 设置结转标志，会从指定月份开始对所有未来月份生效：

```typescript
export async function setCategoryCarryover({ startMonth, category, flag }) {
    const months = getAllMonths(startMonth);  // 从 startMonth 开始的所有已创建月份
    for (const month of months) {
        setCarryover(table, category, dbMonth(month).toString(), flag);
    }
}
```

### 3.7 to-budget（可预算金额）完整公式

代码位置：[envelope.ts#L193-L209](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/envelope.ts#L193-L209)

```
to-budget = available-funds       // 可用资金（本月收入 + 上月结转来的可预算额）
          + last-month-overspent  // 上月未结转的超支（需要补的洞）
          + total-budgeted        // 已预算总额（负数）
          - buffered-selected     // 缓冲/持有金额
```

---

## 四、关键调用链路

### 4.1 模板应用主流程

```
CategoryTemplateContext.init()        // 初始化，读取上月结转
  └─ 计算 fromLastMonth（考虑结转规则）
  └─ 校验所有模板（schedule 名称是否存在等）

CategoryTemplateContext.runTemplatesForPriority()  // 按优先级执行模板
  ├─ switch (template.type)
  │    ├─ case 'schedule':
  │    │    └─ runSchedule()           // ← 计划模板计算入口
  │    ├─ case 'simple': runSimple()
  │    ├─ case 'periodic': runPeriodic()
  │    ├─ case 'percentage': runPercentage()
  │    ├─ case 'by': runBy()
  │    └─ ...
  ├─ redistributeBatch()               // 重新分配批量计算的模板金额
  ├─ 检查 limit（上限）
  ├─ 四舍五入（hideDecimal）
  └─ 检查可用资金，避免超预算

CategoryTemplateContext.runRemainder() // 执行 remainder 模板
CategoryTemplateContext.getValues()     // 获取最终结果
```

### 4.2 runSchedule 在模板上下文中的调用

代码位置：[category-template-context.ts#L203-L224](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/category-template-context.ts#L203-L224)

```typescript
case 'schedule': {
    if (!scheduleFlag) {
        const budgeted = this.fromLastMonth + toBudget;  // 已有资金 = 上月带入 + 本优先级已预算
        const ret = await runSchedule(
            t,                    // 所有同优先级的模板
            this.month,           // 当前月份
            budgeted,             // 已有余额
            remainder,            // 分摊余数
            this.fromLastMonth,   // 上月带入金额
            toBudget,             // 当前已预算金额
            [],                   // 错误数组
            this.category,        // 类别
            this.currency,        // 货币
        );
        newBudget = ret.to_budget - toBudget;  // 本优先级的预算增量
        remainder = ret.remainder;
        schedulePerTemplate = ret.perScheduleMonthly;
        scheduleFlag = true;
    }
    break;
}
```

**注意**：同优先级的所有 schedule 模板是**批量一次性计算**的，而非逐个计算。

---

## 五、测试用例验证

基于 [schedule-template.test.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/schedule-template.test.ts) 的测试用例：

| 测试场景 | 预期结果 |
|----------|----------|
| 月度计划 $100，本月到期 | to_budget = $100 |
| 年度计划 $1200，资金充足 | 每月分摊 $100 |
| 每日计划 $1，1 月份 | $1 × 31 天 = $31 |
| adjustment=10% × $100 | $110 |
| adjustment=fixed $5 × $100 | $105 |
| `full: true` 的年度计划（非到期月） | $0（只在到期月预填） |
| 已完成（completed=1）的计划 | $0（跳过） |
| Tracking 预算模式的远期年度计划 | $0（按 pay-month-of 处理，非到期月不计） |
| 上月余额 $150 > 年度目标 $120 | $0（资金已充足） |
| 多个 sinking 计划按日期排序优先覆盖近期 | 日期越近越先被覆盖 |

---

## 六、总结

### 6.1 预填金额计算的关键字段

| 字段来源 | 说明 |
|----------|------|
| `amountCondition.value` | Schedule 规则中的金额条件（is/isapprox/isbetween） |
| `template.adjustment` / `template.adjustmentType` | 模板上的调整值（百分比或固定金额） |
| `category.is_income` | 收支类别（决定最终符号） |
| `rule.execActions()` 结果 | 规则动作（含 BALANCE_OF 公式）处理后的金额 |
| `dateConditions` | 日期频率（monthly/yearly/weekly/daily）和间隔 |
| `num_months` | 距下次发生的月份数 |
| `template.full` | 是否在到期月一次性全额预填 |
| `last_month_balance` | 上月结余（用于精确分摊） |
| `isTrackingBudget()` | 预算模式（影响 PayMonthOf 判断） |

### 6.2 跨月结转的核心逻辑

| 情况 | Envelope 模式 | Tracking 模式 |
|------|---------------|---------------|
| 开启结转 + 上月结余 ✅ | 带入（leftover） | 带入（leftover） |
| 开启结转 + 上月超支 ❌ | 带入（leftover） | 带入（leftover） |
| 关闭结转 + 上月结余 ✅ | 带入（leftover-pos） | **不带入（0）** |
| 关闭结转 + 上月超支 ❌ | **不带入（0）** | **不带入（0）** |
| 收入类别 | 始终不带入 | 始终不带入 |

---

## 七、跨月结转与 Schedule 模板的衔接：上月结余如何进入下月预算计算

本节详细追踪"上月结余 → 电子表格单元格 → 模板上下文 → runSchedule 参数 → 最终预填金额"的完整数据流，以及不同预算模式下结转开关对 schedule 模板结果的间接影响。

### 7.1 完整数据流总览

```
                          ┌──────────────────────┐
                          │  上月电子表格单元格     │
                          │  leftover-{catId}     │
                          │  carryover-{catId}    │
                          │  leftover-pos-{catId} │
                          └──────────┬───────────┘
                                     │
                    ┌────────────────┼─────────────────┐
                    │                │                  │
                    ▼                ▼                  ▼
          ┌─────────────┐  ┌──────────────┐   ┌───────────────┐
          │ Envelope 模式│  │ Tracking 模式│   │   limit 检查   │
          │ leftover公式 │  │ leftover公式 │   │ fromLastMonth │
          │ (带结转逻辑) │  │ (带结转逻辑) │   │  ≥ limitAmount│
          └──────┬──────┘  └──────┬───────┘   └───────┬───────┘
                 │                │                    │
                 └────────┬───────┘                    │
                          ▼                            ▼
              ┌───────────────────────┐    ┌──────────────────┐
              │ CategoryTemplateContext│    │  limitMet=true   │
              │ .init()               │    │  toBudgetAmount=0│
              │ 计算 fromLastMonth    │    │  跳过模板执行     │
              └──────────┬────────────┘    └──────────────────┘
                         │
              ┌──────────▼────────────┐
              │ runTemplatesForPriority│
              │ 计算 budgeted 参数:    │
              │ fromLastMonth+toBudget │
              └──────────┬────────────┘
                         │
              ┌──────────▼────────────┐
              │ runSchedule()          │
              │ balance = budgeted     │
              │ last_month_balance =   │
              │   fromLastMonth        │
              │                        │
              │ 两条分支都受影响:       │
              │ A: balance 判断资金充足 │
              │ B: last_month_balance  │
              │    精确分摊扣减        │
              └──────────┬────────────┘
                         │
              ┌──────────▼────────────┐
              │ 最终 to_budget 值      │
              │ 写入 budget-{catId}   │
              │ → 影响下月 leftover    │
              └───────────────────────┘
```

### 7.2 第一步：上月 leftover 的计算（电子表格层）

每月创建电子表格时，`leftover-{catId}` 单元格被注册为**动态单元格**，其值在依赖项变化时自动重算。

#### Envelope 模式

代码位置：[envelope.ts#L48-L64](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/envelope.ts#L48-L64)

```typescript
leftover = budgeted + spent + (prevCarryover ? prevLeftover : prevLeftoverPos);
```

| 上月结转 | 上月 leftover | 上月 leftover-pos | 本月带入值 |
|----------|--------------|-------------------|-----------|
| ✅ 开启 | $50 (结余) | $50 | $50（prevLeftover） |
| ✅ 开启 | -$30 (超支) | $0 | -$30（prevLeftover） |
| ❌ 关闭 | $50 (结余) | $50 | $50（prevLeftoverPos） |
| ❌ 关闭 | -$30 (超支) | $0 | $0（prevLeftoverPos = 0） |

**核心差异**：Envelope 模式下，关闭结转时超支被截断为 0，但结余**仍然带入**。

#### Tracking 模式

代码位置：[tracking.ts#L23-L46](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/tracking.ts#L23-L46)

```typescript
// 支出类别
leftover = budgeted + spent + (prevCarryover ? prevLeftover : 0);
```

| 上月结转 | 上月 leftover | 本月带入值 |
|----------|--------------|-----------|
| ✅ 开启 | $50 (结余) | $50 |
| ✅ 开启 | -$30 (超支) | -$30 |
| ❌ 关闭 | $50 (结余) | **$0** |
| ❌ 关闭 | -$30 (超支) | **$0** |

**核心差异**：Tracking 模式下，关闭结转时**结余和超支都不带入**。

### 7.3 第二步：CategoryTemplateContext.init() 读取并过滤 fromLastMonth

代码位置：[category-template-context.ts#L55-L73](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/category-template-context.ts#L55-L73)

```typescript
let fromLastMonth = await getSheetValue(lastMonthSheet, `leftover-${category.id}`);
const carryover = await getSheetBoolean(lastMonthSheet, `carryover-${category.id}`);

if (
    (fromLastMonth < 0 && !carryover) ||  // 条件1
    category.is_income ||                  // 条件2
    (isTrackingBudget() && !carryover)     // 条件3
) {
    fromLastMonth = 0;
}
```

这里存在**双重过滤**：电子表格层已经根据 carryover 规则计算了 leftover，但模板上下文又做了一层额外的过滤。理解这层"双重过滤"是理解衔接的关键。

#### 双重过滤详解

**Envelope 模式**：

电子表格已经做了：关闭结转时，超支不带入（leftover-pos 截断为 0），结余带入（leftover-pos = leftover）。

模板上下文额外做的：
- `fromLastMonth < 0 && !carryover`：如果电子表格计算出的 leftover 仍为负值（说明上月开启了结转但超支），且本月关闭了结转 → 清零
- 注意：Envelope 模式不命中条件 3

**结果**：Envelope 模式下 `fromLastMonth` 始终 ≥ 0（要么是正的结余，要么被清零为 0）。

**Tracking 模式**：

电子表格已经做了：关闭结转时，上月 leftover 不带入（公式中取 0）。

模板上下文额外做的：
- `isTrackingBudget() && !carryover`：只要 Tracking 模式下关闭结转 → 清零
- 这涵盖了电子表格已处理的情况，也处理了边界情况

**结果**：Tracking 模式下关闭结转时 `fromLastMonth = 0`；开启结转时可能为正也可能为负。

#### fromLastMonth 在不同场景下的值

| 场景 | 预算模式 | 上月结转 | 上月 leftover | fromLastMonth |
|------|---------|---------|-------------|---------------|
| 结余+结转 | Envelope | ✅ | $50 | **$50** |
| 超支+结转 | Envelope | ✅ | -$30 | **-$30** |
| 结余+不结转 | Envelope | ❌ | $50 | **$50** |
| 超支+不结转 | Envelope | ❌ | $0 | **$0** |
| 结余+结转 | Tracking | ✅ | $50 | **$50** |
| 超支+结转 | Tracking | ✅ | -$30 | **-$30** |
| 结余+不结转 | Tracking | ❌ | $0 | **$0** |
| 超支+不结转 | Tracking | ❌ | $0 | **$0** |
| 收入类别 | 任意 | 任意 | 任意 | **$0** |

### 7.4 第三步：limit 检查——结转金额可能导致模板被跳过

代码位置：[category-template-context.ts#L550-L622](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/category-template-context.ts#L550-L622)

```typescript
private checkLimit(templates: Template[]) {
    // ...计算 limitAmount...
    if (this.fromLastMonth >= this.limitAmount) {
        this.limitMet = true;
        if (this.limitHold) {
            this.limitExcess = 0;
            this.toBudgetAmount = 0;
            this.fullAmount = 0;
        } else {
            this.limitExcess = this.fromLastMonth - this.limitAmount;
            this.toBudgetAmount = -this.limitExcess;
            this.fullAmount = -this.limitExcess;
        }
    }
}
```

**关键影响**：如果 `fromLastMonth`（受结转影响）已经 ≥ limit 金额，则 `limitMet = true`，后续 `runTemplatesForPriority()` 会直接返回 0：

```typescript
if (this.limitMet) return 0;  // category-template-context.ts L146
```

**示例**：
- 类别设置了 `up to $500 monthly`
- 上月结余 $300，本月开启结转 → `fromLastMonth = $300`
- 此时 `fromLastMonth < limitAmount` → 正常执行模板，但 limit 上限减为 $500 - $300 = $200
- 如果上月结余 $600 → `fromLastMonth >= limitAmount` → `limitMet = true` → 模板被跳过

**这意味着**：开启结转后，上月大额结余可能导致 schedule 模板本月完全不需要预填金额。

### 7.5 第四步：runSchedule 中 balance 和 last_month_balance 的使用

代码位置：[category-template-context.ts#L203-L224](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/category-template-context.ts#L203-L224)

```typescript
case 'schedule': {
    const budgeted = this.fromLastMonth + toBudget;  // ① balance 参数
    const ret = await runSchedule(
        t,
        this.month,
        budgeted,           // ← 传入 balance
        remainder,
        this.fromLastMonth, // ← 传入 last_month_balance
        toBudget,
        [],
        this.category,
        this.currency,
    );
    newBudget = ret.to_budget - toBudget;
    // ...
}
```

`runSchedule` 接收两个受结转影响的参数：

| 参数 | 传值 | 含义 |
|------|------|------|
| `balance` | `fromLastMonth + toBudget` | 当前类别的已有资金（含上月结转+本优先级已预算） |
| `last_month_balance` | `fromLastMonth` | 纯粹的上月带入金额 |

### 7.6 第五步：balance 如何影响 runSchedule 的分支选择

代码位置：[schedule-template.ts#L367-L393](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/schedule-template.ts#L367-L393)

```typescript
if (
    balance >= totalSinking + totalPayMonthOf ||          // 条件1
    (lastMonthGoal < totalSinking + totalPayMonthOf &&    // 条件2
      lastMonthGoal !== 0 &&
      balance >= lastMonthGoal &&
      numSubMonthly > 0)
) {
    // 分支 A：资金充足，使用基础分摊
    to_budget += Math.round(totalPayMonthOf + totalSinkingBaseContribution);
} else {
    // 分支 B：资金不足，使用精确分摊
    const { total: totalSinkingContribution, perSchedule: sinkingPerSchedule } =
        getSinkingContributionBreakdown(t_sinking, remainder, last_month_balance);
    if (t_sinking.length === 0) {
        to_budget += Math.round(totalPayMonthOf + totalSinkingContribution) - last_month_balance;
    } else {
        to_budget += Math.round(totalPayMonthOf + totalSinkingContribution);
    }
}
```

#### balance 对分支选择的影响

`balance` 的值直接决定了走哪个分支：

| fromLastMonth | toBudget | balance | 总目标 | 分支 |
|--------------|----------|---------|--------|------|
| $0 | $0 | $0 | $300 | B（资金不足） |
| $200 | $0 | $200 | $300 | B（资金不足） |
| $300 | $0 | $300 | $300 | A（资金充足） |
| $0 | $100 | $100 | $300 | B（资金不足） |
| $200 | $100 | $300 | $300 | A（资金充足） |

**结转开关的间接影响**：
- **开启结转 → fromLastMonth 可能为正** → balance 更大 → 更可能走分支 A（基础分摊，每月固定金额）
- **关闭结转 → fromLastMonth 可能为 0** → balance 更小 → 更可能走分支 B（精确分摊，金额可能更高因为要补缺口）

### 7.7 第六步：last_month_balance 在精确分摊中的角色

代码位置：[schedule-template.ts#L221-L252](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/schedule-template.ts#L221-L252)

在分支 B 中，`last_month_balance` 直接参与 `getSinkingContributionBreakdown()` 的计算：

```typescript
function getSinkingContributionBreakdown(t, remainder, last_month_balance) {
    for (const [index, schedule] of t.entries()) {
        remainder = index === 0
            ? schedule.target - last_month_balance    // ← 第一个计划直接扣减上月余额
            : schedule.target - remainder;            // ← 后续计划扣减上一次剩余
        let tg = 0;
        if (remainder >= 0) {
            tg = remainder;        // 余额不够覆盖 → 需要分摊 remainder
            remainder = 0;         // 余额用完
        } else {
            tg = 0;                // 余额已覆盖此计划 → 无需额外分摊
            remainder = Math.abs(remainder);  // 多余余额留给下一个计划
        }
        const contribution = tg / (schedule.num_months + 1);
        total += contribution;
    }
}
```

**核心逻辑**：`last_month_balance` 被视为"已有资金"，直接从最近的计划目标中扣减。扣减后剩余的需要分摊到剩余月份。

#### 无 sinking 计划时的特殊处理

```typescript
if (t_sinking.length === 0) {
    to_budget += Math.round(totalPayMonthOf + totalSinkingContribution) - last_month_balance;
}
```

当没有远期计划（只有 PayMonthOf 类计划）时，`last_month_balance` 从最终结果中**直接扣减**。这是因为上月余额已经覆盖了部分本月到期金额，无需重复预填。

#### 数值示例

**场景**：年度保险 $1200（5 月到期，距当前 5 个月）+ 上月余额不同

| fromLastMonth | last_month_balance | 计算过程 | 本月分摊 |
|--------------|-------------------|---------|---------|
| $0 | $0 | ($1200 - $0) / 6 | $200.00 |
| $200 | $200 | ($1200 - $200) / 6 | $166.67 |
| $600 | $600 | ($1200 - $600) / 6 | $100.00 |
| $1200 | $1200 | ($1200 - $1200) / 6 | $0.00 |

**结转开关的影响链**：

```
关闭结转 → fromLastMonth = 0 → last_month_balance = 0 → 完整分摊（$200）
开启结转 → fromLastMonth = $200 → last_month_balance = $200 → 减少分摊（$166.67）
```

### 7.8 第七步：最终预算值写回 → 影响下月 leftover

代码位置：[goal-template.ts#L339-L354](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/goal-template.ts#L339-L354)

模板计算完成后，预算值通过 `setBudget()` 写入电子表格的 `budget-{catId}` 单元格，进而影响 `leftover-{catId}` 的重算，形成闭环：

```
本月: budget = template_result.to_budget
     → leftover = budget + spent + (prevCarryover ? prevLeftover : ...)
     → 下月: fromLastMonth = leftover (经 CategoryTemplateContext.init 过滤)
     → 下月: runSchedule(balance, last_month_balance) 受影响
     → 下月: budget 受影响
     → ...循环
```

### 7.9 结转开关对 Schedule 模板结果影响的完整场景分析

#### 场景 1：Envelope 模式，上月有结余

```
类别: 保险
Schedule: 年度保险 $1200，5 月到期（距当前 5 个月）
上月 leftover: $200（结余）

情况 A：关闭结转
  fromLastMonth = $200  (Envelope 模式下结余仍带入)
  balance = $200
  last_month_balance = $200
  → balance < $1200 → 走分支 B
  → ($1200 - $200) / 6 = $166.67
  → to_budget ≈ $167

情况 B：开启结转
  fromLastMonth = $200  (同上)
  → 结果完全相同: to_budget ≈ $167
  
⚠️ Envelope 模式下，上月有结余时，结转开关对 fromLastMonth 无影响
   （因为 leftover-pos = leftover 当 leftover > 0）
```

#### 场景 2：Envelope 模式，上月超支

```
类别: 餐饮
Schedule: 月度餐费 $400
上月 leftover: -$100（超支）

情况 A：关闭结转
  电子表格 leftover = budget + spent + prevLeftoverPos = budget + spent + 0
  fromLastMonth = 0 (条件1: fromLastMonth<0 && !carryover → 清零)
  balance = 0
  last_month_balance = 0
  → 走分支 B（balance=0 < totalPayMonthOf=$400）
  → to_budget = $400 (全额预填，不扣减上月)

情况 B：开启结转
  电子表格 leftover = budget + spent + prevLeftover = budget + spent + (-$100)
  fromLastMonth = -$100 (不命中任何清零条件)
  balance = -$100
  last_month_balance = -$100
  → 走分支 B（balance=-$100 < totalPayMonthOf=$400）
  → getSinkingContributionBreakdown 中: $400 - (-$100) = $500
  → 但这是 PayMonthOf，无 sinking → to_budget = $400 - (-$100) = $500

⚠️ Envelope 模式下，上月超支+开启结转会导致本月需要额外预填来弥补超支缺口
```

#### 场景 3：Tracking 模式，上月有结余

```
类别: 娱乐
Schedule: 年度订阅 $240，10 月到期（距当前 10 个月）
上月 leftover: $80（结余）

情况 A：关闭结转
  电子表格 leftover = budget + spent + 0  (不带入)
  fromLastMonth = 0 (条件3: isTrackingBudget() && !carryover → 清零)
  balance = 0
  last_month_balance = 0
  → 走分支 B
  → ($240 - $0) / 11 = $21.82
  → to_budget ≈ $22

情况 B：开启结转
  电子表格 leftover = budget + spent + $80
  fromLastMonth = $80
  balance = $80
  last_month_balance = $80
  → balance < $240 → 走分支 B
  → ($240 - $80) / 11 = $14.55
  → to_budget ≈ $15

⚠️ Tracking 模式下，结转开关直接决定上月结余是否参与扣减
   关闭结转 → 每月分摊更多（$22 vs $15）
```

#### 场景 4：Tracking 模式，所有计划都走 PayMonthOf

```
类别: 日用品
Schedule: 每周购物 $50
上月 leftover: $30（结余）

情况 A：关闭结转
  fromLastMonth = 0 (Tracking + !carryover)
  → Tracking 模式下所有计划都是 PayMonthOf
  → 走分支 B（balance=0 < totalPayMonthOf=$200）
  → 无 sinking → to_budget = $200 - $0 = $200

情况 B：开启结转
  fromLastMonth = $30
  balance = $30
  → 走分支 B（balance=$30 < totalPayMonthOf=$200）
  → 无 sinking → to_budget = $200 - $30 = $170

⚠️ Tracking 模式下，开启结转后的上月结余会从本月到期金额中直接扣减
```

### 7.10 limit 上限与结转的交互

代码位置：[category-template-context.ts#L267-L278](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/category-template-context.ts#L267-L278)

```typescript
if (this.limitCheck) {
    if (toBudget + this.toBudgetAmount + this.fromLastMonth >= this.limitAmount) {
        const orig = toBudget;
        toBudget = this.limitAmount - this.toBudgetAmount - this.fromLastMonth;
        this.limitMet = true;
        // ...
    }
}
```

**limit 公式**：`最终预算 = limitAmount - toBudgetAmount(已累计) - fromLastMonth`

这意味着 `fromLastMonth` 会**压缩**模板的可用预算空间：

```
limitAmount = $500
fromLastMonth (结转开启) = $200
→ 最多还能预填 $300

fromLastMonth (结转关闭) = $0
→ 最多还能预填 $500
```

### 7.11 available 资金钳位与结转

代码位置：[category-template-context.ts#L290-L305](file:///d:/fz/0601-1/solo-dogfeeding/code/31-actual/packages/loot-core/src/server/budget/category-template-context.ts#L290-L305)

```typescript
if (
    priority > 0 &&
    available < 0 &&
    !this.category.is_income &&
    !this.skipAvailableClamp
) {
    const adjusted = Math.max(0, toBudget + available);
    toBudget = adjusted;
}
```

当全局可用资金不足时，模板计算结果会被削减为 0。`fromLastMonth` 不直接影响 `available`（`available` 来自全局 `to-budget`），但通过影响 `toBudget` 间接影响后续类别的可用资金。

### 7.12 衔接总结：结转开关对 Schedule 模板的完整影响路径

```
结转开关 (carryover)
    │
    ├─→ 电子表格 leftover 公式
    │     ├─ Envelope: prevCarryover ? prevLeftover : prevLeftoverPos
    │     └─ Tracking: prevCarryover ? prevLeftover : 0
    │
    ├─→ CategoryTemplateContext.init() 过滤
    │     ├─ fromLastMonth < 0 && !carryover → 清零
    │     ├─ is_income → 清零
    │     └─ isTrackingBudget() && !carryover → 清零
    │
    ├─→ limit 检查
    │     └─ fromLastMonth ≥ limitAmount → limitMet → 跳过模板
    │
    ├─→ runSchedule() 的 balance 参数
    │     └─ balance = fromLastMonth + toBudget
    │         └─ 决定走分支 A（基础分摊）还是分支 B（精确分摊）
    │
    ├─→ runSchedule() 的 last_month_balance 参数
    │     └─ getSinkingContributionBreakdown()
    │         └─ 从最近的计划目标中扣减已有余额
    │         └─ 无 sinking 时直接从结果中扣减
    │
    └─→ limit 上限扣减
          └─ 最终预算 = limitAmount - toBudgetAmount - fromLastMonth
```

**核心结论**：

1. **Envelope 模式下**，上月有结余时结转开关对 schedule 模板结果**几乎无影响**（因为 leftover-pos = leftover）；上月超支时开启结转会**增加**本月预填金额（弥补超支缺口）。

2. **Tracking 模式下**，结转开关的影响**最显著**：关闭结转时 fromLastMonth 始终为 0，模板按完整金额分摊；开启结转时上月结余可以**减少**本月预填金额。

3. **limit 交互**：无论哪种模式，fromLastMonth 都会压缩 limit 的可用空间，可能导致模板计算结果被大幅削减甚至清零。

4. **资金充足分支**：当 fromLastMonth 使 balance 达到总目标时，schedule 模板走分支 A（基础分摊），远期计划只需预填每月基础分摊额，当月到期计划全额预填。此时结转带来的结余使得资金"看起来充足"，模板不会额外补填。

5. **资金不足分支**：当 fromLastMonth 较小或为 0 时，schedule 模板走分支 B（精确分摊），`last_month_balance` 直接从最近计划目标中扣减，影响分摊计算。
