# 自动银行同步主链路概念报告

本文档仅描述 **自动银行同步主链路**（GoCardless、SimpleFin、Pluggy.ai、Enable Banking），不包含文件导入流程。

---

## 一、匹配顺序（三步渐进式）

### 1.1 入口与上下文

自动银行同步的核心入口在 `packages/loot-core/src/server/accounts/sync.ts`：

```typescript
// 同步主函数
syncAccount(userId, userKey, acctId, externalAccountId, bankId, ...)

// 内部调用
processBankSyncDownload(download, id, acctRow, initialSync, ...)
  → reconcileTransactions(...)
    → matchTransactions(...)
```

在 `processBankSyncDownload` 中（`sync.ts:968-1098`），会根据账户状态决定 `strictIdChecking` 的值：

```typescript
// 银行同步源账户自动关闭 strictIdChecking
const useStrictIdChecking = !acctRow.account_sync_source;
```

这意味着：
- **银行已链接账户**（`account_sync_source` 非空）：`strictIdChecking = false`
- **非银行同步账户**（如手动创建或文件导入账户）：`strictIdChecking = true`

然后调用 `reconcileTransactions`：

```typescript
// 首次同步
reconcileTransactions(
  id,                    // 本地账户ID
  transactions,          // 银行原始交易
  true,                  // isBankSyncAccount = true
  useStrictIdChecking,   // 银行账户时为 false
  false,                 // isPreview = false
  true,                  // defaultCleared = true
  updateDates,           // 视用户偏好
)

// 后续同步
reconcileTransactions(
  id,
  importTransactions ? transactions : [],  // 受 sync-import-transactions 偏好控制
  true,                  // isBankSyncAccount = true
  useStrictIdChecking,   // 银行账户时为 false
  false,                 // isPreview = false
  true,                  // defaultCleared = true
  updateDates,
)
```

### 1.2 交易规范化

银行返回的原始数据首先经过 `normalizeBankSyncTransactions` 函数处理：

**规范化后的关键字段**：

| 字段 | 来源 | 说明 |
|------|------|------|
| `imported_id` | 优先 `transactionId`，否则 `${account}-${internalTransactionId}` | 用于精确匹配的唯一标识 |
| `imported_payee` | 银行原始收款人名称（trim 后） | 保留银行原始名称 |
| `cleared` | `booked` 字段 | 是否已在银行清算 |
| `raw_synced_data` | `JSON.stringify(原始交易)` | 完整原始数据 |
| `amount` | 金额字段 × 100 后四舍五入 | 内部统一整数表示 |

规范化之后，还会经过 `runRules` 应用用户设置的自动规则（可能修改 payee、category 等）。

### 1.3 匹配流程详解

匹配逻辑在 `matchTransactions` 函数中（`sync.ts:698-897`），采用 **三步渐进式** 策略，每一步都依赖前一步未匹配的结果。

#### 参与匹配的核心维度

| 维度 | 类型 | 匹配中的作用 |
|------|------|-------------|
| `account` | 账户ID | 必须相同（仅在同一账户内匹配） |
| `imported_id` | 字符串 | Pass 1 精确匹配键 |
| `amount` | 整数（分） | 必须完全相等（模糊匹配前提） |
| `date` | YYYY-MM-DD | 模糊匹配窗口：±7 天 |
| `payee` | 收款人ID | Pass 2 增强匹配条件 |

---

#### **Pass 1：`imported_id` 精确匹配（最高优先级）**

```sql
SELECT * FROM v_transactions
WHERE imported_id = ? AND account = ?
```

**执行时机**：仅当银行交易有 `imported_id` 时尝试。

**查询表选择**：
- 如果 `sync-reimport-deleted-${acctId} = true`（默认）：查询 `v_transactions`（包含已删除的 tombstone 记录）
- 否则：查询 `v_transactions_internal`（仅有效记录）

**命中后**：
- 立即标记该本地交易 ID 到 `hasMatched` Set
- 跳过后续 Pass 2 和 Pass 3

**未命中**：继续 Pass 2。

---

#### **Pass 2：模糊匹配 + Payee 匹配（中等优先级）**

当 Pass 1 未命中时，构建模糊匹配候选集。

##### 候选集构建

候选集的 SQL 查询受 `strictIdChecking` 影响：

**strictIdChecking = true（非银行同步账户）**：
```sql
SELECT id, is_parent, date, imported_id, payee, imported_payee,
       category, notes, reconciled, cleared, amount
FROM v_transactions
WHERE
  (imported_id IS NULL OR ? IS NULL)  -- 关键约束：双方都有ID则跳过
  AND date >= ? AND date <= ?        -- 目标日期 ±7 天
  AND amount = ?                     -- 金额完全相等
  AND account = ?                    -- 同一账户
```

**strictIdChecking = false（银行同步账户）**：
```sql
SELECT ...
FROM v_transactions
WHERE date >= ? AND date <= ?  -- 目标日期 ±7 天
  AND amount = ?               -- 金额完全相等
  AND account = ?              -- 同一账户
-- 注意：没有 imported_id IS NULL 的约束！
```

**strictIdChecking 在银行同步场景下的真实行为**：

| 场景 | 银行交易（待匹配） | 本地交易（候选） | strictIdChecking=true | strictIdChecking=false |
|------|-------------------|-----------------|----------------------|-----------------------|
| A | 有 imported_id | 有 imported_id（不同值） | ❌ 从候选集中排除 | ✅ 保留在候选集中 |
| B | 有 imported_id | 无 imported_id | ✅ 保留 | ✅ 保留 |
| C | 无 imported_id | 有 imported_id | ✅ 保留 | ✅ 保留 |
| D | 无 imported_id | 无 imported_id | ✅ 保留 | ✅ 保留 |

**银行同步场景的实际意义**：
- 银行可能对同一笔交易生成不同的 `transactionId`（如系统迁移、数据重发等）
- 如果严格检查，银行重发的交易会被错误地排除在模糊匹配之外，导致重复入账
- 银行同步账户自动关闭严格检查，允许通过日期+金额+payee 来匹配

##### 候选集排序

候选集按"与目标日期的时间差"从近到远排序：
```typescript
fuzzyDataset.sort((a, b) => {
  const aDistance = Math.abs(目标日期 - a.date);
  const bDistance = Math.abs(目标日期 - b.date);
  return aDistance > bDistance ? 1 : -1;
});
```

即：日期相同的排最前，然后是前一天/后一天，再远的排后面。

##### Payee 匹配

在候选集中查找：
```typescript
const match = fuzzyDataset.find(
  row => !hasMatched.has(row.id)  // 未被其他银行交易匹配过
         && data.trans.payee === row.payee  // payee ID 相同
);
```

**命中后**：
- 标记该本地交易 ID 到 `hasMatched` Set
- 跳过 Pass 3

**未命中**：继续 Pass 3。

---

#### **Pass 3：模糊匹配 + 首个可用（最低优先级）**

如果 Payee 匹配也未命中，直接选取候选集中第一个尚未被匹配的交易：

```typescript
const match = fuzzyDataset.find(row => !hasMatched.has(row.id));
```

**设计意图**：
- 候选集已按日期距离排序，取第一个即取"日期最接近"的
- 通过 `hasMatched` Set 保证一笔本地交易最多被匹配一次

---

#### 匹配流程图

```
银行交易 T
    ↓
有 imported_id?
    ├─ Yes → Pass 1: 精确匹配 existing.imported_id
    │         ├─ 命中 → 标记 hasMatched，跳过后续
    │         └─ 未命中 → 继续
    └─ No → 直接进入 Pass 2
              ↓
Pass 2: 构建模糊候选集
        基础条件: account相同 + amount相等 + date在±7天内
        额外约束 (strictIdChecking=true时):
          ├─ 如果双方都有 imported_id 且 Pass 1 未命中
          └─ 则该本地交易从候选集中排除
        按日期距离排序 (近→远)
              ↓
尝试 Payee 匹配 (候选集中找 payee 相同且未被匹配的)
    ├─ 命中 → 标记 hasMatched，跳过 Pass 3
    └─ 未命中 → Pass 3
              ↓
Pass 3: 取候选集中首个未被匹配的
    ├─ 命中 → 标记 hasMatched
    └─ 未命中 → 匹配失败，作为新增交易
```

---

## 二、冲突回退规则

当匹配成功（Pass 1/2/3 任意一步命中），进入 `reconcileTransactions` 的更新逻辑（`sync.ts:569-645`）。

### 2.1 已对账交易保护

```typescript
// 如果本地交易已对账，跳过更新
if (match.reconciled) {
  updatedPreview.push({ transaction: trans, ignored: true });
  continue;
}
```

**原因**：对账是用户的重要确认动作，不应被自动同步覆盖。

### 2.2 字段更新策略

采用 **"现有值优先"** 策略（`sync.ts:585-594`）：

```typescript
const updates = {
  imported_id: trans.imported_id || null,           // 新值：用于下次匹配
  imported_payee: trans.imported_payee || null,     // 新值：保留银行原始名称
  payee: existing.payee || trans.payee || null,     // 现有 > 新 > null
  category: existing.category || trans.category || null, // 现有 > 新 > null
  notes: existing.notes || trans.notes || null,     // 现有 > 新 > null
  cleared: existing.cleared || trans.cleared || false, // OR 逻辑
  raw_synced_data: existing.raw_synced_data ?? trans.raw_synced_data ?? null, // 现有优先
};
```

**策略说明**：

| 字段 | 策略 | 原因 |
|------|------|------|
| `imported_id` | 新值 | 用于下次同步精确匹配 |
| `imported_payee` | 新值 | 保留银行原始名称，供规则和展示使用 |
| `payee` | 现有优先 | 保护用户设置的收款人 |
| `category` | 现有优先 | 保护用户设置的分类（这是核心记账数据） |
| `notes` | 现有优先 | 保护用户备注 |
| `cleared` | OR 逻辑 | 任一为已清算则保持已清算 |
| `raw_synced_data` | 现有优先 | 保留首次同步的原始数据（用户可能已调整字段映射） |

### 2.3 日期更新策略

日期是否更新由用户偏好控制：

```typescript
if (updateDates && trans.date) {
  updates['date'] = trans.date;
}
```

其中 `updateDates` 来自偏好 `sync-update-dates-${acctId}`，**默认值为 false**。

| 偏好值 | 行为 |
|--------|------|
| false（默认） | 不更新日期，保留用户录入的日期 |
| true | 用银行交易的日期覆盖本地日期 |

### 2.4 是否执行更新的最终判定

即使匹配成功，也只有当字段实际变化时才执行更新：

```typescript
if (hasFieldsChanged(existing, updates, fieldsToMarkUpdated)) {
  updated.push({ id: existing.id, ...updates });
} else {
  updatedPreview.push({ transaction: trans, ignored: true });
}
```

`fieldsToMarkUpdated` 会排除一种特殊情况：`raw_synced_data` 从 falsy 变为 falsy（例如从 `null` 变为 `undefined`）不视为变化。

### 2.5 新增 vs 更新的最终判定

```typescript
// reconcileTransactions 返回结果
{
  added: string[],    // 新增的交易 ID 列表（匹配失败）
  updated: string[],  // 更新的交易 ID 列表（匹配成功且字段有变化）
}
```

**新增交易**：
- 匹配失败（`match` 为空）
- 或 `forceAddTransaction = true`（银行同步场景不会出现，这是文件导入时用户可选择的选项）

新增交易的默认值：
```typescript
{
  id: uuidv4(),
  category: trans.category || null,
  cleared: trans.cleared ?? defaultCleared,  // 银行同步时 defaultCleared = true
  sort_order: now - index * 1000,  // 按导入顺序生成
}
```

---

## 三、前端可见效果

### 3.1 服务端返回结构

银行同步完成后，服务端返回 `SyncResponseWithErrors`（`app.ts:1217-1219`）：

```typescript
{
  errors: SyncError[],           // 同步错误列表
  newTransactions: string[],     // 新增的交易 ID
  matchedTransactions: string[], // 已匹配（更新）的交易 ID
  updatedAccounts: string[],     // 有新增交易的账户 ID
}
```

**对应关系**：
- `newTransactions` ← `reconcileTransactions().added`
- `matchedTransactions` ← `reconcileTransactions().updated`

### 3.2 前端状态流转

#### 账户同步状态

同步过程中，前端通过 Redux 维护 `accountsSyncing` 状态（`accountsSlice.ts`）：

```typescript
// 开始同步（示例：同步3个账户）
dispatch(setAccountsSyncing({ ids: [acct1, acct2, acct3] }));

// 每完成一个账户，进度式移除
dispatch(setAccountsSyncing({ ids: [acct2, acct3] }));
dispatch(setAccountsSyncing({ ids: [acct3] }));

// 全部完成
dispatch(setAccountsSyncing({ ids: [] }));
```

#### 交易状态（Redux Store）

同步结果通过 `setNewTransactions` action 存入 Redux（`transactionsSlice.ts:38-49`）：

```typescript
// Redux State 结构
transactions: {
  newTransactions: [id1, id2, ...],     // 新增交易 ID 列表
  matchedTransactions: [id3, id4, ...], // 已匹配交易 ID 列表
  lastTransaction: Transaction | null,
}
```

**关键特性**：
- **状态累积**：每次同步会将新结果 **追加** 到现有列表中（`concat`，不是替换）
- **状态清除**：当用户编辑某笔交易时，通过 `updateNewTransactions` 将其从两个列表中移除

```typescript
updateNewTransactions(state, action) {
  state.newTransactions = state.newTransactions.filter(
    id => id !== action.payload.id,
  );
  state.matchedTransactions = state.matchedTransactions.filter(
    id => id !== action.payload.id,
  );
}
```

### 3.3 交易列表的真实展示效果

#### 状态传递链

```
Redux: newTransactions, matchedTransactions
    ↓
Account.tsx 组件读取
    ↓
定义 isNew / isMatched 函数
    ↓
传递给 TransactionList → TransactionsTable → Transaction（每一行）
```

```typescript
// Account.tsx:938-944
isNew = (id) => this.props.newTransactions.includes(id);
isMatched = (id) => this.props.matchedTransactions.includes(id);

// TransactionsTable.tsx:2442-2444
<Transaction
  added={isNew?.(trans.id)}      // 来自 added 列表
  matched={isMatched?.(trans.id)} // 来自 updated 列表
  ...
/>
```

#### 新增交易（`added = true`）的视觉效果

**真实实现**（`TransactionsTable.tsx:1178`）：
```typescript
const valueStyle = added ? { fontWeight: 600 } : null;
```

**效果**：
- 交易行的 **Date、Account、Payee、Notes 字段** 的文字以 **粗体（fontWeight: 600）** 显示
- 应用位置：
  - Date 字段：`valueStyle={valueStyle}`（行 1475）
  - Account 字段：`valueStyle={valueStyle}`（行 1519）
  - Payee 字段：`valueStyle={valueStyle}`（行 1562）
  - Notes 字段：`valueStyle={valueStyle}`（行 1583）

**用户感知**：
- "这笔交易是新的，我还没看过/处理过"

#### 已匹配交易（`matched = true`）的视觉效果

**真实实现**（`TransactionsTable.tsx:1454-1464`）：
```typescript
<SelectCell
  value={matched
    ? <SvgHyperlink2 style={{ width: 13, height: 13, color: 'inherit' }} />
    : undefined
  }
  ...
/>
```

**效果**：
- 在交易行的 **选择框列**（最左侧的复选框位置）显示一个 **链接图标**（`SvgHyperlink2`）
- 这是一个小图标，约 13×13 像素

**用户感知**：
- "这笔本地交易已与银行同步数据匹配确认"
- 图标暗示"已关联/已同步"的状态

#### 标记清除时机

当用户**编辑**该交易的任意字段时：
1. 触发 `updateNewTransactions` action
2. 该交易 ID 从 `newTransactions` 和 `matchedTransactions` 中移除
3. 下次渲染时，`added` 和 `matched` 变为 false
4. 视觉效果消失（粗体恢复正常，链接图标消失）

### 3.4 其他用户可见变化

#### 账户同步状态

- **同步中**：账户列表显示加载/同步指示器
- **同步成功**：账户的 `last_sync` 字段更新为同步完成的时间戳
- **同步失败**：账户显示失败状态，并显示错误通知

#### 通知（Notification）

**成功场景**：
- 无专门的成功通知
- 交易列表自动刷新（通过 `sync-event` 或 React Query 失效）
- 新增/匹配的交易有上述视觉标记

**失败场景**：
- 显示错误通知，包含具体错误信息
- 特殊情况 `RATE_LIMIT_EXCEEDED` 会显示友好提示

### 3.5 视觉效果汇总表

| 状态 | 标记方式 | 展示位置 | 清除时机 |
|------|---------|---------|---------|
| 新增交易（`added = true`） | `fontWeight: 600`（粗体） | Date、Account、Payee、Notes 字段 | 用户编辑该交易时 |
| 已匹配交易（`matched = true`） | `SvgHyperlink2` 链接图标 | 交易行最左侧的选择框列 | 用户编辑该交易时 |

---

## 四、关键代码速查

| 功能 | 文件 | 函数/代码位置 | 行号 |
|------|------|--------------|------|
| strictIdChecking 判定 | `loot-core/src/server/accounts/sync.ts` | `useStrictIdChecking = !acctRow.account_sync_source` | 979 |
| 银行同步 reconcile 调用 | `loot-core/src/server/accounts/sync.ts` | `reconcileTransactions(..., true, useStrictIdChecking, ...)` | 1060-1068, 1082-1090 |
| Pass 1 精确匹配 | `loot-core/src/server/accounts/sync.ts` | `matchTransactions` 中 imported_id 查询 | 746-758 |
| Pass 2 严格 ID 约束 | `loot-core/src/server/accounts/sync.ts` | strictIdChecking 分支的 SQL 条件 | 770-801 |
| Pass 2 非严格模式 | `loot-core/src/server/accounts/sync.ts` | 无 strictIdChecking 的 SQL | 802-824 |
| Pass 2 Payee 匹配 | `loot-core/src/server/accounts/sync.ts` | `transactionsStep2` 映射 | 861-874 |
| Pass 3 首个可用 | `loot-core/src/server/accounts/sync.ts` | `transactionsStep3` 映射 | 880-889 |
| 字段更新策略 | `loot-core/src/server/accounts/sync.ts` | 构建 `updates` 对象 | 585-594 |
| 已对账保护 | `loot-core/src/server/accounts/sync.ts` | `if (match.reconciled) continue` | 572-575 |
| 新增交易粗体效果 | `desktop-client/src/components/transactions/TransactionsTable.tsx` | `valueStyle = added ? { fontWeight: 600 } : null` | 1178 |
| 已匹配链接图标 | `desktop-client/src/components/transactions/TransactionsTable.tsx` | `SelectCell` 的 `value` 字段 | 1454-1464 |
| Redux 状态累积 | `desktop-client/src/transactions/transactionsSlice.ts` | `setNewTransactions` reducer | 38-49 |
| Redux 状态清除 | `desktop-client/src/transactions/transactionsSlice.ts` | `updateNewTransactions` reducer | 50-60 |

---

## 五、核心原则总结

### 匹配去重原则

1. **精确优先，模糊兜底**：`imported_id` 精确匹配 → 日期+金额+payee 模糊 → 日期+金额兜底
2. **银行源宽松匹配**：银行同步账户自动关闭 `strictIdChecking`，允许银行可能的 ID 变化
3. **候选集有序匹配**：按日期距离从近到远排序，`hasMatched` Set 保证一一对应

### 冲突回退原则

1. **用户数据保护**：`payee`、`category`、`notes` 采用"现有优先"策略
2. **已对账锁定**：`reconciled = true` 的交易不被自动更新
3. **变化才更新**：只有字段实际变化时才执行数据库操作

### 前端展示原则

1. **状态持久化**：标记累积在 Redux 中，刷新页面不丢失
2. **交互清除**：用户编辑即清除，不干扰日常使用
3. **视觉差异**：新增用粗体文字，已匹配用链接图标，信息层次分明
