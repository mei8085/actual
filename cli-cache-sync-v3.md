# CLI 缓存与锁协作机制 - 深度校准分析 v3

> **本版校准**：读写互斥时序事实、两类风险区分（CLI 进程间竞争 vs 其他客户端一致性窗口）、逐条标注前提条件与回退路径

---

## 1. 锁机制校准：读写互斥的准确时序

### 1.1 锁实现核心原理

```
┌─────────────────────────────────────────────────────────────────┐
│                     Gate 锁 (lock 文件)                         │
│  互斥锁，保护锁元数据。任何锁获取前必须先持有。                    │
│  proper-lockfile 管理，30 秒 stale 检测。                        │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                   Readers 目录 (readers/)                       │
│  每个共享锁持有者创建 {pid}-{rand} 标记文件。                     │
│  独占锁获取前必须等待该目录为空。                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 共享锁 (Shared) 获取与释放流程

```typescript
// lock.ts:129-149
export async function acquireShared(dir, { timeoutMs }): Promise<Release> {
  const gate = await acquireGate(dir, timeoutMs);    // 1. 获取 gate 锁 (互斥)
  try {
    writeFileSync(markerPath, '');                   // 2. 创建 reader 标记
  } catch {
    await gate();                                    // 异常时释放 gate 锁
    throw err;
  }
  await gate();                                      // 3. 释放 gate 锁！
  return async () => {
    rmSync(markerPath, { force: true });             // 释放：仅删除标记
  };
}
```

**关键事实**：共享锁持有者在**业务逻辑执行期间不持有 gate 锁**，仅通过 reader 标记文件表示存在。

### 1.3 独占锁 (Exclusive) 获取与释放流程

```typescript
// lock.ts:113-127
export async function acquireExclusive(dir, { timeoutMs }): Promise<Release> {
  const release = await acquireGate(dir, timeoutMs);  // 1. 获取 gate 锁 (互斥)
  try {
    await waitForReadersEmpty(dir, remaining);       // 2. 轮询等待 readers 为空
  } catch {
    await release();                                 // 异常时释放 gate 锁
    throw err;
  }
  return () => release();                            // 释放：释放 gate 锁
}
```

**关键事实**：独占锁持有者在**整个业务逻辑执行期间持续持有 gate 锁**。

### 1.4 读写互斥的准确时序

#### 场景：共享锁持有期间，独占锁请求到达

```
时间轴：
T0: 进程 A (只读) 调用 acquireShared
    ├─ 获取 gate 锁 ✓
    ├─ 创建 reader 标记 A-marker
    └─ 释放 gate 锁 ✓
T1: 进程 A 开始执行业务逻辑 (加载预算、查询等)
    ┌───────────────────────────────────────────────┐
    │  此时进程 A 不持有任何锁！                      │
    │  仅通过 readers/A-marker 文件表示存在          │
    └───────────────────────────────────────────────┘
T2: 进程 B (写入) 调用 acquireExclusive
    ├─ 获取 gate 锁 ✓  (因为 A 已释放)
    └─ 进入 waitForReadersEmpty 轮询
        T2.1: 扫描 readers/，发现 A-marker 存在
        T2.2: sleep 100ms
        T2.3: 再次扫描，A-marker 仍存在
        ... (重复直到超时或 A-marker 消失)
T3: 进程 A 业务逻辑完成
    └─ 删除 A-marker 文件
T4: 进程 B 下一次扫描发现 readers/ 为空
    └─ 从 acquireExclusive 返回，开始执行业务逻辑
```

**校准结论**：
- ✅ 共享锁持有期间，独占锁**无法执行写操作**，只能等待
- ✅ 一旦独占锁获取了 gate 锁，**新的共享锁请求会被阻塞**（因为 acquireShared 第一步就是获取 gate 锁）
- ❌ 之前 v2 版中"只读锁持有期间写路径已执行"的描述是错误的
- ✅ 正确描述：共享锁持有期间，独占锁在 `waitForReadersEmpty` 中等待，无法前进

### 1.5 锁保证的互斥性矩阵（校准版）

| 已持有锁 → <br>↓ 请求锁 | 无锁 | 共享锁 | 独占锁 |
|------------------------|------|--------|--------|
| **共享锁** | ✓ 立即获取 | ✓ 立即获取（多读者并行） | ✗ 阻塞（gate 锁被独占） |
| **独占锁** | ✓ 立即获取 | ✗ 等待 readers 清空 | ✗ 阻塞（gate 锁被独占） |

---

## 2. 风险分类与边界

将风险分为**两类本质不同**的场景：

| 类别 | 定义 | 锁能否防止 | 影响范围 |
|------|------|-----------|---------|
| **A 类：CLI 进程间竞争** | 多个 CLI 进程同时操作同一份预算 | ✅ 部分可防止 | CLI 进程之间 |
| **B 类：其他客户端一致性窗口** | 桌面端/移动设备/其他 API 客户端修改数据后，CLI 缓存不一致 | ❌ 完全无法防止 | 所有客户端之间 |

---

## 3. A 类风险：CLI 进程间竞争

### A1：多只读进程同时同步

**前提条件**：
1. 多个只读命令（`mutates: false`）在短时间内（< 网络往返时间）启动
2. 满足同步触发条件之一：
   - TTL 刚好过期
   - 使用了 `--refresh`
   - 是加密预算
   - TTL = 0

**时序**：
```
T0: 进程 A、B、C (都是 accounts list) 几乎同时启动
T1: 三个进程都调用 acquireShared
    ├─ 都获取 gate 锁（串行获取，因为 gate 是互斥的）
    ├─ 都创建各自的 reader 标记
    └─ 都释放 gate 锁
T2: 三个进程都读取 state.json，都决定需要同步
T3: 三个进程**并行**调用 api.sync()
T4: 三个进程各自执行业务查询
T5: 三个进程各自删除 reader 标记
```

**影响**：
- ✅ 正确性：CRDT 同步是幂等的，无数据损坏风险
- ❌ 效率：N 倍重复网络请求和服务器负载
- ❌ 资源浪费：N 倍带宽消耗

**回退路径**：
1. 延长 TTL：`--cache-ttl 3600`（减少同步频率）
2. 主动预热：先执行 `actual sync`，再批量执行只读命令
3. 脚本串行化：避免在 shell 脚本中使用 `&` 并行调用 CLI

---

### A2：缓存写入竞争 (Last-Write-Wins)

**前提条件**：
1. 多个进程（只读或写入）同时完成同步
2. 都调用 `writeCacheState()` 更新 `lastSyncedAt`

**时序**：
```
T0: 进程 A 同步完成，lastSyncedAt = 1002
T1: 进程 B 同步完成，lastSyncedAt = 1000
T2: 进程 A 写入临时文件 state.json.pidA-randA
T3: 进程 B 写入临时文件 state.json.pidB-randB
T4: 进程 A renameSync(tmpA, state.json) → state.json = 1002
T5: 进程 B renameSync(tmpB, state.json) → state.json = 1000 (覆盖 A)
```

**原子写入保证**：
```typescript
// cache.ts:64-66
const tmp = `${target}.${process.pid}-${randomBytes(4).toString('hex')}.tmp`;
writeFileSync(tmp, JSON.stringify(state));
renameSync(tmp, target);  // 原子操作，永不损坏
```

**影响**：
- ✅ 文件永不损坏（原子重命名保证）
- ⚠️ 可能丢失较新的 `lastSyncedAt` 时间戳
- ⚠️ 最坏情况：缓存时间戳倒退几毫秒，导致下次命令提前同步

**回退路径**：
- 这是设计上的"最后写入者胜"策略
- 影响极小，无需特殊处理
- 不影响数据正确性，仅可能提前几毫秒触发下次同步

---

### A3：崩溃进程的锁残留

**前提条件**：
1. CLI 进程持有锁期间被强制终止（`kill -9`、断电、OOM）
2. 进程来不及执行释放逻辑

**锁组件的恢复机制**：

| 锁组件 | 恢复机制 | 恢复时间 |
|--------|---------|---------|
| Gate 锁 | proper-lockfile 的 `stale: 30_000` 选项 | 最多 30 秒 |
| Reader 标记文件 | 下次 `acquireExclusive()` 时扫描清理 | 下一个写命令启动时 |

**时序（崩溃场景）**：
```
T0: 进程 A (写入) 获取独占锁，持有 gate 锁
T1: 进程 A 被 kill -9，未释放锁
T2: 进程 B (写入) 尝试获取独占锁
    ├─ 尝试获取 gate 锁 → 被持有
    ├─ proper-lockfile 内部检测 mtime，30 秒后视为 stale
    └─ 30 秒后成功获取 gate 锁
T3: 进程 B 扫描 readers/，清理可能残留的标记
T4: 进程 B 正常执行
```

**回退路径**：
1. 等待 30 秒自动恢复
2. 单进程环境可使用 `--no-lock` 绕过
3. 手动清理：删除 `{dataDir}/.actual-cli/{syncId}/lock` 和 `readers/` 目录

---

### A4：锁获取超时

**前提条件**：
1. 另一个 CLI 进程正在执行慢操作（如大额数据导入、批量交易处理）
2. 或存在崩溃进程的锁残留（见 A3）

**错误信息**：
```
Error: Another CLI process is holding the budget (waited 10s).
Retry, or use a different --data-dir.
```

**回退路径**：
1. 重试命令
2. 延长超时：`--lock-timeout 60`（默认 10 秒）
3. 检查是否有僵死进程：`ps aux | grep actual`
4. 确认不是桌面端在操作（桌面端不参与 CLI 锁，但可能占用服务器资源）

---

### A5：写命令排队超时

**前提条件**：
1. 多个写命令在短时间内连续触发
2. 每个写命令的执行时间 > 锁超时时间 / 队列长度

**示例**：
- 锁超时 10 秒
- 每个写命令执行 3 秒
- 同时触发 4 个写命令 → 第 4 个需要等待 9 秒 → 可能超时

**回退路径**：
1. 延长超时：`--lock-timeout 30`
2. 脚本中串行执行写命令，避免并行触发
3. 考虑使用 API 批量操作（如果支持）

---

## 4. B 类风险：其他客户端一致性窗口

这类风险的本质是：**CLI 的文件锁机制仅协调 CLI 进程之间，不涉及桌面端、移动设备或其他 API 客户端**。这些客户端通过同步服务器交换数据，CLI 只能通过缓存 TTL 和显式同步来感知。

### B1：共享锁持有期间，其他客户端修改了数据

**前提条件**：
1. CLI 只读进程获取共享锁，检查 TTL 有效
2. CLI 加载本地预算数据（此时数据是最新的）
3. 在 CLI 执行业务查询期间，桌面端修改了同一预算并同步到服务器
4. CLI 返回查询结果

**时序**：
```
T0: CLI 进程 A (accounts list) 获取共享锁
T1: A 读取 state.json，TTL 有效，决定 skip 同步
T2: A 调用 api.loadBudget() 加载本地数据库
    ┌───────────────────────────────────────────────┐
    │  此时桌面端用户添加了一笔交易并同步到服务器    │
    │  服务器数据已更新，但 CLI 本地数据未同步      │
    └───────────────────────────────────────────────┘
T3: A 调用 api.getAccounts()，返回本地缓存的旧数据
T4: A 释放共享锁
```

**这不是锁的 bug，而是最终一致性系统的正常权衡**：
- 锁保证 CLI 进程间不互相干扰
- 但锁无法阻止其他客户端修改服务器数据
- TTL 窗口内的数据不一致是可接受的权衡

**影响**：
- ⚠️ CLI 返回的数据可能滞后于实际状态（最多 TTL 秒）
- ✅ 不影响数据正确性，只是可见性延迟

**回退路径**：
1. 需要强一致性时使用 `--refresh` 标志
2. 缩短 TTL：`--cache-ttl 5`（减少不一致窗口，但增加同步频率）
3. 关键操作前主动执行 `actual sync`

---

### B2：写命令基于过期数据修改

**前提条件**：
1. CLI 写命令获取独占锁
2. 写前同步拉取了服务器最新数据
3. 在 CLI 执行业务修改期间，其他客户端修改了同一数据
4. CLI 写后同步推送修改

**时序**：
```
T0: CLI 进程 A (transactions update) 获取独占锁
T1: A 执行写前同步，拉取最新数据 (余额 = $1000)
T2: A 加载预算，准备修改
    ┌───────────────────────────────────────────────┐
    │  桌面端用户转出 $200，服务器余额变为 $800      │
    │  CLI 本地余额仍为 $1000                       │
    └───────────────────────────────────────────────┘
T3: A 基于本地余额 $1000 转出 $300 → 本地余额 $700
T4: A 执行写后同步，推送修改
    ├─ CRDT 合并：服务器余额 $800 - $300 = $500
    └─ 最终结果正确，即使基于过期数据
```

**CRDT 拯救了这个场景**：
- Actual 使用 CRDT（无冲突复制数据类型）
- 即使基于过期数据修改，最终合并结果也是正确的
- 数值类操作（加减）通过数学合并而非覆盖

**影响**：
- ✅ 数据正确性由 CRDT 保证，无损坏风险
- ⚠️ 业务逻辑可能基于过期数据做出决策（如检查余额是否足够）

**回退路径**：
1. 关键业务逻辑在应用层进行双重检查
2. 缩短 TTL，减少数据过期概率
3. 接受最终一致性的权衡（大多数场景下可接受）

---

### B3：缓存未命中但数据已被修改

**前提条件**：
1. 缓存已过期（TTL 到期）
2. 只读命令触发同步
3. 在同步完成前，其他客户端修改了数据
4. 同步拉取到修改后的数据

**影响**：
- ✅ 同步会拉取最新数据，无不一致
- ❌ 但用户可能预期看到的是"命令启动时"的状态，而非"同步完成时"的状态

**回退路径**：
- 这是同步系统的正常行为，无需特殊处理
- 如需时间点快照，应在服务器端实现

---

## 5. 两类风险的本质区别总结

| 维度 | A 类：CLI 进程间竞争 | B 类：其他客户端一致性窗口 |
|------|---------------------|-------------------------|
| **参与方** | 多个 CLI 进程 | CLI + 桌面端/移动设备/其他 API |
| **协调机制** | 文件锁（读者-写者锁） | 同步服务器 + CRDT |
| **锁能否防止** | ✅ 部分可防止（互斥、排队） | ❌ 完全无法防止 |
| **数据正确性** | 锁保证不损坏 | CRDT 保证不损坏 |
| **可见性延迟** | 仅锁等待时间 | TTL + 同步延迟 |
| **回退手段** | 调整锁超时、串行化 | 调整 TTL、使用 --refresh |
| **是否为 bug** | 部分是（可优化） | 否（设计权衡） |

---

## 6. 校准后的关键结论

### 6.1 锁实际保证了什么
- ✅ **写写互斥**：同一时间只有一个写命令执行
- ✅ **读写互斥**：写命令执行时，新的读命令被阻塞；读命令执行时，写命令等待
- ✅ **读读并行**：多个读命令可同时执行
- ❌ **不保证**：读命令期间数据不被其他客户端修改
- ❌ **不保证**：读命令返回的数据是绝对最新的

### 6.2 缓存实际保证了什么
- ✅ **减少网络请求**：TTL 内复用本地数据
- ✅ **提高响应速度**：避免每次命令都同步
- ✅ **最终一致性**：过期后自动同步
- ❌ **不保证**：强一致性（任何时刻都返回最新数据）
- ❌ **不保证**：CLI 进程间缓存状态完全一致

### 6.3 安全使用建议

| 场景 | 建议配置 |
|------|---------|
| 脚本批量只读操作 | `--cache-ttl 3600`，先 `actual sync` 预热 |
| 关键写入操作 | 默认配置即可，CRDT 保证正确性 |
| 需要最新数据的只读操作 | `--refresh` 标志 |
| 单进程环境 | 可使用 `--no-lock` 减少锁开销 |
| 加密预算 | 接受每次都同步的事实，或考虑不加密 |
| 慢操作场景 | 延长 `--lock-timeout`，避免超时 |

---

## 7. 代码位置索引

| 功能 | 文件 | 行号 |
|------|------|------|
| 共享锁实现 | `packages/cli/src/lock.ts` | 129-149 |
| 独占锁实现 | `packages/cli/src/lock.ts` | 113-127 |
| Gate 锁获取 | `packages/cli/src/lock.ts` | 96-111 |
| Readers 等待 | `packages/cli/src/lock.ts` | 85-94 |
| 主连接流程 | `packages/cli/src/connection.ts` | 40-154 |
| 同步决策逻辑 | `packages/cli/src/cache.ts` | 88-106 |
| 原子缓存写入 | `packages/cli/src/cache.ts` | 56-71 |
| 下载预算内部同步 | `packages/loot-core/src/server/budgetfiles/app.ts` | 182-216 |
