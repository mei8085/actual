# CLI 锁机制对账版 - no-lock 前提边界分析 v5

> **本版新增**：默认加锁 vs no-lock 两条路径对比、结论适用前提速查表、无锁并发风险标注

---

## 1. no-lock 标志的实现与影响范围

### 1.1 no-lock 的作用点

```typescript
// connection.ts:79-87
if (!config.noLock) {
  release = mutates
    ? await acquireExclusive(meta, { timeoutMs: config.lockTimeout * 1000 })
    : await acquireShared(meta, { timeoutMs: config.lockTimeout * 1000 });
}
```

**核心事实**：`noLock: true` 时，**完全跳过锁获取逻辑**，`release` 始终为 `null`，后续 `finally` 块也不会执行任何锁释放操作。

### 1.2 受影响的代码路径

| 模块 | 加锁行为 | no-lock 行为 |
|------|---------|-------------|
| `withConnection()` | 根据 `mutates` 获取共享/独占锁 | 跳过锁获取，无锁保护 |
| `actual sync --clear` | 获取独占锁后删除缓存 | 直接删除缓存，无锁保护 |
| `actual sync --status` | 无锁（只读文件） | 无锁（相同行为） |
| `actual sync`（默认） | `mutates: true` → 独占锁 | 无锁保护 |

---

## 2. 四阶段时序对比：加锁 vs no-lock

### 2.1 只读命令 (`mutates: false`)

| 阶段 | 默认加锁路径 | no-lock 路径 | 差异 |
|------|------------|-------------|------|
| **1. 请求** | 调用 `acquireShared()` | 直接跳过 | ❌ 无锁请求 |
| **2. 等待** | 等待 gate 锁（如被持有） | 无等待 | ❌ 无等待阶段 |
| **3. 进入临界区** | 1. 获取 gate<br>2. 创建 reader 标记<br>3. 释放 gate | 直接进入 | ❌ 无 gate 保护<br>❌ 无 reader 标记 |
| **4. 释放** | 删除 reader 标记 | 无操作 | ❌ 无释放操作 |

### 2.2 写入命令 (`mutates: true`)

| 阶段 | 默认加锁路径 | no-lock 路径 | 差异 |
|------|------------|-------------|------|
| **1. 请求** | 调用 `acquireExclusive()` | 直接跳过 | ❌ 无锁请求 |
| **2. 等待** | 1. 等 gate 锁<br>2. 等 readers 清空 | 无等待 | ❌ 无等待阶段 |
| **3. 进入临界区** | 持续持有 gate 锁 | 直接进入 | ❌ 无 gate 保护 |
| **4. 释放** | 释放 gate 锁 | 无操作 | ❌ 无释放操作 |

### 2.3 `actual sync --clear` 命令

| 阶段 | 默认加锁路径 | no-lock 路径 | 差异 |
|------|------------|-------------|------|
| **1. 请求** | 调用 `acquireExclusive()` | 直接跳过 | ❌ 无锁请求 |
| **2. 等待** | 等 gate + 等 readers | 无等待 | ❌ 无等待阶段 |
| **3. 进入临界区** | 持 gate 锁执行 `rmSync(state.json)` | 直接删除文件 | ❌ 无互斥保护 |
| **4. 释放** | 释放 gate 锁 | 无操作 | ❌ 无释放操作 |

---

## 3. 同步行为对比

### 3.1 只读命令同步决策

| 场景 | 默认加锁 | no-lock | 差异 |
|------|---------|---------|------|
| TTL 内 | skip 同步，读本地缓存 | 相同 | ✅ 无差异 |
| TTL 过期 | 多进程可能同时同步 | 多进程一定同时同步 | ⚠️ 加锁时共享锁不阻止同时同步；no-lock 下风险相同 |
| `--refresh` | 强制同步 | 相同 | ✅ 无差异 |
| 加密预算 | 强制同步 | 相同 | ✅ 无差异 |

**关键洞察**：即使在加锁模式下，只读命令的共享锁也**不阻止**多个进程同时同步。no-lock 模式在这一点上没有增加新风险。

### 3.2 写入命令双同步

| 场景 | 默认加锁 | no-lock | 差异 |
|------|---------|---------|------|
| 写前同步 | 串行执行（独占锁保证） | 可能并行执行 | ⚠️ no-lock 下多个写命令可能同时同步和修改 |
| 业务逻辑执行 | 串行执行 | 可能并行执行 | ⚠️ 竞态风险 |
| 写后同步 | 串行执行 | 可能并行执行 | ⚠️ 竞态风险 |

---

## 4. 竞态风险对比：加锁 vs no-lock

### 4.1 风险矩阵

| 风险场景 | 默认加锁模式 | no-lock 模式 | 风险变化 |
|---------|------------|-------------|---------|
| **A1 多只读同时同步** | ✅ 可能发生（共享锁不阻止） | ✅ 可能发生 | ➖ 风险相同 |
| **A2 缓存写入竞争** | ✅ 原子重命名保证不损坏 | ✅ 原子重命名保证不损坏 | ➖ 风险相同 |
| **A3 崩溃锁残留** | ⚠️ 30 秒自动恢复 | ✅ 无此问题 | ➕ no-lock 更优 |
| **A4 锁获取超时** | ⚠️ 可能发生 | ✅ 无此问题 | ➕ no-lock 更优 |
| **A5 写命令排队** | ⚠️ 串行执行，可能超时 | ✅ 无排队，但有竞态 | ⚠️ 权衡 |
| **B1 其他客户端修改** | ⚠️ TTL 窗口不一致 | ⚠️ TTL 窗口不一致 | ➖ 风险相同 |
| **B2 写基于过期数据** | ✅ CRDT 保证正确 | ✅ CRDT 保证正确 | ➖ 风险相同 |
| **C1 多写同时修改** | ✅ 串行执行，安全 | ⚠️ 可能并发修改 | ❌ no-lock 新增风险 |
| **C2 缓存清除竞争** | ✅ 独占锁保证原子性 | ⚠️ 可能删除半写入文件 | ❌ no-lock 新增风险 |

---

## 5. 结论适用前提速查表

### 5.1 仅在加锁模式下成立的结论

| 结论 | 加锁模式 | no-lock 模式 | 备注 |
|------|---------|-------------|------|
| 写命令串行执行 | ✅ 成立 | ❌ 不成立 | no-lock 下多个写命令可能并行执行 |
| 读-写互斥 | ✅ 成立 | ❌ 不成立 | no-lock 下读写可同时执行 |
| `sync --clear` 原子性 | ✅ 成立 | ❌ 不成立 | no-lock 下可能删除半写入的 state.json |
| 锁等待超时错误 | ✅ 可能发生 | ❌ 永不发生 | no-lock 跳过锁获取 |
| 崩溃进程 30 秒锁残留 | ✅ 可能发生 | ❌ 永不发生 | no-lock 无锁可残留 |

### 5.2 两种模式下均成立的结论

| 结论 | 加锁模式 | no-lock 模式 | 备注 |
|------|---------|-------------|------|
| 多个只读命令可同时同步 | ✅ 成立 | ✅ 成立 | 共享锁不阻止同步并行 |
| 缓存原子写入不损坏 | ✅ 成立 | ✅ 成立 | 原子重命名与锁无关 |
| 其他客户端修改导致 TTL 不一致 | ✅ 成立 | ✅ 成立 | 锁不保护跨客户端一致性 |
| CRDT 保证并发修改正确性 | ✅ 成立 | ✅ 成立 | CRDT 在服务器端合并 |
| TTL 过期后自动同步 | ✅ 成立 | ✅ 成立 | 同步决策与锁无关 |
| `--refresh` 强制同步 | ✅ 成立 | ✅ 成立 | 同步决策与锁无关 |

### 5.3 no-lock 模式下新增的风险

| 风险 | 触发条件 | 可能后果 | 缓解措施 |
|------|---------|---------|---------|
| **多写并发修改** | 多个写命令同时执行 | 业务逻辑基于过期数据决策 | 脚本串行化、避免并行触发 |
| **缓存清除竞争** | `sync --clear` 与写入同时执行 | 可能删除半写入的 state.json | 避免在脚本运行时手动清缓存 |
| **读写同时执行** | 读命令和写命令同时执行 | 读命令可能读到部分修改的中间状态 | 关键操作使用加锁模式 |

---

## 6. 最小化时序图：加锁 vs no-lock 对比

### 场景：两个写命令同时触发

**默认加锁模式**：
```
进程 A (写入)                进程 B (写入)
    │                            │
    ├─ acquireExclusive        ├─ acquireExclusive
    │  获取 gate ✓              │  等 gate...
    │  等 readers 清空           │  等 gate...
    │  进入临界区               │  等 gate...
    │  写前同步                 │  等 gate...
    │  执行业务                 │  等 gate...
    │  写后同步                 │  等 gate...
    │  释放 gate                │  获取 gate ✓
    ▼                            │  等 readers 清空
                                 │  进入临界区
                                 │  写前同步
                                 │  执行业务
                                 │  写后同步
                                 │  释放 gate
                                 ▼
```
✅ **安全**：串行执行，无竞态。

---

**no-lock 模式**：
```
进程 A (写入)                进程 B (写入)
    │                            │
    │  跳过锁获取                │  跳过锁获取
    │  进入临界区                │  进入临界区
    │  写前同步                 │  写前同步
    │  执行业务                 │  执行业务
    │  写后同步                 │  写后同步
    ▼                            ▼
```
⚠️ **风险**：并行执行，可能基于过期数据修改。

---

### 场景：读命令和写命令同时触发

**默认加锁模式**：
```
进程 A (只读)                进程 B (写入)
    │                            │
    ├─ acquireShared           ├─ acquireExclusive
    │  获取 gate ✓              │  等 gate...
    │  创建 reader-A            │  等 gate...
    │  释放 gate                │  获取 gate ✓
    │  执行业务                 │  等 readers 清空...
    │  删除 reader-A            │  等 readers 清空...
    ▼                            │  进入临界区
                                 │  执行业务
                                 ▼
```
✅ **安全**：读写互斥，写命令等待读完成。

---

**no-lock 模式**：
```
进程 A (只读)                进程 B (写入)
    │                            │
    │  跳过锁获取                │  跳过锁获取
    │  进入临界区                │  进入临界区
    │  加载本地数据              │  写前同步
    │  执行业务                 │  执行业务
    │                            │  写后同步
    ▼                            ▼
```
⚠️ **风险**：读命令可能读到修改前、修改中或修改后的数据，结果不确定。

---

## 7. no-lock 适用场景速查表

| 场景 | 推荐使用 no-lock | 原因 |
|------|-----------------|------|
| 单进程脚本，串行执行命令 | ✅ 推荐 | 无并发风险，跳过锁开销 |
| 只读命令密集，无写入操作 | ✅ 推荐 | 加锁不阻止多只读同时同步，no-lock 无额外风险 |
| 崩溃锁残留问题严重 | ✅ 推荐 | no-lock 无锁可残留 |
| 多进程并行执行写命令 | ❌ 不推荐 | 竞态风险高 |
| 关键业务写入（如转账） | ❌ 不推荐 | 需要串行保证 |
| 脚本中混合读写操作 | ❌ 不推荐 | 读写可能同时执行，结果不确定 |
| 性能测试，排除锁开销影响 | ✅ 推荐 | 测量真实同步和业务逻辑耗时 |

---

## 8. 阻塞类型与 no-lock 关系

| 阻塞现象 | 加锁模式 | no-lock 模式 |
|---------|---------|-------------|
| 共享锁在 acquireGate 等待 | ✅ 可能发生 | ❌ 永不发生 |
| 独占锁在 acquireGate 等待 | ✅ 可能发生 | ❌ 永不发生 |
| 独占锁在 waitForReadersEmpty 等待 | ✅ 可能发生 | ❌ 永不发生 |
| 同步等待网络响应 | ✅ 可能发生 | ✅ 仍可能发生 |
| API 调用等待服务器响应 | ✅ 可能发生 | ✅ 仍可能发生 |

---

## 9. 代码位置索引

| 功能 | 文件 | 行号 |
|------|------|------|
| no-lock 标志检查 (connection) | `packages/cli/src/connection.ts` | 79-87 |
| no-lock 标志检查 (sync --clear) | `packages/cli/src/commands/sync.ts` | 84-88 |
| no-lock 配置解析 | `packages/cli/src/config.ts` | 215-220 |
| 共享锁实现 | `packages/cli/src/lock.ts` | 129-149 |
| 独占锁实现 | `packages/cli/src/lock.ts` | 113-127 |
| acquireGate | `packages/cli/src/lock.ts` | 96-111 |
