# Actual Budget 弹窗栈与焦点恢复机制分析报告

## 一、概述

Actual Budget 项目实现了一套完整的多层弹窗管理机制，支持弹窗叠加打开与逐层关闭，并能在关闭后准确将键盘焦点恢复到上一层的触发元素。这套机制由两大部分组成：

1. **弹窗栈管理**：基于 Redux Toolkit 的数组数据结构维护弹窗的叠加次序
2. **焦点恢复机制**：结合 React Aria 内置焦点管理与自定义 `InitialFocus` 组件实现

---

## 二、弹窗栈管理机制

### 2.1 数据结构

弹窗栈的核心状态定义在 [modalsSlice.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/74-actual/packages/desktop-client/src/modals/modalsSlice.ts#L691-L699)：

```typescript
type ModalsState = {
  modalStack: Modal[];  // 弹窗栈数组，按打开顺序存储
  isHidden: boolean;    // 弹窗是否隐藏（加载状态时使用）
};

const initialState: ModalsState = {
  modalStack: [],
  isHidden: false,
};
```

**关键特性**：
- `modalStack` 是一个**普通数组**，索引 `0` 为最底层弹窗，索引 `length-1` 为最顶层弹窗
- 每个元素是一个 discriminated union 类型 `Modal`，通过 `name` 字段区分弹窗类型

### 2.2 核心操作方法

弹窗栈提供 5 个核心操作（[modalsSlice.ts#L716-L748](file:///d:/fz/0601-1/solo-dogfeeding/code/74-actual/packages/desktop-client/src/modals/modalsSlice.ts#L716-L748)）：

| 方法 | 功能 | 代码实现 |
|------|------|----------|
| `pushModal` | 新弹窗推入栈顶 | `state.modalStack = [...state.modalStack, modal]` |
| `popModal` | 移除栈顶弹窗 | `state.modalStack = state.modalStack.slice(0, -1)` |
| `closeModal` | 清空整个栈 | `state.modalStack = []` |
| `replaceModal` | 替换整个栈 | `state.modalStack = [modal]` |
| `collapseModals` | 折叠到指定弹窗 | 从栈顶移除直到找到目标弹窗 |

**pushModal 特殊处理**：
```typescript
if (modal.name.endsWith('keyboard-shortcuts') && state.modalStack.length > 0) {
  return state;  // 已有弹窗打开时，不显示快捷键帮助
}
```

### 2.3 渲染机制

弹窗渲染由 [Modals.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/74-actual/packages/desktop-client/src/components/Modals.tsx) 组件负责：

```typescript
const modals = modalStack
  .map((modal, idx) => {
    const { name } = modal;
    const key = `${name}-${idx}`;  // 用索引确保唯一性
    switch (name) {
      case 'confirm-delete':
        return <ConfirmDeleteModal key={key} {...modal.options} />;
      // ... 约 70 种弹窗类型
      default:
        throw new Error('Unknown modal');
    }
  });
```

**渲染规则**：
- 遍历 `modalStack` 数组，**按数组顺序依次渲染**
- 使用 `name-index` 作为 React 的 `key`，确保组件正确复用
- 数组末尾的弹窗（栈顶）最后渲染，自然位于 DOM 树的最上层

### 2.4 叠加次序维护

叠加次序通过三层机制保证：

#### 1. DOM 渲染顺序（自然层叠）
数组索引 `idx` 越大，渲染越靠后。在相同 z-index 情况下，DOM 中后出现的元素会覆盖先出现的元素。

#### 2. 统一 z-index
在 [Modal.tsx#L35](file:///d:/fz/0601-1/solo-dogfeeding/code/74-actual/packages/desktop-client/src/components/common/Modal.tsx#L35) 定义：
```typescript
export const MODAL_Z_INDEX = 3000;
```
所有弹窗使用相同的 z-index，依靠 DOM 顺序决定覆盖关系。

#### 3. 视觉反馈（isActive 状态）
在 [Modal.tsx#L187-L258](file:///d:/fz/0601-1/solo-dogfeeding/code/74-actual/packages/desktop-client/src/components/common/Modal.tsx#L187-L258) 的 `ModalContentContainer` 中：
```typescript
if (isActive) {
  contentRef.current.style.transform = 'none';
  contentRef.current.style.pointerEvents = 'auto';
} else {
  contentRef.current.style.transform = 
    `translateY(-40px) scale(.95) rotate(${rotateFactor.current}deg)`;
  contentRef.current.style.pointerEvents = 'none';
}
```

- 顶层弹窗（`isActive=true`）：正常显示，可交互
- 下层弹窗（`isActive=false`）：缩小并上移，不可点击

**isActive 判断逻辑**（[useModalState.ts#L25-L34](file:///d:/fz/0601-1/solo-dogfeeding/code/74-actual/packages/desktop-client/src/hooks/useModalState.ts#L25-L34)）：
```typescript
const lastModal = modalStack[modalStack.length - 1];
const isActive = useCallback((name: string) => {
  return !lastModal || name === lastModal.name;
}, [lastModal]);
```

---

## 三、焦点恢复机制

焦点恢复由 **React Aria 内置焦点管理** 和 **自定义 InitialFocus 组件** 协同完成。

### 3.1 React Aria 内置焦点管理

Actual Budget 使用 `react-aria-components` 库的 `ModalOverlay` 和 `Modal` 组件（[Modal.tsx#L8-L12](file:///d:/fz/0601-1/solo-dogfeeding/code/74-actual/packages/desktop-client/src/components/common/Modal.tsx#L8-L12)）：

```typescript
import {
  Dialog,
  Modal as ReactAriaModal,
  ModalOverlay as ReactAriaModalOverlay,
} from 'react-aria-components';
```

**React Aria Modal 自动处理的焦点逻辑**：

1. **打开弹窗时**：
   - 自动保存当前 `document.activeElement`（触发弹窗的元素）
   - 将焦点移入弹窗内的第一个可聚焦元素

2. **关闭弹窗时**：
   - 自动将焦点恢复到之前保存的触发元素
   - 这是 WAI-ARIA 模态对话框规范的标准行为

3. **焦点陷阱**：
   - Tab 键在弹窗内循环，不会跳出到后面的页面元素

**关键配置**（[Modal.tsx#L77-L82](file:///d:/fz/0601-1/solo-dogfeeding/code/74-actual/packages/desktop-client/src/components/common/Modal.tsx#L77-L82)）：
```typescript
<ReactAriaModalOverlay
  isDismissable    // 允许点击遮罩层或按 ESC 关闭
  defaultOpen      // 默认打开（由 modalStack 控制存在性）
  onOpenChange={isOpen => !isOpen && handleOnClose?.()}
>
```

### 3.2 InitialFocus 组件

自定义组件 [InitialFocus.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/74-actual/packages/component-library/src/InitialFocus.ts) 用于指定弹窗内的初始焦点元素：

```typescript
export function InitialFocus<T extends HTMLElement = HTMLElement>({ children }) {
  const ref = useRef<T | null>(null);

  useEffect(() => {
    if (ref.current) {
      setTimeout(() => {
        if (ref.current) {
          ref.current.focus();
          // 输入框自动选中内容
          if (ref.current instanceof HTMLInputElement ||
              ref.current instanceof HTMLTextAreaElement) {
            ref.current.setSelectionRange(0, 10000);
          }
        }
      }, 0);  // setTimeout 确保 DOM 完全渲染后执行
    }
  }, []);

  // 克隆子元素并注入 ref
  if (typeof children === 'function') {
    return children(ref);
  }
  return cloneElement(Children.only(children), { ref });
}
```

**使用示例**（[ConfirmDeleteModal.tsx#L56-L67](file:///d:/fz/0601-1/solo-dogfeeding/code/74-actual/packages/desktop-client/src/components/modals/ConfirmDeleteModal.tsx#L56-L67)）：
```tsx
<InitialFocus>
  <Button variant="primary" onPress={() => { onConfirm(); state.close(); }}>
    <Trans>Delete</Trans>
  </Button>
</InitialFocus>
```

### 3.3 完整焦点流转示例

假设场景：页面 → 弹窗 A（"删除确认"）→ 弹窗 B（"确认密码"）

```
┌───────────────────────────────────────────────────────────┐
│  阶段 1：用户点击页面上的"删除账户"按钮                      │
└───────────────────────────────────────────────────────────┘
         │
         ▼  document.activeElement = <button id="btn-delete">
         │
┌───────────────────────────────────────────────────────────┐
│  阶段 2：pushModal('close-account') → 渲染弹窗 A           │
└───────────────────────────────────────────────────────────┘
         │
         ▼  1. React Aria 保存 btn-delete 为"触发元素"
         ▼  2. InitialFocus 将焦点设到弹窗 A 的"确认"按钮
         ▼  document.activeElement = <button id="btn-confirm-a">
         │
┌───────────────────────────────────────────────────────────┐
│  阶段 3：用户点击"确认"，需要验证密码                        │
└───────────────────────────────────────────────────────────┘
         │
         ▼  pushModal('confirm-password') → 渲染弹窗 B
         │
         ▼  1. React Aria 保存 btn-confirm-a 为"触发元素"
         ▼  2. InitialFocus 将焦点设到弹窗 B 的密码输入框
         ▼  document.activeElement = <input id="input-password">
         │
┌───────────────────────────────────────────────────────────┐
│  阶段 4：用户输入密码后关闭弹窗 B                           │
└───────────────────────────────────────────────────────────┘
         │
         ▼  popModal() → 弹窗 B 卸载
         ▼  React Aria 自动恢复焦点到 btn-confirm-a
         ▼  document.activeElement = <button id="btn-confirm-a">
         │
┌───────────────────────────────────────────────────────────┐
│  阶段 5：用户关闭弹窗 A                                    │
└───────────────────────────────────────────────────────────┘
         │
         ▼  popModal() → 弹窗 A 卸载
         ▼  React Aria 自动恢复焦点到 btn-delete
         ▼  document.activeElement = <button id="btn-delete">
```

### 3.4 焦点栈的维护

**关键点**：每个弹窗独立维护自己的"触发元素"引用。

- 弹窗 A 的 `ModalOverlay` 在挂载时保存 `btn-delete`
- 弹窗 B 的 `ModalOverlay` 在挂载时保存 `btn-confirm-a`
- 弹窗 B 卸载时，只恢复自己保存的 `btn-confirm-a`
- 弹窗 A 卸载时，只恢复自己保存的 `btn-delete`

这形成了一个**隐式的焦点栈**，与弹窗栈一一对应。

---

## 四、完整代码时序示例

### 4.1 打开弹窗 A
```typescript
// 1. 用户点击按钮触发
dispatch(pushModal({
  modal: { name: 'close-account', options: { account } }
}));

// 2. Redux 更新 modalStack: [] → [modalA]

// 3. Modals.tsx 渲染弹窗 A
<Modal name="close-account">
  {/* 内部使用 ReactAriaModalOverlay */}
</Modal>

// 4. React Aria 自动保存触发元素
// 5. InitialFocus 将焦点设到弹窗 A 的主按钮
```

### 4.2 打开弹窗 B
```typescript
// 1. 在弹窗 A 中触发
dispatch(pushModal({
  modal: { name: 'confirm-delete', options: { message, onConfirm } }
}));

// 2. Redux 更新 modalStack: [modalA] → [modalA, modalB]

// 3. Modals.tsx 渲染两个弹窗
//    - 先渲染 modalA（isActive=false）
//    - 后渲染 modalB（isActive=true）

// 4. 弹窗 B 的 React Aria 保存当前焦点（弹窗 A 的按钮）
// 5. 弹窗 B 的 InitialFocus 将焦点设到"删除"按钮
```

### 4.3 关闭弹窗 B
```typescript
// 1. 用户点击"删除"或按 ESC
dispatch(popModal());

// 2. Redux 更新 modalStack: [modalA, modalB] → [modalA]

// 3. Modals.tsx 只渲染弹窗 A（isActive=true）

// 4. React Aria 自动将焦点恢复到弹窗 A 的按钮
// 5. 弹窗 A 从缩小状态恢复为正常状态
```

### 4.4 关闭弹窗 A
```typescript
// 1. 用户点击"取消"或按 ESC
dispatch(popModal());

// 2. Redux 更新 modalStack: [modalA] → []

// 3. Modals.tsx 不渲染任何弹窗

// 4. React Aria 自动将焦点恢复到最初的页面按钮
```

---

## 五、设计亮点与权衡

### 5.1 设计亮点

| 设计决策 | 优点 |
|---------|------|
| 用普通数组作为栈结构 | 简单直观，易于理解和调试 |
| React Aria 接管焦点管理 | 符合 WAI-ARIA 标准，无需手动管理焦点栈 |
| InitialFocus 组件 | 灵活控制弹窗内初始焦点位置 |
| isActive 视觉状态 | 多层弹窗时用户有清晰的层级感知 |
| 数组索引决定渲染顺序 | 天然保证 DOM 层叠顺序，无需动态计算 z-index |

### 5.2 潜在问题

1. **z-index 固定为 3000**：如果页面其他元素使用了更高的 z-index，弹窗可能被覆盖
2. **无显式焦点栈**：完全依赖 React Aria 内部实现，调试时不易观察焦点链
3. **setTimeout(0)  hack**：InitialFocus 使用 setTimeout 规避渲染时序问题，不够优雅

### 5.3 代码优化建议

**问题**：InitialFocus 中的 `setTimeout(0)` 可以改进

```typescript
// 原代码
setTimeout(() => {
  if (ref.current) {
    ref.current.focus();
  }
}, 0);

// 优化建议：使用 requestAnimationFrame 或 useLayoutEffect
useLayoutEffect(() => {
  if (ref.current) {
    // 同步执行，避免闪烁
    ref.current.focus();
  }
}, []);
```

---

## 六、关键文件索引

| 文件 | 作用 |
|------|------|
| [modalsSlice.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/74-actual/packages/desktop-client/src/modals/modalsSlice.ts) | 弹窗栈状态管理与核心操作 |
| [Modals.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/74-actual/packages/desktop-client/src/components/Modals.tsx) | 弹窗栈渲染入口 |
| [Modal.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/74-actual/packages/desktop-client/src/components/common/Modal.tsx) | 弹窗底层组件（基于 React Aria） |
| [useModalState.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/74-actual/packages/desktop-client/src/hooks/useModalState.ts) | 弹窗状态 Hook |
| [InitialFocus.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/74-actual/packages/component-library/src/InitialFocus.ts) | 初始焦点设置组件 |

---

## 七、总结

Actual Budget 的弹窗管理采用了**"Redux 数组栈 + React Aria 焦点管理"**的分层架构：

1. **弹窗栈**：用简单的数组结构维护弹窗次序，通过 `push/pop` 操作实现叠加打开与逐层关闭
2. **叠加次序**：靠数组索引决定 DOM 渲染顺序，后渲染的弹窗自然覆盖先渲染的
3. **焦点恢复**：每个弹窗的 React Aria `ModalOverlay` 独立保存自己的触发元素，关闭时自动恢复，形成隐式的焦点栈
4. **初始焦点**：通过 `InitialFocus` 组件在弹窗内灵活指定首个获得焦点的元素

这种设计既利用了成熟库（React Aria）的健壮实现，又保持了应用层状态管理的简单清晰，是生产级应用中值得参考的实现方案。
