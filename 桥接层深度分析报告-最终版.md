# Actual Budget 桥接层深度分析报告（最终证据对齐版）

## 1. 架构总览

Actual Budget 的桥接层是连接桌面端 React UI 与核心业务逻辑的关键中间层，负责**请求序列化**、**权限边界控制**和**异步响应回调**三大核心职责。

---

## 2. Mutator 机制深度解析

### 2.1 Mutator 核心代码实现

**文件**: `loot-core/src/server/mutators.ts`

```typescript
// 标记处理器为变异操作
const mutatingMethods = new WeakMap();

export function mutator<T extends HandlerFunctions>(handler: T): T {
  mutatingMethods.set(handler, true);
  return handler;
}

// 核心执行逻辑
export async function runHandler<T extends Handlers[keyof Handlers]>(
  handler: T,
  args?: Parameters<T>[0],
  { undoTag, name }: { undoTag?; name? } = {},
): Promise<ReturnType<T>> {
  if (mutatingMethods.has(handler)) {
    // Mutator 方法通过 runMutator 执行
    return runMutator(() => handler(args), { undoTag }) as Promise<
      ReturnType<T>
    >;
  }

  // 非 Mutator 方法直接执行
  const promise = handler(args);
  runningMethods.add(promise);
  void promise.then(() => {
    runningMethods.delete(promise);
  });
  return promise as Promise<ReturnType<T>>;
}

// runMutator 被 sequential 包装，确保串行执行
export const runMutator = sequential(_runMutator) as typeof _runMutator;

function _runMutator<T extends () => Promise<unknown>>(
  func: T,
  initialContext = {},
): Promise<Awaited<ReturnType<T>>> {
  currentContext = initialContext;  // 设置 Undo 上下文
  return func().finally(() => {
    currentContext = null;  // 清理上下文
  }) as Promise<Awaited<ReturnType<T>>>;
}
```

### 2.2 sequential 队列机制实现

**文件**: `loot-core/src/shared/async.ts`

```typescript
export function sequential<T extends AnyFunction>(
  fn: T,
): (...args: Parameters<T>) => Promise<Awaited<ReturnType<T>>> {
  const sequenceState: {
    running: Promise<Awaited<ReturnType<T>>> | null;
    queue: Array<{ args; resolve; reject }>;
  } = { running: null, queue: [] };

  function pump() {
    const next = sequenceState.queue.shift();
    if (next !== undefined) {
      run(next.args, next.resolve, next.reject);
    } else {
      sequenceState.running = null;
    }
  }

  function run(args, resolve, reject) {
    sequenceState.running = fn.apply(null, args).then(
      val => { pump(); resolve(val); },
      err => { pump(); reject(err); },
    );
  }

  return ((...args) => {
    if (!sequenceState.running) {
      return new Promise((resolve, reject) => run(args, resolve, reject));
    } else {
      return new Promise((resolve, reject) => {
        sequenceState.queue.push({ resolve, reject, args });
      });
    }
  }) as T;
}
```

### 2.3 Mutator 的真实作用

| 特性 | 代码证据 | 说明 |
|------|----------|------|
| ✅ **顺序执行** | `runMutator = sequential(_runMutator)` | 所有标记为 mutator 的方法共享一个执行队列，串行执行，避免竞态条件 |
| ✅ **Undo 上下文** | `currentContext = initialContext` | 在 mutator 执行期间设置全局上下文，供 undo 系统追踪操作记录 |
| ❌ **数据库事务回滚** | **无相关代码** | mutator 机制本身不包含任何数据库 BEGIN/COMMIT/ROLLBACK 逻辑，不等于事务回滚 |

---

## 3. 交易服务桥接分析

### 3.1 方法注册与 Mutator 标记

**文件**: `loot-core/src/server/transactions/app.ts`

| 方法名 | Mutator 标记 | Undo 包装 | 操作类型 | 风险控制说明 |
|--------|-------------|-----------|----------|--------------|
| `transactions-batch-update` | ✅ `mutator(undoable(...))` | ✅ | 批量增删改 | mutator 串行执行 + undo 追踪 |
| `transactions-merge` | ✅ `mutator(undoable(...))` | ✅ | 合并交易 | mutator 串行执行 + undo 追踪 |
| `transaction-add` | ✅ `mutator(...)` | ❌ | 单条添加 | mutator 串行执行 |
| `transaction-update` | ✅ `mutator(...)` | ❌ | 单条更新 | mutator 串行执行 |
| `transaction-delete` | ✅ `mutator(...)` | ❌ | 单条删除 | mutator 串行执行 |
| `transaction-move` | ✅ `mutator(undoable(...))` | ✅ | 排序移动 | mutator 串行执行 + undo 追踪 + 输入验证 |
| `transactions-parse-file` | ✅ `mutator(...)` | ❌ | 文件解析 | mutator 串行执行 |
| `transactions-export` | ✅ `mutator(...)` | ❌ | 导出 CSV | mutator 串行执行 |
| `transactions-export-query` | ✅ `mutator(...)` | ❌ | 按查询导出 | mutator 串行执行 |
| `get-earliest-transaction` | ❌ | ❌ | 只读查询 | 直接执行 |
| `get-latest-transaction` | ❌ | ❌ | 只读查询 | 直接执行 |

### 3.2 输入验证代码证据

```typescript
// transaction-move 输入验证
async function moveTransaction({ id, accountId, targetId }) {
  const transaction = await db.getTransaction(id);
  if (!transaction) throw new Error(`Transaction not found: ${id}`);
  if (transaction.account !== accountId)
    throw new Error(`Account mismatch: expected ${transaction.account}, got ${accountId}`);
  await db.moveTransaction(id, accountId, targetId);
  return {};
}
```

---

## 4. 预算文件模块桥接分析（纯代码证据）

### 4.1 方法注册与风险控制依据

**文件**: `loot-core/src/server/budgetfiles/app.ts`

| 方法名 | Mutator 标记 | 写操作 | 直接代码证据的风险控制依据 |
|--------|-------------|--------|----------------------------|
| `validate-budget-name` | ❌ | ❌ | 纯函数验证，无副作用 |
| `unique-budget-name` | ❌ | ❌ | 纯函数生成，无副作用 |
| `get-budgets` | ❌ | ❌ | fs.getDocumentDir() 沙箱 + try-catch 包裹 |
| `get-remote-files` | ❌ | ❌ | cloudStorage.listRemoteFiles() 内部错误捕获 |
| `reset-budget-cache` | ✅ `mutator(...)` | ✅ | mutator 串行执行 |
| `upload-budget` | ❌ | ✅ | 1. try-catch 包裹 2. finally 块 unloadPrefs |
| `download-budget` | ❌ | ✅ | 1. try-catch 包裹 2. FileDownloadError 特殊处理 |
| `sync-budget` | ❌ | ✅ | 无显式 try-catch，依赖 CRDT 内部机制 |
| `load-budget` | ❌ | ✅ | 1. 已有预算先 close-budget 2. 调用 _loadBudget |
| `_loadBudget` | ❌ | ✅ | 1. try-catch 包裹 2. catch 中调用 closeBudget 清理 |
| `create-demo-budget` | ❌ | ✅ | 复用 create-budget 逻辑 |
| `close-budget` | ❌ | ✅ | 1. sheet.waitOnSpreadsheet() 等待电子表格 2. try-catch 包裹 asyncStorage |
| `delete-budget` | ❌ | ✅ | 1. cloudStorage.removeFile 带空 catch 2. try-catch 包裹本地删除 |
| `duplicate-budget` | ❌ | ✅ | 1. validateBudgetName 校验 2. try-catch 包裹 3. catch 中清理新创建目录 |
| `create-budget` | ❌ | ✅ | 1. try-catch 包裹 2. DEMO_BUDGET_ID 特殊处理 |
| `import-budget` | ❌ | ✅ | try-catch 包裹 |
| `export-budget` | ❌ | ✅ | try-catch 包裹 |
| `upload-file-web` | ❌ | ✅ | 仅浏览器端执行，返回空对象 / null |
| `backups-get` | ❌ | ❌ | 无风险控制代码 |
| `backup-load` | ❌ | ✅ | 无显式 try-catch |
| `backup-make` | ❌ | ✅ | 无显式 try-catch |
| `get-last-opened-backup` | ❌ | ❌ | 无风险控制代码 |

### 4.2 关键风险控制代码证据

#### ✅ 证据 1: close-budget flushRunningMethods

```typescript
// mutators.ts - 仅对 close-budget 方法特殊处理
if (name === 'close-budget') {
  await flushRunningMethods();
}

async function flushRunningMethods() {
  await wait(200);
  while (runningMethods.size > 0) {
    await Promise.all([...runningMethods.values()]);
    await wait(100);
  }
}
```

#### ✅ 证据 2: loadBudget try-catch + closeBudget 清理

```typescript
// budgetfiles/app.ts
async function _loadBudget(id: Budget['id']) {
  try {
    await prefs.loadPrefs(id);
    await db.openDatabase(id);
    await updateVersion();
    await sheet.loadSpreadsheet(db, onSheetChange);
    // ... 其他初始化
  } catch (e) {
    await closeBudget();  // 失败时调用 closeBudget 清理
    return { error: 'opening-budget' };
  }
}
```

#### ✅ 证据 3: deleteBudget try-catch

```typescript
if (id) {
  try {
    await db.openDatabase(id);
    db.closeDatabase();
    const budgetDir = fs.getBudgetDir(id);
    await fs.removeDirRecursively(budgetDir);
  } catch {
    return 'fail';
  }
}
```

#### ✅ 证据 4: duplicateBudget 失败清理

```typescript
try {
  const newId = await idFromBudgetName(newName);
  const newBudgetDir = fs.getBudgetDir(newId);
  await fs.mkdir(newBudgetDir);
  // ... 复制文件
} catch (error) {
  try {
    const newBudgetDir = fs.getBudgetDir(newId);
    if (await fs.exists(newBudgetDir)) {
      await fs.removeDirRecursively(newBudgetDir);
    }
  } catch {}  // 忽略清理错误
  throw new Error(`Failed to duplicate budget: ${error.message}`);
}
```

#### ✅ 证据 5: uploadBudget finally 清理

```typescript
try {
  await cloudStorage.upload();
} catch (e) {
  // ... 错误处理
} finally {
  if (id) {
    prefs.unloadPrefs();
  }
}
```

---

## 5. 自定义同步映射桥接分析（读写双链路）

### 5.1 Preferences 服务方法注册

**文件**: `loot-core/src/server/preferences/app.ts`

```typescript
// 写操作：保存首选项（标记为 mutator + undoable）
app.method('preferences/save', mutator(undoable(saveSyncedPrefs)));

// 读操作：获取所有首选项（只读）
app.method('preferences/get', getSyncedPrefs);
```

---

### 链路 A: 读链路 (preferences/get)

**触发时机**: 预算加载时初始化

#### 步骤 A1-A9 完整时序

| 步骤 | 描述 | 代码位置 | 关键代码 |
|------|------|----------|---------|
| A1 | loadPrefs thunk 发起并行请求 | `prefsSlice.ts:48-55` | `Promise.all([send('load-global-prefs'), send('preferences/get')])` |
| A2 | 客户端连接层生成 UUID + 注册回调 | `connection/index.electron.ts:send` | `id = uuidv4(); replyHandlers.set(id, {resolve, reject})` |
| A3 | SharedWorker 路由到 Leader | `shared-browser-server-core.ts` | `targetGroup.requestToPort.set(msg.id, port)` |
| A4 | Leader Worker 转发到后端 | `shared-browser-server-core.ts` | `leaderPort.postMessage({type: '__to-worker', msg})` |
| A5 | 服务端执行查询（非 mutator，直接执行） | `connection/index.electron.ts` | `SELECT id, value FROM preferences` |
| A6 | 服务端返回响应 (mutated: false) | `connection/index.electron.ts` | `process.parentPort.postMessage({type: 'reply', id, result, mutated: false})` |
| A7 | SharedWorker 路由回源标签页 | `shared-browser-server-core.ts` | `targetPort = group.requestToPort.get(workerMsg.id)` |
| A8 | 客户端触发 resolve + 清理 undoTag | `connection/index.electron.ts` | `undo.gc(msg.undoTag); handler.resolve(msg.result)` |
| A9 | dispatch(setPrefs) 更新 Redux | `prefsSlice.ts` | `dispatch(setPrefs({local, global, synced}))` |

---

### 链路 B: 写链路 (preferences/save)

**触发时机**: 用户修改银行同步设置并保存

#### 步骤 B1-B10 完整时序

| 步骤 | 描述 | 代码位置 | 关键代码 |
|------|------|----------|---------|
| B1 | UI 调用 saveSettings | `EditSyncAccount.tsx` | `saveSettings(); close()` |
| B2 | useBankSyncAccountSettings 调用 setSavedMappings | `useBankSyncAccountSettings.ts` | `setSavedMappings(mappingsToString(mappings))` |
| B3 | useSyncedPref 分发 saveSyncedPrefs thunk | `useSyncedPref.ts` | `dispatch(saveSyncedPrefs({prefs: {[prefName]: value}}))` |
| B4 | saveSyncedPrefs thunk await Promise.all send | `prefsSlice.ts:120-132` | **`await Promise.all(Object.entries(prefs).map(send))`** |
| B5 | 客户端连接层生成 UUID + 注册回调 | `connection/index.electron.ts` | `id = uuidv4(); replyHandlers.set(id, {resolve, reject})` |
| B6 | SharedWorker 路由到 Leader | `shared-browser-server-core.ts` | `targetGroup.requestToPort.set(msg.id, port)` |
| B7 | 服务端进入 Mutator 队列串行执行 | `connection/index.electron.ts` | `mutatingMethods.has(handler) === true` |
| B8 | 执行 UPDATE 后返回响应 (mutated: true) | `connection/index.electron.ts` | `process.parentPort.postMessage({..., mutated: true})` |
| B9 | SharedWorker 路由回源 + 客户端 resolve | 同上 | `handler.resolve(msg.result)` |
| B10 | **await send 完成后 dispatch mergeSyncedPrefs** | `prefsSlice.ts:131` | **所有请求成功后才更新本地状态** |

> **关键证据修正**: saveSyncedPrefs 是**等待所有 send 请求成功后才更新本地 Redux 状态**，不是"立即本地更新失败靠 undo 回滚"模式。

---

### 5.2 读写双链路可视化

```
读链路 (preferences/get):
┌───────────────────────────────────────────────────────────────┐
│ 预算加载 → Promise.all(send) → UUID/回调 → SharedWorker路由   │
│    ↓                                                           │
│  服务端查询SELECT → 返回(mutated:false) → 路由回源 → resolve   │
│    ↓                                                           │
│  dispatch(setPrefs) → Redux更新 → useSyncedPref读取            │
└───────────────────────────────────────────────────────────────┘

写链路 (preferences/save):
┌───────────────────────────────────────────────────────────────┐
│ 用户保存 → setSavedMappings → dispatch saveSyncedPrefs         │
│    ↓                                                           │
│  → await Promise.all(send) → UUID/回调 → SharedWorker路由      │
│    ↓                                                           │
│  服务端Mutator队列 → UPDATE → 返回(mutated:true) → resolve      │
│    ↓                                                           │
│  ✅所有send成功后 → dispatch(mergeSyncedPrefs)                 │
└───────────────────────────────────────────────────────────────┘
```

---

## 6. 桥接层权限边界总结矩阵

| 维度 | 说明 | 代码证据位置 |
|------|------|--------------|
| **执行隔离** | Mutator 串行队列 vs 非 mutator 并行执行 | `mutators.ts: sequential(_runMutator)` |
| **Undo 上下文** | 仅 Mutator 执行期设置 currentContext | `mutators.ts: getMutatorContext()` |
| **文件沙箱** | 预算目录基于 getDocumentDir() | `fs.getBudgetDir(id)` 内部实现 |
| **生命周期** | close-budget 调用 flushRunningMethods | `mutators.ts: name === 'close-budget'` |
| **路径安全** | upload-file-web 仅浏览器端执行 | `uploadFileWeb` Platform.isBrowser 判断 |

---

## 7. 最终结论

### 7.1 关键证据对齐结论

| 事项 | 最终结论 | 代码证据 |
|------|---------|----------|
| Mutator 作用 | Mutator = 顺序执行 + Undo 上下文，**不是数据库事务** | `mutators.ts` 无 BEGIN/COMMIT/ROLLBACK |
| saveSyncedPrefs 时序 | **先 await 所有 send 成功，再更新本地 Redux** | `prefsSlice.ts:123-131` |
| 预算写操作风险控制 | 每个方法 try-catch 策略不同，**无统一模式** | `budgetfiles/app.ts` 各方法独立实现 |

### 7.2 架构设计洞察

1. **Mutator 是并发控制机制**
   - 解决：多标签页并发写竞态
   - 不解决：数据库原子性、崩溃恢复

2. **SharedWorker 是路由中枢**
   - `requestToPort` UUID 映射实现端到端回调追踪
   - Leader 单实例 + Follower 多标签页架构

3. **saveSyncedPrefs 是悲观更新模式**
   - 等待所有远程请求成功后才更新本地状态
   - 确保本地与远程数据一致性
